\documentclass[12pt,letterpaper]{report}

% =========================
% PAQUETES BÁSICOS
% =========================
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\usepackage{geometry}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{float}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{csquotes}
\usepackage[backend=biber,style=ieee,sorting=none]{biblatex}
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{fancyhdr}

\usetikzlibrary{arrows.meta,positioning}

\addbibresource{Reference/references.bib}

\newcommand{\autosenseicon}[1]{\includegraphics[height=0.42cm]{Figures/icons/#1.png}}
\newcommand{\autosenseiconsmall}[1]{\includegraphics[height=0.32cm]{Figures/icons/#1.png}}

% =========================
% FORMATO FISC / UTP
% =========================
\geometry{
    letterpaper,
    left=1.5in,
    right=1in,
    top=1.5in,
    bottom=1in
}

\onehalfspacing
\setlength{\emergencystretch}{3em}

\addto\captionsspanish{%
    \renewcommand{\tablename}{Tabla}%
    \renewcommand{\listtablename}{Índice de tablas}%
}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=black,
    citecolor=black,
    pdftitle={Sistema Edge-IoT con inteligencia artificial para el monitoreo predictivo de vehículos},
    pdfauthor={Antonio Zhong Qiu},
    pdfsubject={Trabajo de graduación},
    pdfkeywords={AutoSense, OBD-II, Edge-IoT, ESP32-S3, inteligencia artificial, monitoreo vehicular}
}

% =========================
% FORMATO DE CAPÍTULOS
% =========================
\titleformat{\chapter}[display]
{\normalfont\bfseries\centering}
{\MakeUppercase{\chaptername}\ \thechapter}
{12pt}
{\MakeUppercase}

\titlespacing*{\chapter}{0pt}{0pt}{20pt}

\titleformat{\section}
{\normalfont\bfseries}
{\thesection}
{1em}
{}

\titleformat{\subsection}
{\normalfont\bfseries}
{\thesubsection}
{1em}
{}

% =========================
% ENCABEZADO Y PIE
% =========================
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

% =========================
% DATOS DE PORTADA
% =========================
\newcommand{\universidad}{UNIVERSIDAD TECNOLÓGICA DE PANAMÁ}
\newcommand{\facultad}{FACULTAD DE INGENIERÍA DE SISTEMAS COMPUTACIONALES}
\newcommand{\carrera}{LICENCIATURA EN INGENIERÍA DE SISTEMAS Y COMPUTACIÓN}
\newcommand{\titulo}{SISTEMA EDGE-IOT CON INTELIGENCIA ARTIFICIAL PARA EL MONITOREO PREDICTIVO DE VEHÍCULOS}
\newcommand{\autor}{ANTONIO ZHONG QIU}
\newcommand{\cedula}{8-980-821}
\newcommand{\asesor}{DR. LUIYIANA DEL C. PÉREZ.}
\newcommand{\lugar}{PANAMÁ}
\newcommand{\fecha}{2026}

\begin{document}

% =========================
% PORTADA
% =========================
\hypersetup{pageanchor=false}
\begin{titlepage}
    \centering

    {\bfseries \universidad \par}
    \vspace{0.5cm}

    {\bfseries \facultad \par}
    \vspace{0.5cm}

    {\bfseries \carrera \par}

    \vfill

    {\bfseries\Large \titulo \par}

    \vfill

    {\bfseries PRESENTADO POR:\par}
    \vspace{0.3cm}
    {\autor \par}
    {\cedula \par}

    \vspace{1.5cm}

    {\bfseries ASESOR:\par}
    \vspace{0.3cm}
    {\asesor \par}

    \vfill

    {\lugar \par}
    {\fecha \par}

\end{titlepage}
\clearpage
\hypersetup{pageanchor=true}

% =========================
% PÁGINAS PRELIMINARES
% =========================
\pagenumbering{roman}

\chapter*{Dedicatoria}
\addcontentsline{toc}{chapter}{Dedicatoria}

A mi familia, por su apoyo constante, paciencia y acompañamiento durante cada etapa de mi formación académica y del desarrollo de este proyecto.

A la profesora Luiyiana Pérez, por su guía, orientación y acompañamiento académico durante este trabajo.

A Ricardo Calvo, Juan Mesa, Christopher Díaz y Alan Delgado, por su apoyo, amistad y colaboración en distintos momentos de este proceso.

\newpage

\chapter*{Agradecimientos}
\addcontentsline{toc}{chapter}{Agradecimientos}

Agradezco a la Universidad Tecnológica de Panamá y a la Facultad de Ingeniería de Sistemas Computacionales por la formación recibida durante la carrera. Extiendo mi agradecimiento a mi asesora, a los docentes que aportaron a mi desarrollo profesional y a las personas que colaboraron directa o indirectamente en las pruebas, revisión y mejora del sistema AutoSense.

\newpage

\chapter*{Resumen}
\addcontentsline{toc}{chapter}{Resumen}

AutoSense es un prototipo Edge-IoT para monitoreo vehicular mediante OBD-II/CAN. El proyecto integra un adaptador con ESP32-S3, firmware de adquisición, análisis local, Bluetooth Low Energy, una aplicación móvil desarrollada con React Native/Expo y servicios Firebase para perfiles, sesión, sincronización y respaldo histórico.

El sistema consulta el vehículo en modo de solo lectura. No borra códigos de falla, no reprograma módulos y no envía comandos de control a la ECU. El firmware descubre PIDs disponibles, aplica el perfil activo, guarda muestras binarias en el ESP32-S3 y transmite telemetría a la aplicación. Para el análisis local se usan estadística incremental con Welford, Z-score por señal y Tiny Isolation Forest, aplicados como apoyo para reconocer desviaciones dentro del alcance validado.

La validación se realizó en banco, con simulador OBD-II y con un Volkswagen Passat 2016. La campaña principal reunió 86\,998 registros clasificados como \texttt{city}, \texttt{highway} y \texttt{unknown}. Las señales analizadas incluyeron velocidad, RPM, temperatura del refrigerante, carga del motor y consumo instantáneo. Los resultados muestran que el prototipo adquiere, almacena, visualiza y analiza datos vehiculares reales. El componente predictivo se mantiene en el nivel de seguimiento y detección de desviaciones; no constituye un diagnóstico mecánico definitivo.

\textbf{Palabras clave:} OBD-II, CAN Bus, ESP32-S3, Edge-IoT, Bluetooth Low Energy, Firebase, inteligencia artificial, monitoreo vehicular, mantenimiento predictivo.

\newpage

\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

AutoSense is an Edge-IoT prototype for vehicle monitoring through OBD-II/CAN. It combines an ESP32-S3 adapter, embedded firmware, Bluetooth Low Energy, a React Native/Expo mobile app, and Firebase services for profiles, user session, synchronization, and historical backup.

The system queries the vehicle in read-only mode. It does not clear diagnostic trouble codes, reprogram modules, or send ECU control commands. The firmware discovers available PIDs, applies the active profile, stores compact binary samples on the ESP32-S3, and streams telemetry to the app. Local analysis uses Welford statistics, per-signal Z-score, and Tiny Isolation Forest as support for detecting deviations within the tested scope.

Validation was carried out on a bench setup, with an OBD-II simulator, and with a 2016 Volkswagen Passat. The main campaign produced 86,998 records classified as \texttt{city}, \texttt{highway}, and \texttt{unknown}. The analyzed signals included speed, RPM, coolant temperature, engine load, and instantaneous fuel consumption. The prototype acquired, stored, displayed, and analyzed real vehicle data. Its predictive scope is limited to monitoring and deviation detection, not final mechanical diagnosis.

\textbf{Keywords:} OBD-II, CAN Bus, ESP32-S3, Edge-IoT, Bluetooth Low Energy, Firebase, artificial intelligence, vehicle monitoring, predictive maintenance.

\newpage

% =========================
% ÍNDICES
% =========================
\tableofcontents
\newpage

\listoffigures
\addcontentsline{toc}{chapter}{Índice de figuras}
\newpage

\listoftables
\addcontentsline{toc}{chapter}{Índice de tablas}
\newpage

% =========================
% CUERPO DEL DOCUMENTO
% =========================
\pagenumbering{arabic}

\chapter*{Introducción}
\addcontentsline{toc}{chapter}{Introducción}

Los vehículos modernos producen datos durante todo el recorrido: velocidad, temperatura, carga del motor, voltaje, consumo, códigos de diagnóstico y otros parámetros generados por sus módulos electrónicos. En la práctica, buena parte de esa información se queda en el tablero, en un escáner usado de forma ocasional o en una revisión que llega cuando la falla ya es visible.

AutoSense parte de una idea concreta: leer esos datos cerca del vehículo y procesarlos sin depender todo el tiempo de la nube. Los enfoques de Internet de los Vehículos y computación en el borde respaldan esa decisión, porque permiten trabajar con menor latencia y conservar información útil aun cuando la conectividad sea irregular \cite{zhang2020_iov_edge,kalalas2025_ai_driven_monitoring}.

El puerto OBD-II es la entrada natural para esa lectura, ya que permite consultar información diagnóstica sin modificar la arquitectura original del automóvil. La literatura reciente muestra usos que van más allá de leer códigos de falla: detección de anomalías, análisis de conducción, estimación de consumo y apoyo a la seguridad mediante aprendizaje automático \cite{michailidis2025_obdii_review}. Aun así, cada vehículo responde solo a los parámetros que su fabricante expone, por lo que el sistema necesita validar el perfil antes de interpretar las señales \cite{sae2022_j1978_obdii_scan_tool,sae2025_j1979_diagnostic_test_modes}.

A pesar de esa disponibilidad de datos, el desafío principal sigue siendo interpretarlos de manera oportuna. En muchos casos, el mantenimiento de un vehículo continúa dependiendo de acciones reactivas, es decir, se interviene cuando el problema ya se manifestó, o de revisiones preventivas basadas únicamente en calendario o kilometraje. Ese esquema puede ser insuficiente cuando aparecen cambios graduales en variables críticas como temperatura, voltaje, carga del motor o códigos de error, ya que tales variaciones no siempre generan una advertencia inmediata en el tablero. La investigación reciente en mantenimiento predictivo destaca precisamente la necesidad de usar modelos de análisis capaces de detectar señales tempranas y patrones anómalos antes de que el deterioro se convierta en una falla evidente \cite{mahale2025_review_predictive_maintenance,tormos2025_explainable_ai}.

La computación en el borde reduce dos problemas frecuentes en sistemas vehiculares: latencia y dependencia de conectividad. En lugar de enviar cada medición a un servidor remoto, el procesamiento local filtra, agrupa o infiere parte de la información cerca del vehículo. Así, el sistema puede seguir funcionando durante trayectos con cobertura variable y recursos limitados \cite{zhang2020_iov_edge,minamy2025_edgecomputing,kalalas2025_ai_driven_monitoring}.

Con ese enfoque, la propuesta integra lectura OBD-II, procesamiento en el borde, almacenamiento en la nube y visualización móvil para convertir datos del vehículo en información comprensible para el conductor. Su alcance se centra en identificar condiciones anómalas y emitir alertas tempranas, manteniendo la compatibilidad con vehículos que ya cuentan con infraestructura de diagnóstico \cite{selvaraj2023_advanced_telematics,michailidis2025_obdii_review}.

Esta investigación diseña e implementa una solución funcional que integra adquisición de datos, transmisión, almacenamiento y visualización de parámetros críticos del vehículo. El sistema aprovecha recursos ya presentes en el automóvil, evita modificaciones invasivas y construye una base técnica para supervisión continua y apoyo a decisiones de mantenimiento \cite{mahale2025_review_predictive_maintenance,sae2022_j1978_obdii_scan_tool}.

\newpage

% =========================
% CAPÍTULO 1
% =========================
\chapter{Aspectos generales}

\section{Descripción de la problemática}

Los vehículos tradicionales no cuentan con un seguimiento continuo de su estado mecánico, lo que puede derivar en averías imprevistas, costos de reparación elevados y riesgos para la seguridad vial. Aunque la mayoría de los siniestros viales se relaciona con factores humanos, estudios de seguridad vehicular también documentan razones críticas asociadas al vehículo, especialmente neumáticos, ruedas y frenos \cite{nhtsa2018_critical_reasons_crashes}. En el contexto local, reportes periodísticos han señalado la incidencia de fallas mecánicas y mantenimiento insuficiente como factores de riesgo para la conducción \cite{laprensa2025_fallas_vehiculos,laprensa2025_fallas_autos}. Estas situaciones evidencian la necesidad de detectar a tiempo condiciones irregulares antes de que se conviertan en riesgos mayores.

El mantenimiento vehicular suele apoyarse en dos enfoques: reparación después de la falla o revisiones preventivas por kilometraje y calendario. El primero llega tarde; el segundo puede pasar por alto cambios rápidos entre revisiones. Muchos conductores dependen casi por completo de la luz de ``Check Engine'' u otras señales básicas del tablero, que por lo general aparecen cuando el problema ya se manifestó \cite{selvaraj2023_advanced_telematics}. Por ello, variaciones sostenidas en temperatura, voltaje, carga del motor, consumo o códigos de diagnóstico pueden pasar inadvertidas hasta convertirse en fallas más serias.

AutoSense responde a ese problema con una lectura continua de los datos que el vehículo ya genera. El aporte de IoT está en conectar esa información con el usuario; el aporte de la inteligencia artificial ligera está en comparar las señales con su comportamiento aprendido y señalar desviaciones \cite{kalalas2025_ai_driven_monitoring}. Trabajos basados en OBD-II han mostrado que estos datos sirven para supervisión remota y alertas de mantenimiento \cite{singh2021_obdii_iot,dabarera2022_iot_vehicle_management}. Otros estudios, enfocados en diagnóstico embarcado y análisis explicable, refuerzan la necesidad de mostrar no solo la alerta, sino también las señales que la originan \cite{tormos2025_explainable_ai,mahale2025_review_predictive_maintenance}.

El problema se concentra en cómo aprovechar la infraestructura existente en los vehículos, especialmente el puerto OBD-II y sus sensores asociados, para ofrecer al conductor supervisión accesible y alertas tempranas en tiempo real. Los vehículos compatibles con OBD-II pueden beneficiarse de un sistema que convierta datos disponibles en información útil para conducción y mantenimiento, siempre que expongan señales suficientes para el perfil aplicado \cite{mahale2025_review_predictive_maintenance}.

\section{Objetivo general}
Diseñar e implementar una arquitectura IoT vehicular con procesamiento en el borde e inteligencia artificial ligera para monitorear en tiempo real parámetros críticos disponibles por OBD-II/CAN, registrar historial operativo, detectar desviaciones y visualizar la información mediante una aplicación móvil.

Para mantener la coherencia con el alcance validado, en esta tesis el diagnóstico se interpreta como monitoreo, lectura de parámetros, detección de desviaciones y generación de alertas tempranas. Las funciones de control remoto o interacción activa con el vehículo se conservan únicamente como línea futura, no como una función implementada ni validada sobre el vehículo real.
\section{Objetivos específicos}

\begin{enumerate}
\item Diseñar un sistema de adquisición de datos vehiculares mediante la lectura de parámetros disponibles por OBD-II/CAN, incluyendo RPM, velocidad, temperatura del refrigerante, carga del motor, voltaje de la ECU, posición del acelerador, MAP/MAF y consumo estimado cuando el vehículo los exponga.
\item Implementar un módulo de análisis de datos vehiculares que identifique variaciones anómalas en parámetros críticos del vehículo y genere alertas tempranas para apoyar la toma de decisiones del conductor.
\item Implementar un entorno en la nube para el almacenamiento estructurado de los datos vehiculares procesados, que permita consultas históricas, visualización remota y respaldo de la información.
\item Diseñar e integrar una aplicación móvil multiplataforma que permita a los usuarios visualizar el estado del vehículo en tiempo real, recibir alertas y consultar el historial de funcionamiento del sistema.
\item Validar el funcionamiento del sistema completo mediante pruebas prácticas de adquisición, comunicación, almacenamiento, visualización y análisis local, utilizando indicadores verificables de estabilidad, cobertura de señales, continuidad del registro y coherencia de resultados.
\end{enumerate}

\section{Justificación}

El trabajo se justifica por su aporte académico y técnico al integrar diagnóstico OBD-II, firmware sobre ESP32-S3, comunicación BLE, procesamiento de datos y visualización móvil. El problema no se aborda como una simple lectura de códigos, sino como un sistema compuesto por adquisición, análisis, almacenamiento y presentación de información. Esta orientación coincide con la evolución reciente del monitoreo vehicular basado en datos, el análisis en el borde y las aplicaciones de aprendizaje automático sobre OBD-II \cite{michailidis2025_obdii_review,zhang2020_iov_edge,kalalas2025_ai_driven_monitoring}.

Desde el punto de vista práctico, la propuesta aprovecha la infraestructura ya presente en vehículos compatibles con OBD-II para convertir mediciones de operación en alertas comprensibles para el conductor. Los signos de deterioro no siempre aparecen como una falla evidente en el tablero; por eso, la lectura continua de parámetros críticos puede apoyar decisiones de mantenimiento más oportunas. El sistema no sustituye el criterio mecánico: complementa la supervisión tradicional con información accesible en tiempo real \cite{sae2022_j1978_obdii_scan_tool,mahale2025_review_predictive_maintenance,tormos2025_explainable_ai}.

La solución tiene un componente económico y social. Al utilizar un ESP32-S3, una aplicación desarrollada con Expo/React Native y servicios en la nube para autenticación, sincronización y almacenamiento, el proyecto evita depender de equipos especializados de diagnóstico que suelen ser más costosos. La detección temprana de anomalías y el historial de funcionamiento pueden reducir paradas imprevistas, gastos correctivos y riesgos asociados a fallas no atendidas a tiempo.

El desarrollo se organizó en etapas verificables: adquisición de datos, transmisión, almacenamiento, visualización, validación y ajuste. Esta separación permite evaluar cada componente por separado y después verificar el comportamiento del sistema completo. Firebase aporta gestión de usuarios, perfiles vehiculares, datos e historial, dejando una base reutilizable para ampliar métricas, alertas o compatibilidad con más vehículos.

\section{Alcance}

El alcance de este trabajo comprende el diseño, desarrollo e integración de un prototipo funcional de monitoreo vehicular basado en la lectura de datos OBD-II, el procesamiento local en un ESP32-S3, la transmisión inalámbrica mediante BLE y la visualización de información en una aplicación móvil. La propuesta se orienta a vehículos que dispongan de una interfaz OBD-II compatible y se apoya en el uso de datos embarcados para supervisar el comportamiento del vehículo, identificar condiciones anómalas y presentar alertas tempranas al usuario \cite{sae2022_j1978_obdii_scan_tool,michailidis2025_obdii_review}.

Funcionalmente, el sistema abarca las siguientes partes:

\begin{itemize}
    \item Adquisición de parámetros vehiculares desde el puerto OBD-II mediante el firmware del ESP32-S3.
    \item Descubrimiento de PIDs soportados, lectura de parámetros críticos, generación de muestras compactas y detección local de anomalías.
    \item Comunicación BLE entre el dispositivo embarcado y la aplicación móvil.
    \item Visualización de telemetría en tiempo real, alertas, historial básico y administración de perfiles de vehículo desde la aplicación desarrollada con Expo y React Native.
    \item Sincronización y resguardo de información mediante servicios en la nube, utilizando Firebase como soporte para autenticación, almacenamiento y funciones de aplicación.
\end{itemize}

El alcance incluye pruebas de funcionamiento sobre perfiles vehiculares compatibles y escenarios controlados de validación. En esta etapa, el perfil evaluado fue Volkswagen Passat 2016; en otros vehículos, la compatibilidad depende de que el puerto OBD-II exponga parámetros equivalentes y configuraciones semejantes. La solución usa inteligencia en el borde y monitoreo vehicular para reducir latencia y entregar información procesada con mayor oportunidad \cite{zhang2020_iov_edge,kalalas2025_ai_driven_monitoring}.

Por esa razón, el prototipo se concibe como una solución de observación y apoyo al usuario, sin capacidad de intervención directa sobre los sistemas del vehículo.

\section{Limitaciones}

La propuesta presenta limitaciones derivadas tanto de la diversidad de plataformas vehiculares como del alcance experimental del prototipo. En particular, el comportamiento del sistema depende de la compatibilidad del puerto OBD-II, del soporte de PIDs disponibles y de la estabilidad de la comunicación entre el ESP32-S3, la aplicación móvil y los servicios en la nube \cite{sae2022_j1978_obdii_scan_tool,michailidis2025_obdii_review,zhang2020_iov_edge}. La validación práctica se concentró en el perfil de Volkswagen Passat 2016, por lo que la extensión a otros modelos queda sujeta a prueba y ajuste específicos.

\begin{itemize}
    \item La compatibilidad, el conjunto de parámetros y la frecuencia de actualización pueden variar entre vehículos según los PIDs expuestos y el perfil aplicado.
    \item La validación se limita a unidades compatibles y a escenarios controlados, por lo que el desempeño debe confirmarse antes de extenderlo a otros modelos.
    \item Las alertas generadas tienen carácter orientativo y no sustituyen un diagnóstico mecánico especializado.
    \item La sincronización de historial y perfiles depende de la disponibilidad de conectividad y de los servicios configurados en Firebase.
    \item El módulo de inteligencia artificial fue validado como detección de desviaciones sobre datos reales de operación normal y escenarios de simulación, no como clasificador de fallas mecánicas reales etiquetadas.
    \item Las métricas de latencia extremo a extremo, pérdida de paquetes BLE y tiempo de sincronización histórica requieren instrumentación adicional para una evaluación de rendimiento más estricta.
\end{itemize}

Por eso, los resultados deben interpretarse como evidencia de funcionamiento del prototipo dentro de un entorno acotado, no como una cobertura universal de la totalidad del parque automotor.

\newpage

% =========================
% CAPÍTULO 2
% =========================
\chapter{Marco teórico}

\section{Antecedentes}

La evolución del diagnóstico vehicular ha estado marcada por la estandarización de la interfaz OBD-II, que permitió acceder a datos de la ECU sin intervenir físicamente la arquitectura original del automóvil. Ese cambio transformó el uso de los sistemas embarcados: de la lectura ocasional de códigos de falla se pasó a la consulta sistemática de variables de operación, estados de emisiones y condiciones de conducción. En la literatura reciente, OBD-II ya no se interpreta únicamente como una herramienta de diagnóstico, sino como una fuente estructurada de datos para monitoreo continuo, analítica vehicular y soporte al mantenimiento predictivo, especialmente cuando se combina con técnicas de aprendizaje automático y con esquemas de conectividad más cercanos al vehículo \cite{sae2022_j1978_obdii_scan_tool,michailidis2025_obdii_review,mahale2025_review_predictive_maintenance}.

Shafi et al. propusieron un sistema de monitoreo remoto de salud vehicular y mantenimiento prognóstico. En ese trabajo, la información del vehículo se adquiría durante la operación normal, se transmitía a un servidor para análisis y se complementaba con una aplicación móvil que mostraba el estado del sistema al usuario. El antecedente demuestra que la supervisión vehicular puede extenderse más allá del tablero del automóvil y convertirse en un flujo de datos para anticipar fallas, registrar comportamiento histórico y apoyar decisiones preventivas antes de que la avería sea visible. También introduce una separación que resulta útil para AutoSense: adquisición, transporte, análisis y presentación de resultados \cite{shafi2018_vehicle_remote_health}.

La convergencia entre OBD-II e Internet de las Cosas acercó el diagnóstico al usuario final. Singh et al. conectaron la lectura OBD-II con un teléfono mediante Bluetooth, una arquitectura de bajo costo que permite consulta remota y visualización inmediata. Dabarera et al. trabajaron en gestión vehicular con rastreo, diagnóstico y telemetría OBD-II. En ambos casos, el valor no está solo en leer el puerto de diagnóstico, sino en llevar esa lectura a una interfaz que el usuario pueda revisar durante el uso cotidiano del vehículo \cite{singh2021_obdii_iot,dabarera2022_iot_vehicle_management}.

Más allá del diagnóstico mecánico, varias investigaciones han usado datos OBD-II para estudiar conducción y consumo. DrivingStyles, propuesto por Meseguer et al., adopta minería de datos y redes neuronales para clasificar estilos de conducción a partir de variables de la ECU y calcular en tiempo real el consumo y el impacto ambiental de vehículos a gasolina y diésel. Este antecedente muestra que una misma infraestructura de adquisición puede interpretar comportamiento del conductor, estimar eficiencia y ofrecer retroalimentación operativa. Para AutoSense, confirma la utilidad de combinar datos vehiculares, visualización móvil y análisis de datos sin depender de sensores externos adicionales \cite{meseguer2017_drivingstyles}.

Campos-Ferreira et al. presentaron un sistema de monitoreo integrado de conductor y vehículo. El estudio combina sensores a bordo y remotos para observar variables del vehículo y del usuario en una arquitectura configurable y replicable. Su aporte está en ampliar el análisis de movilidad: no observa solo el estado mecánico, sino también el contexto de uso y la interacción humana. Esta visión se relaciona con AutoSense porque la telemetría, la interfaz y la interpretación de datos se tratan como partes de un mismo sistema, no como módulos aislados \cite{camposferreira2023_vehicle_driver_monitoring}.

La compatibilidad entre vehículos aparece como un problema repetido. Yen et al. trabajaron con un módulo OBD-II universal, distintos estándares CAN y aprendizaje profundo para estimar consumo de combustible \cite{yen2021_universal_obdii}. La parte que interesa a esta tesis es práctica: no basta con conectar un adaptador y asumir que todos los vehículos responden igual. Hay que descubrir señales, normalizar respuestas y validar cada modelo. Por eso AutoSense usa perfiles vehiculares y tomó el \texttt{vw\_passat\_2016} como caso verificado antes de extender compatibilidad.

Los trabajos sobre Internet de los Vehículos y computación en el borde desplazan parte del análisis hacia el vehículo o hacia nodos cercanos. Zhang y Letaief lo plantean como una forma de reducir latencia; Kalalas et al. lo usan para monitoreo de condición; Selvaraj et al. lo relacionan con analítica en tiempo real y mantenimiento \cite{zhang2020_iov_edge,kalalas2025_ai_driven_monitoring,selvaraj2023_advanced_telematics}. En conjunto, estas fuentes apoyan una decisión de diseño de AutoSense: capturar datos por OBD-II, interpretarlos cerca del vehículo y enviar al usuario una salida ya procesada, no una lista cruda de bytes \cite{michailidis2025_obdii_review,tormos2025_explainable_ai}.

Estos antecedentes muestran una transición clara: el vehículo pasó de ser un sistema de diagnóstico puntual a una fuente de datos para monitoreo continuo, análisis contextual y apoyo a decisiones de mantenimiento. AutoSense se ubica en esa línea con una arquitectura distribuida que separa adquisición en el ESP32-S3, transmisión BLE, visualización móvil y sincronización de perfiles e historial en la nube. La diferencia práctica está en la validación sobre un perfil vehicular específico, \texttt{vw\_passat\_2016}, lo que permite partir de una base real antes de ampliar compatibilidad a otros modelos.

\section{Diagnóstico vehicular}
\label{sec:diagnostico-vehicular}

El diagnóstico vehicular observa, interpreta y valida la información generada por los sistemas electrónicos del automóvil para identificar fallas, detectar deterioro o confirmar el estado de sus componentes críticos. En los vehículos modernos, esta tarea se apoya en la ECU y en los módulos asociados, que registran estados operativos, códigos de falla y variables de funcionamiento. La estandarización OBD-II formalizó parte de esa interacción al definir un conjunto mínimo de información que puede ser consultado por herramientas externas de diagnóstico, lo que convierte al puerto OBD en una interfaz práctica para revisión técnica, mantenimiento y supervisión continua \cite{sae2022_j1978_obdii_scan_tool,epa2016_obd_information_exchange}.

Desde esa perspectiva, el diagnóstico no equivale a predecir con exactitud una falla mecánica. Consiste en leer de forma estructurada el estado del vehículo a partir de señales observables. Un escáner OBD-II no reemplaza el criterio técnico del mantenimiento mecánico, pero aporta datos consistentes para reconocer desviaciones, confirmar síntomas y orientar el análisis. La información diagnóstica actúa como una capa intermedia entre el comportamiento físico del vehículo y la interpretación del usuario o del técnico \cite{sae2022_j1978_obdii_scan_tool,epa2016_obd_information_exchange}.

Los DTC o \textit{Diagnostic Trouble Codes} son una de las lecturas básicas del diagnóstico vehicular. Cuando la ECU detecta una condición fuera de rango, puede guardar un código y activar el indicador MIL, conocido como \textit{Check Engine}. Ese código no equivale a una reparación. Normalmente identifica el sistema afectado o la condición detectada, mientras que la causa raíz exige análisis adicional. Por eso, en esta tesis se separan tres ideas: síntoma observado, código reportado y causa probable \cite{epa2016_obd_information_exchange}.

Los DTC pueden acompañarse de datos de contexto. El \textit{freeze frame} guarda una fotografía técnica del momento en que se registró la condición de falla: velocidad, temperatura, carga, RPM o tensión de la ECU. Esa captura ayuda a revisar qué estaba ocurriendo cuando apareció el código. También se distinguen códigos almacenados, pendientes y permanentes, porque cada uno representa un nivel distinto de confirmación. AutoSense usa esa información para mostrar la alerta con contexto y dejar una referencia en el historial.

Los parámetros en tiempo real, o \textit{live data}, permiten observar el vehículo mientras está funcionando. A diferencia de un DTC, que registra un evento, estos valores muestran el estado operativo en cada consulta: RPM, velocidad, temperatura del refrigerante, acelerador, carga del motor, flujo de aire, presión del múltiple o voltaje de la ECU. AutoSense no parte de una lista fija. Primero consulta qué PIDs responde el vehículo y después arma el plan de lectura según el perfil activo. Esa decisión es necesaria porque las señales disponibles cambian entre marcas, modelos y ECUs \cite{yen2021_universal_obdii}.

La lectura de \textit{live data} también sirve para reconocer cambios graduales. Una temperatura que se eleva con más frecuencia, una carga fuera de lo habitual o un voltaje que se aparta de su patrón normal no siempre generan un DTC, pero quedan reflejados en la serie histórica. AutoSense usa esa continuidad para comparar sesiones y señalar desviaciones frente a una línea base. Ese uso de datos OBD-II coincide con trabajos recientes sobre mantenimiento predictivo e inteligencia artificial aplicada al vehículo \cite{michailidis2025_obdii_review,mahale2025_review_predictive_maintenance,tormos2025_explainable_ai}.

La lectura de \textit{readiness} agrega otra pista. Indica si los monitores internos de emisiones terminaron sus comprobaciones o si falta parte del ciclo de diagnóstico. Si esos monitores no están completos, la ausencia de códigos no basta para cerrar la revisión. AutoSense consulta también Mode 09 para obtener el VIN y escoger el perfil del automóvil.

AutoSense consulta el vehículo en modo de solo lectura. El ESP32-S3 pide datos por CAN, decodifica servicios OBD-II permitidos y envía el resultado por BLE. El firmware bloquea borrado de DTC, reprogramación y comandos de control. Así, la prueba no cambia el estado diagnóstico que se está observando.

La compatibilidad entre vehículos requiere verificación. OBD-II define una base común, pero cada fabricante puede variar PIDs disponibles, respuestas de ECU y datos expuestos. Por eso, AutoSense usa perfiles de vehículo. En este trabajo la validación corresponde al Volkswagen Passat 2016; otros modelos quedan sujetos a pruebas de disponibilidad y respuesta.

En esta tesis, el diagnóstico vehicular aporta la base de datos: estado actual, historial y señales para el análisis de anomalías. Los detalles de OBD-II, CAN y PIDs se separan en los apartados siguientes.

\begin{figure}[H]
    \centering
    \resizebox{0.96\textwidth}{!}{
    \begin{tikzpicture}[
        node distance=1.1cm,
        block/.style={draw, rounded corners=4pt, align=center, minimum height=1.05cm, minimum width=2.45cm, font=\small, line width=0.6pt},
        data/.style={draw, rounded corners=4pt, align=center, minimum height=0.85cm, minimum width=2.15cm, font=\scriptsize, line width=0.5pt},
        arrow/.style={-{Latex[length=2.2mm]}, line width=0.7pt}
    ]
        \node[block, fill=gray!12] (vehiculo) {\autosenseicon{vehicle}\\Vehículo\\ECU y sensores};
        \node[block, fill=blue!10, right=of vehiculo] (obd) {\autosenseicon{obd}\\Puerto OBD-II\\CAN};
        \node[block, fill=cyan!12, right=of obd] (esp) {\autosenseicon{esp32}\\ESP32-S3\\firmware AutoSense};
        \node[block, fill=green!12, right=of esp] (app) {\autosenseicon{phone}\\Aplicación móvil\\visualización};
        \node[block, fill=orange!14, right=of app] (historial) {\autosenseicon{cloud}\\Historial\\local y nube};

        \draw[arrow] (vehiculo) -- node[above, font=\scriptsize]{datos internos} (obd);
        \draw[arrow] (obd) -- node[above, font=\scriptsize]{lectura segura} (esp);
        \draw[arrow] (esp) -- node[above, font=\scriptsize]{BLE} (app);
        \draw[arrow] (app) -- node[above, font=\scriptsize]{sincronización} (historial);

        \node[data, fill=red!10, below=1.05cm of obd] (dtc) {\autosenseiconsmall{shield}\\DTC / MIL\\eventos};
        \node[data, fill=yellow!18, below=1.05cm of esp] (live) {\autosenseiconsmall{stats}\\Live data\\PIDs};
        \node[data, fill=purple!10, below=1.05cm of app] (freeze) {\autosenseiconsmall{log}\\Freeze frame\\contexto};
        \node[data, fill=teal!10, below=1.05cm of historial] (mode09) {\autosenseiconsmall{config}\\Readiness / Mode 09\\perfil};

        \draw[arrow] (obd.south) -- (dtc.north);
        \draw[arrow] (esp.south) -- (live.north);
        \draw[arrow] (app.south) -- (freeze.north);
        \draw[arrow] (historial.south) -- (mode09.north);
    \end{tikzpicture}
    }
    \caption{Esquemático de referencia del flujo de diagnóstico vehicular en AutoSense. Elaboración propia.}
    \label{fig:diagnostico-vehicular}
\end{figure}

\begin{table}[H]
    \centering
    \caption{Tipos de información utilizados en el diagnóstico vehicular de AutoSense. Elaboración propia.}
    \label{tab:diagnostico-autosense}
    \small
    \begin{tabular}{p{0.25\textwidth}p{0.40\textwidth}p{0.25\textwidth}}
        \toprule
        Nivel & Información principal & Observación \\
        \midrule
        DTC / MIL & Códigos de falla almacenados, pendientes y permanentes & Permite ubicar la condición detectada por la ECU \\
        Datos en vivo & RPM, velocidad, temperatura, carga, voltaje y otros PIDs & Se usa para el panel en tiempo real y el historial \\
        Freeze frame & Captura del estado del vehículo al momento de la falla & Aporta contexto para interpretar el evento \\
        Readiness & Estado de monitores y preparación del sistema & Ayuda a confirmar si las pruebas internas están completas \\
        Mode 09 & Información de identificación del vehículo, como VIN & Se usa para resolver el perfil correcto \\
        \bottomrule
    \end{tabular}
\end{table}

La figura organiza el flujo de observación que sigue AutoSense desde el vehículo hasta la aplicación móvil y el historial sincronizado. La tabla agrupa la información diagnóstica que el sistema utiliza, distinguiendo entre fallas detectadas, señales operativas y campos de identificación del perfil vehicular.

\section{Sistema OBD-II}
\label{sec:sistema-obd2}

El sistema OBD-II (\textit{On-Board Diagnostics, segunda generación}) es la interfaz estandarizada que permite acceder a información de diagnóstico y de supervisión del vehículo mediante herramientas externas de prueba. Su propósito central es brindar un punto común de consulta para que un escáner pueda identificar el estado de los sistemas relacionados con emisiones, funcionamiento del motor y autoverificación de componentes, sin depender por completo de la lógica interna de cada fabricante \cite{sae2025_j1979_diagnostic_test_modes,epa2016_vehicle_emissions_obd}.

En la práctica, OBD-II no es una sola pieza de hardware ni un bloque aislado de software. Es un conjunto de reglas que define qué información mínima debe estar disponible para un equipo genérico de diagnóstico y cómo debe solicitarse esa información. Por esa razón, el estándar resultó decisivo para la evolución de las herramientas de revisión vehicular, ya que permitió pasar de diagnósticos propietarios y cerrados a una consulta estructurada y repetible. En vehículos modernos, ese acceso se utiliza para inspección, mantenimiento preventivo, validación de emisiones y lectura de eventos que el controlador ha detectado durante la operación normal \cite{epa2016_vehicle_emissions_obd,sae2022_j1978_obdii_scan_tool}.

La relevancia del sistema OBD-II también se explica por su alcance regulatorio y operativo. La EPA ha señalado que los vehículos equipados con OBD pueden aportar datos oportunos y precisos para los programas de inspección y mantenimiento, y que esta tecnología forma parte de la estrategia de control de emisiones en vehículos ligeros desde modelos 1996 y posteriores \cite{epa2016_vehicle_emissions_obd}. De forma complementaria, la familia de normas SAE J1979 organiza los modos de diagnóstico y la información que puede solicitar una herramienta externa, mientras que SAE J1978 establece las condiciones generales para el funcionamiento de un escáner compatible con OBD-II \cite{sae2025_j1979_diagnostic_test_modes,sae2022_j1978_obdii_scan_tool}.

Para AutoSense, OBD-II constituye la capa de consulta que hace posible la lectura del vehículo sin intervenir sus funciones internas. El firmware del ESP32-S3 actúa como intermediario entre el puerto de diagnóstico y la aplicación móvil, de modo que el sistema obtiene la información, la interpreta y la presenta al usuario en una interfaz comprensible. En la configuración actual del proyecto, esta lógica se trabaja bajo una política de solo lectura y con un perfil vehicular verificado para Volkswagen Passat 2016, lo que reduce la ambigüedad al momento de validar qué señales están realmente disponibles en el automóvil.

\begin{figure}[H]
    \centering
    \resizebox{0.94\textwidth}{!}{
    \begin{tikzpicture}[
        node distance=0.95cm,
        block/.style={draw, rounded corners=4pt, align=center, minimum height=1cm, minimum width=2.5cm, font=\small, line width=0.6pt},
        smallblock/.style={draw, rounded corners=4pt, align=center, minimum height=0.78cm, minimum width=2.05cm, font=\scriptsize, line width=0.45pt},
        arrow/.style={-{Latex[length=2.2mm]}, line width=0.7pt}
    ]
        \node[block, fill=gray!10] (ecu) {\autosenseicon{vehicle}\\ECU y módulos\\del vehículo};
        \node[block, fill=blue!10, right=of ecu] (j1962) {\autosenseicon{obd}\\Conector OBD-II\\J1962};
        \node[block, fill=cyan!12, right=of j1962] (can) {\autosenseicon{can}\\CAN\\consulta/ respuesta};
        \node[block, fill=green!12, right=of can] (adapter) {\autosenseicon{esp32}\\Adaptador\\ESP32-S3};
        \node[block, fill=orange!14, right=of adapter] (mobile) {\autosenseicon{phone}\\App móvil\\AutoSense};

        \draw[arrow] (ecu) -- node[above, font=\scriptsize]{diagnóstico} (j1962);
        \draw[arrow] (j1962) -- node[above, font=\scriptsize]{pines 6/14} (can);
        \draw[arrow] (can) -- node[above, font=\scriptsize]{solo lectura} (adapter);
        \draw[arrow] (adapter) -- node[above, font=\scriptsize]{BLE} (mobile);

        \node[smallblock, fill=red!10, below=0.85cm of ecu] (mil) {\autosenseiconsmall{shield}\\MIL / DTC};
        \node[smallblock, fill=yellow!18, right=0.35cm of mil] (pid) {\autosenseiconsmall{stats}\\Mode 01\\PIDs};
        \node[smallblock, fill=purple!10, right=0.35cm of pid] (ready) {\autosenseiconsmall{config}\\Readiness\\Mode 06};
        \node[smallblock, fill=teal!10, right=0.35cm of ready] (vin) {\autosenseiconsmall{obd}\\Mode 09\\VIN};

        \draw[arrow] (mil.north) -- (ecu.south);
        \draw[arrow] (pid.north) -- (j1962.south);
        \draw[arrow] (ready.north) -- (can.south);
        \draw[arrow] (vin.north) -- (adapter.south);
    \end{tikzpicture}
    }
    \caption{Esquema conceptual del sistema OBD-II integrado a AutoSense. Elaboración propia.}
    \label{fig:obd2-autosense-general}
\end{figure}

OBD-II aporta una base común para leer fallas, estados de operación e identificación del vehículo. Con ella se pueden consultar DTC, pruebas internas, VIN y, cuando el vehículo lo permite, datos asociados al momento de una anomalía. AutoSense toma esa base y la ajusta por perfil vehicular; así evita escribir reglas sueltas para cada modelo y trabaja solo con señales verificadas \cite{sae2025_j1979_diagnostic_test_modes,epa2013_obd_readiness_best_practices}.

La separación por modos también ayuda a ordenar la consulta. Un código almacenado, una captura \textit{freeze frame}, un monitor de preparación y un VIN no representan el mismo tipo de dato. Por eso el firmware no pide todo de una sola vez: usa el modo que corresponde a cada lectura y conserva el significado de la respuesta dentro del flujo de AutoSense.

\begin{figure}[H]
    \centering
    \resizebox{0.88\textwidth}{!}{
    \begin{tikzpicture}[
        lane/.style={draw, rounded corners=4pt, align=center, minimum height=1.05cm, minimum width=2.85cm, font=\small, line width=0.6pt},
        msg/.style={draw, rounded corners=3pt, align=center, minimum height=0.78cm, minimum width=2.45cm, font=\scriptsize, line width=0.45pt},
        arrow/.style={-{Latex[length=2.2mm]}, line width=0.7pt},
        lab/.style={font=\scriptsize, fill=white, inner sep=1.5pt}
    ]
        \node[lane, fill=cyan!12] (autosense) at (0,3.2) {\autosenseicon{esp32}\\AutoSense\\ESP32-S3};
        \node[lane, fill=blue!10] (ecu) at (4.8,3.2) {\autosenseicon{vehicle}\\ECU\\vehicular};
        \node[lane, fill=green!12] (app) at (9.6,3.2) {\autosenseicon{phone}\\Aplicación\\móvil};

        \node[msg, fill=yellow!18] (req) at (0,1.15) {\autosenseiconsmall{obd}\\Solicitud\\\texttt{01 0C}};
        \node[msg, fill=orange!16] (res) at (4.8,1.15) {\autosenseiconsmall{can}\\Respuesta\\\texttt{41 0C A B}};
        \node[msg, fill=purple!10] (dec) at (9.6,1.15) {\autosenseiconsmall{stats}\\Decodificación\\RPM};
        \node[msg, fill=gray!10] (formula) at (4.8,-0.35) {\autosenseiconsmall{config}\\$((A \times 256)+B)/4$};

        \draw[arrow] ([yshift=5.5mm]autosense.east) -- node[lab, above=7pt, pos=0.48]{consulta OBD-II} ([yshift=5.5mm]ecu.west);
        \draw[arrow] ([yshift=-7.0mm]ecu.west) -- node[lab, below=7pt, pos=0.50]{respuesta OBD-II} ([yshift=-7.0mm]autosense.east);
        \draw[arrow] ([yshift=0mm]ecu.east) -- node[lab, above=7pt, pos=0.55]{telemetría BLE} ([yshift=0mm]app.west);

        \draw[arrow] (req) -- node[lab, above=4pt]{PID solicitado} (res);
        \draw[arrow] (res) -- node[lab, above=4pt]{bytes A/B} (dec);
        \draw[arrow] (res) -- (formula);
        \draw[arrow] (formula) -| (dec);
    \end{tikzpicture}
    }
    \caption{Secuencia conceptual de consulta y respuesta del sistema OBD-II. Elaboración propia.}
    \label{fig:obd2-secuencia}
\end{figure}

\subsection{Modos de diagnóstico relevantes}

En OBD-II, cada modo agrupa una intención de lectura. AutoSense usa principalmente Mode 01 para datos actuales, Mode 02 para \textit{freeze frame}, Mode 03 para DTC almacenados, Mode 06 para monitores a bordo, Mode 07 para DTC pendientes, Mode 09 para identificación del vehículo y Mode 0A para DTC permanentes \cite{sae2025_j1979_diagnostic_test_modes,epa2013_obd_readiness_best_practices}. La tabla siguiente resume esos usos sin convertir la sección en un catálogo completo del estándar.

Estos modos no responden igual en todos los vehículos. La disponibilidad cambia según modelo, año e implementación del fabricante; por eso, antes de leer una señal conviene verificar si el servicio está soportado. AutoSense trabaja con un perfil validado y con datos confirmados en pruebas locales. De esa forma, el sistema evita interpretar como disponible una señal que el vehículo no entrega.

La lectura de DTC es el uso más visible de OBD-II. El código apunta al sistema o condición que activó la alerta; no entrega, por sí solo, la reparación exacta. Si la ECU enciende el MIL, el conductor sabe que debe revisar el vehículo, pero la causa raíz todavía requiere análisis. Los DTC permanentes tienen una regla más estricta: la EPA indica que no se eliminan con un escáner convencional ni desconectando la batería; desaparecen cuando el sistema confirma que la condición ya no persiste \cite{epa2013_obd_readiness_best_practices}. AutoSense separa estos estados para no presentar todos los códigos con el mismo peso.

El estado de \textit{readiness} se interpreta junto con los DTC. Sus monitores indican si el vehículo ya ejecutó sus comprobaciones internas. Después de una reparación o de un borrado reciente, algunos monitores pueden seguir incompletos; en ese caso, la ausencia de DTC no basta para cerrar el diagnóstico \cite{epa2013_obd_readiness_best_practices}. Esta lectura da contexto y evita conclusiones rápidas.

La otra pieza clave del sistema es la información del vehículo. El modo 09 permite consultar datos como el VIN y otros identificadores útiles para reconocer el automóvil, asociarlo con un perfil y diferenciarlo de otros modelos. En AutoSense, el proceso de aplicación de perfiles parte de esa identificación para cargar únicamente la configuración compatible. Así, OBD-II no se limita a informar que existe una falla o que un monitor está listo, sino que también aporta datos de identidad para organizar el flujo completo de diagnóstico.

\begin{table}[H]
    \centering
    \caption{Modos OBD-II relevantes para AutoSense. Elaboración propia con base en los modos de diagnóstico citados.}
    \label{tab:modos-obd2-autosense}
    \small
    \begin{tabular}{p{0.12\textwidth}p{0.28\textwidth}p{0.42\textwidth}}
        \toprule
        Modo & Función principal & Uso en AutoSense \\
        \midrule
        01 & Datos actuales del vehículo & Panel en vivo y telemetría base \\
        02 & Freeze frame & Contexto histórico del momento de una falla \\
        03 & DTC almacenados & Identificación de fallas activas o registradas \\
        06 & Resultados de monitores & Evaluación de pruebas internas del sistema \\
        07 & DTC pendientes & Detección temprana de anomalías aún no confirmadas \\
        09 & Información del vehículo & Lectura de VIN e identificación para perfilado \\
        0A & DTC permanentes & Confirmación de fallas que aún no han sido eliminadas \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Integración de OBD-II en AutoSense}

En AutoSense, OBD-II ordena la lectura antes de que los datos lleguen a la aplicación móvil. El ESP32-S3 consulta servicios permitidos, normaliza la respuesta y la transmite por BLE. La intención no es reemplazar el sistema de a bordo ni intervenir la ECU; el prototipo aprovecha la información que el vehículo ya expone por el puerto de diagnóstico.

En el prototipo solo se consultan datos. El firmware no borra códigos, no cambia monitores y no envía órdenes de control a la ECU. Con esa regla, las pruebas sobre el mismo vehículo conservan la evidencia diagnóstica. La tesis trabaja sobre lectura, interpretación, visualización y registro. Los servicios de escritura no forman parte del alcance.

El perfil verificado corresponde al Volkswagen Passat 2016. Otro vehículo podría entregar señales equivalentes por OBD-II, pero tendría que pasar primero por pruebas de soporte y respuesta. Por esa razón, el documento defiende el caso validado y no una compatibilidad universal.

La codificación de PIDs, el bus CAN y la interpretación de respuestas se explican después. Aquí se deja establecido el papel de OBD-II: ser la entrada normalizada de los datos que AutoSense usa para monitoreo y análisis.

OBD-II aporta a AutoSense una base estandarizada, verificable y suficientemente amplia para capturar información útil del vehículo sin salir del dominio de la consulta autorizada. Su valor no está únicamente en leer códigos de falla, sino en estructurar el acceso a estados de operación, resultados de pruebas internas e identificación del automóvil. Esa capacidad de organización permite que el resto de la propuesta técnica tenga un punto de partida común y defendible.

\section{Protocolo CAN Bus}
\label{sec:protocolo-can-bus}

El protocolo CAN (\textit{Controller Area Network}) es una red de comunicación serial diseñada para el intercambio confiable de información entre unidades electrónicas del vehículo. En automoción, su función principal es conectar ECUs y módulos internos que comparten datos de operación en tiempo real dentro de una arquitectura distribuida. En esta investigación se trabaja con CAN clásico, debido a que constituye la base de comunicación asociada al flujo actual de lectura de AutoSense y al acceso de diagnóstico disponible desde el vehículo validado.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/can_high_speed_bus_levels.png}
        \par\vspace{0.15cm}
        {\footnotesize Niveles diferenciales CANH/CANL}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/can_bus_access_procedure.png}
        \par\vspace{0.15cm}
        {\footnotesize Procedimiento de acceso al bus}
    \end{minipage}
    \caption{Relación entre los niveles diferenciales del bus CAN y el arbitraje por prioridad, adaptado de Vector Informatik \cite{vector2021_can_bus_levels,vector2022_can_bitwise_arbitration}.}
    \label{fig:can-autosense-topologia}
\end{figure}

CAN clásico usa dos líneas, CANH y CANL. La información no se lee como un voltaje aislado, sino como la diferencia entre ambas señales. Esa forma de transmisión es útil dentro del automóvil, donde motores, actuadores y cargas eléctricas introducen ruido. Para AutoSense, la ventaja práctica es simple: el ESP32-S3 puede leer tramas con mayor estabilidad mientras el vehículo está encendido \cite{vector2021_can_bus,vector2021_can_bus_levels}.

El acceso al bus se decide por arbitraje bit a bit. Si dos nodos transmiten al mismo tiempo, gana el mensaje con identificador de mayor prioridad y el otro nodo espera sin dañar la trama. AutoSense respeta ese comportamiento: no intenta dominar el bus, solo realiza consultas permitidas y escucha respuestas dentro del flujo normal de la red \cite{vector2022_can_bitwise_arbitration}.

\begin{table}[H]
    \centering
    \caption{Elementos básicos del protocolo CAN y su relación con AutoSense. Elaboración propia con base en la documentación técnica citada.}
    \label{tab:can-autosense-elementos}
    \small
    \begin{tabular}{p{0.18\textwidth}p{0.36\textwidth}p{0.34\textwidth}}
        \toprule
        Elemento & Función & Relevancia en AutoSense \\
        \midrule
        CANH/CANL & Líneas diferenciales del bus & Mejoran la inmunidad al ruido y estabilizan la lectura \\
        ECU & Nodo que transmite y recibe marcos & Origina la información que el sistema consulta \\
        Arbitraje por ID & Resuelve el acceso al medio sin colisiones & Permite coexistir con el tráfico interno del vehículo \\
        Puerto OBD-II & Punto físico de acceso al bus & Conecta el ESP32-S3 sin modificar la red original \\
        Transceptor CAN & Adapta niveles eléctricos entre bus y controlador & Habilita la interfaz de lectura del prototipo \\
        \bottomrule
    \end{tabular}
\end{table}

En AutoSense, el protocolo CAN funciona como la capa de transporte que hace posible la adquisición de datos del vehículo a través del puerto de diagnóstico. El proyecto no busca reorganizar la red ni alterar la lógica interna del automóvil, sino aprovechar la infraestructura existente para obtener información de forma segura y repetible. Aquí se delimita el CAN como medio de comunicación; la interpretación de parámetros y la consulta de servicios específicos se tratan en los apartados posteriores. Esta separación evita repeticiones y deja claro que el protocolo de comunicación cumple una función de soporte dentro de la solución global.

\section{PIDs vehiculares}

Los \textit{Parameter Identifiers} o PIDs constituyen el mecanismo mediante el cual el estándar OBD-II expone magnitudes de diagnóstico normalizadas para consulta externa. Cada PID identifica una variable concreta y define la estructura básica de su respuesta, de modo que el equipo de escaneo pueda interpretar el dato sin depender de la marca o del modelo del vehículo \cite{sae2022_j1978_obdii_scan_tool,sae2025_j1979_diagnostic_test_modes,sae2011_j1979_da}. En AutoSense, esta abstracción evita trabajar con una lista abierta de señales: el sistema usa identificadores definidos en los perfiles del proyecto y validados según el vehículo de prueba.

\subsection{Definición y alcance de los PIDs}

En la práctica, AutoSense usa principalmente PIDs de \texttt{Mode 01}, es decir, de lectura de datos en tiempo real. Este modo concentra variables como régimen del motor, velocidad, temperatura, carga y estado de sensores básicos. El anexo digital SAE J1979-DA centraliza la definición de estos identificadores y de otros elementos normalizados, como TIDs, OBDMIDs y tipos de información, lo cual permite que el sistema trate la consulta de forma uniforme aun cuando el vehículo responda con valores distintos o con disponibilidad parcial de parámetros \cite{sae2025_j1979_diagnostic_test_modes,sae2011_j1979_da}.

AutoSense no intenta construir un catálogo completo de todos los PIDs posibles. La lógica del proyecto parte de dos perfiles reales: \texttt{generic\_obd2}, que agrupa las señales estándar más útiles para operación básica, y \texttt{vw\_passat\_2016}, que añade los identificadores verificados específicamente en el vehículo de referencia. Esta separación mantiene una lectura consistente de acuerdo con el perfil activo.

\subsection{Descubrimiento de PIDs y selección del perfil}

El proceso de descubrimiento comienza con las consultas de soporte del modo 01. AutoSense interroga los bloques \texttt{0100}, \texttt{0120}, \texttt{0140}, \texttt{0160}, \texttt{0180}, \texttt{01A0} y \texttt{01C0}, que actúan como mapas de disponibilidad. Con esas respuestas el firmware sabe qué PIDs declara la ECU y evita sondear parámetros que el vehículo no reporta.

Una vez obtenida la disponibilidad básica, el firmware solicita \texttt{Mode 09 PID 02} para recuperar el VIN. Ese dato se utiliza en la aplicación para resolver el perfil de vehículo correspondiente y descargar la configuración correcta desde el backend. En el flujo del proyecto, esta decisión es clave porque el mismo conjunto de respuestas puede interpretarse de manera diferente según la marca, el modelo y el año. Cuando el perfil resuelto es \texttt{vw\_passat\_2016}, el sistema conserva las señales confirmadas para ese vehículo y deja como opcionales aquellas que existen en el estándar, pero que aún no fueron validadas.

El archivo de perfil incluye la estructura \texttt{signals[]} porque cada señal necesita una regla explícita. El motor de consulta no recorre una lista libre: usa una \textit{allowlist} de PIDs y fórmulas. El perfil define qué se consulta, cada cuánto se consulta y cuáles señales permanecen deshabilitadas hasta una validación posterior.

\subsection{Conversión de respuesta bruta a unidades de ingeniería}

Un PID solo es útil cuando la respuesta cruda se convierte a una magnitud legible. AutoSense usa una lista cerrada de fórmulas. Para un byte acepta relaciones como \texttt{A}, \texttt{A-40} o \texttt{A*100/255}. Para dos bytes usa combinaciones big-endian como \texttt{((A*256)+B)/4}, \texttt{((A*256)+B)/100}, \texttt{((A*256)+B)/1000}, \texttt{A*256+B} y \texttt{(A-128)*100/128}. El firmware no ejecuta expresiones libres; aplica reglas ya verificadas en los perfiles.

Con ese criterio, el perfil común utiliza PIDs como \texttt{04} para carga del motor, \texttt{05} para temperatura del refrigerante, \texttt{0B} para presión del múltiple de admisión, \texttt{0C} para revoluciones por minuto, \texttt{0D} para velocidad del vehículo, \texttt{0F} para temperatura del aire de admisión, \texttt{11} para posición del acelerador y \texttt{42} para voltaje de la ECU. Estas señales constituyen la base de lectura en AutoSense porque ofrecen una visión operativa suficiente para registrar estado del motor, demanda del conductor y condiciones generales de funcionamiento. A partir de ellas, el sistema puede construir series de tiempo útiles para visualización y análisis posterior sin entrar en parámetros propietarios complejos.

En el perfil \texttt{vw\_passat\_2016}, AutoSense incorpora además los PIDs \texttt{06} y \texttt{07}, correspondientes a los ajustes de combustible a corto y largo plazo del banco 1, junto con \texttt{1F}, que reporta el tiempo de funcionamiento desde el arranque del motor. El perfil también conserva como opcionales otros PIDs estándar, entre ellos \texttt{10}, \texttt{2F}, \texttt{5C} y \texttt{5E}, que representan masa de aire, nivel de combustible, temperatura del aceite y caudal de combustible. En la configuración actual permanecen desactivados por defecto cuando no existe una verificación directa en el vehículo.

La siguiente tabla resume el conjunto de PIDs utilizado por los perfiles de AutoSense y la función que cumplen dentro de la solución.

\begin{table}[H]
\centering
\scriptsize
\renewcommand{\arraystretch}{1.05}
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{p{0.09\textwidth}p{0.22\textwidth}p{0.18\textwidth}p{0.39\textwidth}}
\toprule
PID & Magnitud & Perfil & Uso en AutoSense \\
\midrule
04 & Carga del motor & \texttt{generic\_obd2} & Base operativa \\
05 & Temperatura del refrigerante & \texttt{generic\_obd2} & Contexto térmico \\
0B & Presión del múltiple de admisión & \texttt{generic\_obd2} & Lectura de carga \\
0C & Revoluciones por minuto & \texttt{generic\_obd2} & Muestreo principal \\
0D & Velocidad del vehículo & \texttt{generic\_obd2} & Contexto de marcha \\
0F & Temperatura del aire de admisión & \texttt{generic\_obd2} & Apoyo térmico \\
11 & Posición del acelerador & \texttt{generic\_obd2} & Demanda del conductor \\
42 & Voltaje de la ECU & \texttt{generic\_obd2} & Estabilidad eléctrica \\
06 & Ajuste de mezcla a corto plazo B1 & \texttt{vw\_passat\_2016} & Perfil validado \\
07 & Ajuste de mezcla a largo plazo B1 & \texttt{vw\_passat\_2016} & Perfil validado \\
1F & Tiempo de funcionamiento & \texttt{vw\_passat\_2016} & Perfil validado \\
10 / 2F / 5C / 5E & MAF, nivel de combustible, temperatura del aceite y caudal de combustible & \texttt{vw\_passat\_2016} & Opcionales desactivados por defecto \\
\bottomrule
\end{tabular}
\caption{PIDs implementados en los perfiles de AutoSense y su uso funcional. Elaboración propia con base en los perfiles del proyecto.}
\end{table}

La capa de PIDs convierte bytes de la ECU en variables que el usuario puede leer. Sin esa traducción, AutoSense solo tendría respuestas hexadecimales. Con ella puede mostrar RPM, velocidad, temperatura o voltaje sin salirse del estándar ni del perfil validado.

\section{Microcontrolador ESP32}
\label{sec:microcontrolador-esp32}

El ESP32-S3 es el núcleo embebido de AutoSense. Lee datos del vehículo, aplica la política de solo lectura, mantiene el perfil activo y entrega la información a la app, al log local y al dashboard USB. Concentrar esas tareas en el adaptador reduce piezas intermedias y deja una secuencia de arranque repetible.

\subsection{Arquitectura del microcontrolador}

El ESP32-S3 articula tres tareas: leer el bus del vehículo, validar solicitudes permitidas y entregar resultados a los módulos internos. Espressif documenta soporte de Bluetooth Low Energy y USB OTG/USB CDC para esta familia, capacidades usadas en el prototipo para conectar firmware, app móvil y supervisión local \cite{espressif2026_esp32s3_get_started,espressif2026_esp32s3_usb_otg_console}.

El microcontrolador recibe la respuesta vehicular, la convierte en estructuras internas y conserva el contexto entre ciclos de muestreo. Así, una lectura no queda aislada: se evalúa junto con el perfil activo, las señales habilitadas y el estado acumulado de la sesión.

\subsection{Funciones del ESP32-S3 en AutoSense}

Durante el arranque, el firmware sigue una secuencia fija. Primero inicializa la consola, los periféricos básicos y el transceptor CAN; luego carga la configuración local desde \texttt{data/config.ini}; después intenta recuperar \texttt{/active\_profile.json} cuando ya existe un perfil aplicado por la aplicación; y finalmente arranca los servicios de consulta, el dashboard, el registro histórico y el enlace BLE. Esa secuencia es relevante porque deja al sistema en un estado conocido antes de comenzar el sondeo de datos del vehículo.

El archivo \texttt{active\_profile.json} guarda el perfil que el firmware usará en la sesión. Allí quedan los PIDs permitidos, las fórmulas de decodificación y las señales válidas para el vehículo activo. El ESP32-S3 no arma la lectura por su cuenta; aplica la configuración que la aplicación resolvió a partir del VIN y del perfil validado. Así se mantiene la misma referencia entre muestreo, decodificación y datos mostrados al usuario.

Otra función clave del microcontrolador es aplicar la política de solo lectura. Cada solicitud pasa por un guardián que bloquea borrado, control o reprogramación antes de llegar al bus. Así, AutoSense observa y registra sin intervenir la ECU. La validación también se vuelve más limpia, porque el prototipo no modifica el estado diagnóstico que intenta medir.

\subsection{Integración con almacenamiento y salida de datos}

El ESP32-S3 también separa los datos por uso. LittleFS guarda configuración y perfil activo. El historial de telemetría se registra aparte, en formato binario compacto dentro de la flash. Con esta separación, un reinicio no borra el perfil ni mezcla mediciones con archivos de control.

AutoSense entrega datos por dos canales. USB-CDC muestra un dashboard local para pruebas y depuración. BLE envía lecturas interpretadas a la app. En ambos casos el ESP32-S3 toma bytes de diagnóstico y los convierte en señales legibles.

Dentro del prototipo, el microcontrolador concentra adquisición, validación, persistencia y distribución. Esa decisión mantiene alineadas las decisiones de firmware con el perfil vehicular activo.

\section{Internet de las Cosas}
\label{sec:iot}

La Internet de las Cosas constituye el marco conceptual que permite tratar al vehículo como una fuente continua de datos y no solo como un sistema que entrega información bajo consulta puntual. En el contexto automotriz, esta perspectiva integra sensores embarcados, procesamiento en el borde y servicios remotos para capturar, organizar y explotar datos de operación, diagnóstico y comportamiento del conductor. Diversos trabajos han mostrado que esta aproximación facilita la supervisión remota del vehículo, la detección oportuna de anomalías y la construcción de historiales útiles para mantenimiento y administración \cite{shafi2018_vehicle_remote_health,camposferreira2023_vehicle_driver_monitoring,singh2021_obdii_iot,dabarera2022_iot_vehicle_management,zhang2020_iov_edge}.

\subsection{Internet de las Cosas en vehículos conectados}

En vehículos conectados, la IoT no se limita a enlazar un dispositivo con una aplicación móvil. Su aporte principal consiste en distribuir funciones entre la captura local, la primera normalización de datos y la disponibilidad remota de la información. La literatura reciente sobre Internet de los Vehículos y computación en el borde destaca que esta separación mejora la respuesta del sistema y reduce la dependencia de un servidor central para tareas que pueden resolverse cerca de la fuente de datos \cite{zhang2020_iov_edge}. En AutoSense, ese criterio se materializa en un flujo donde el vehículo produce los datos, el ESP32-S3 los adquiere y filtra, y la capa remota conserva únicamente la información que resulta útil para el usuario y para el seguimiento del estado del vehículo.

Desde esa perspectiva, AutoSense se comporta como un sistema conectado de monitoreo vehicular más que como un simple lector de diagnóstico. La propuesta no busca enviar todo el tráfico bruto a la nube, sino seleccionar señales relevantes, mantener un perfil vehicular activo y presentar al usuario una visión organizada del estado del automóvil. Este enfoque coincide con soluciones IoT de monitoreo y telemetría vehicular que combinan lectura embarcada, visualización remota y apoyo a la toma de decisiones \cite{shafi2018_vehicle_remote_health,camposferreira2023_vehicle_driver_monitoring}.

\subsection{AutoSense como arquitectura borde-nube}

AutoSense trabaja con una arquitectura borde-nube. El ESP32-S3 queda cerca del vehículo y se encarga de adquirir, filtrar y preparar los datos. La app móvil muestra el estado del automóvil, administra el perfil activo y supervisa el adaptador. Firebase conserva perfiles, estado de conexión e historial asociado al usuario. Con esa separación, las lecturas urgentes se resuelven localmente y la información que debe sobrevivir entre sesiones se guarda fuera del adaptador.

La relación entre estas capas puede representarse de forma conceptual como sigue:

\begin{figure}[H]
    \centering
    \begingroup
    \shorthandoff{>}
    \begin{tikzpicture}[
        font=\small,
        node distance=8mm,
        >=Latex,
        veh/.style={draw=gray!70!black, rounded corners=5pt, fill=gray!10, line width=0.9pt, align=center, text width=4.8cm, minimum height=1.0cm, inner sep=5pt},
        core/.style={draw=blue!70!black, rounded corners=5pt, fill=blue!11, line width=0.9pt, align=center, text width=4.8cm, minimum height=1.05cm, inner sep=5pt},
        app/.style={draw=green!60!black, rounded corners=5pt, fill=green!11, line width=0.9pt, align=center, text width=4.8cm, minimum height=1.05cm, inner sep=5pt},
        cloud/.style={draw=teal!70!black, rounded corners=5pt, fill=teal!11, line width=0.9pt, align=center, text width=4.8cm, minimum height=1.05cm, inner sep=5pt},
        link/.style={draw=black!75, line width=1pt, -{Latex[length=3mm,width=2mm]}}
    ]
        \node[veh] (veh) {\autosenseicon{vehicle}\\Vehículo\\\scriptsize Fuente de datos OBD-II / CAN};
        \node[core, below=of veh] (esp32) {\autosenseicon{esp32}\\ESP32-S3\\\scriptsize Adquisición local y política de lectura};
        \node[app, below=of esp32] (appnode) {\autosenseicon{phone}\\Aplicación móvil\\\scriptsize BLE, perfil activo y visualización};
        \node[cloud, below=of appnode] (cloud) {\autosenseicon{firebase}\\Firebase / nube\\\scriptsize Persistencia y sincronización remota};

        \draw[link] (veh) -- node[right, xshift=4pt, font=\scriptsize] {OBD-II / CAN} (esp32);
        \draw[link, violet!80!black] (esp32) -- node[right, xshift=4pt, font=\scriptsize] {BLE} (appnode);
        \draw[link, teal!70!black] (appnode) -- node[right, xshift=4pt, font=\scriptsize] {Wi-Fi} (cloud);
    \end{tikzpicture}
    \endgroup
    \caption{Arquitectura vertical de la IoT de AutoSense. Elaboración propia.}
\end{figure}

El ESP32-S3 actúa como nodo de borde. Recibe la información del vehículo, aplica las validaciones básicas del firmware y entrega a la aplicación solo los datos necesarios. La app no se limita a mostrar valores: identifica el vehículo, maneja el perfil activo y organiza la experiencia del usuario. Por eso, cuando la información llega a Firebase, ya contiene contexto de usuario, vehículo y sesión.

\subsection{Sincronización y supervisión remota}

La dimensión IoT de AutoSense se consolida cuando la información deja de ser estrictamente local y pasa a formar parte de un ecosistema sincronizado. En el proyecto, Firebase cumple ese papel de soporte remoto: ayuda a resolver perfiles, conservar estado de usuario, registrar aplicación de perfiles y mantener consistencia entre sesiones. Esa estrategia permite que el sistema no dependa de una sola ejecución del firmware para conocer el contexto del vehículo, sino que reutilice datos ya validados para acelerar la interacción futura con la plataforma.

La supervisión remota convierte el monitoreo en una actividad continua y no solo en una lectura ocasional. El usuario puede retomar el estado del vehículo, revisar la información sincronizada y visualizar el historial sin reinstalar ni reconfigurar todo el sistema. AutoSense usa la IoT para enlazar adquisición local, visualización inmediata y persistencia remota dentro de una misma cadena de trabajo. Ese diseño es coherente con propuestas de gestión vehicular y diagnóstico remoto basadas en OBD-II, donde el valor del sistema está en integrar telemetría, conectividad y trazabilidad de la información \cite{singh2021_obdii_iot,dabarera2022_iot_vehicle_management}.

La siguiente sección desarrolla la comunicación Bluetooth, Wi-Fi y USB, que es el mecanismo concreto mediante el cual esta arquitectura de borde y nube intercambia datos entre el ESP32-S3, la aplicación y los servicios auxiliares.

\section{Comunicación Bluetooth, Wi-Fi y USB}
\label{sec:comunicacion}

La comunicación de AutoSense reúne los canales que permiten mover la información desde el ESP32-S3 hacia la aplicación móvil y, desde allí, hacia los servicios remotos. El detalle de OBD-II y CAN ya fue tratado; aquí interesa el transporte de datos dentro de la arquitectura del prototipo. En el caso del ESP32-S3, Espressif documenta el soporte de Bluetooth Low Energy y de USB OTG/USB-CDC para esta familia, capacidades que el proyecto aprovecha para separar la supervisión local del intercambio inalámbrico con el teléfono \cite{espressif2026_esp32s3_get_started,espressif2026_esp32s3_ble_overview,espressif2026_esp32s3_usb_otg_console}.

\subsection{Enlace inalámbrico de la aplicación}

El teléfono no se conecta por cable al adaptador. En una prueba normal, la app busca el periférico Bluetooth anunciado por el firmware. El nombre visible del dispositivo es \texttt{AutoSense OBD2}. Al encontrarlo, abre el servicio del proyecto y trabaja con dos rutas: una para enviar solicitudes y otra para recibir respuestas o telemetría. La app inicia la conversación; el firmware responde.

Por ese enlace pasan lecturas en vivo y mensajes de sesión. La app puede pedir información del dispositivo, revisar el perfil activo, transferir perfiles y activar o detener el flujo de telemetría. La lectura del bus sigue dentro del ESP32-S3; el teléfono recibe el resultado ya preparado.

\subsection{USB-CDC}

USB-CDC tiene otro uso. No enlaza al teléfono, sino que abre una consola local y el dashboard del firmware. Durante el arranque, el ESP32-S3 publica el transporte \texttt{USB-CDC+BLE}. Por USB se revisa diagnóstico, progreso de conexión y estado interno sin depender de la app. En las pruebas, esta salida permitió comparar lo que veía el firmware con lo recibido por la aplicación.

En AutoSense, USB-CDC queda para pruebas. Mientras BLE atiende a la app, la consola USB permite revisar \texttt{can=UP}, respuestas por segundo, avance del log y \texttt{guard=0}. Es una ventana técnica para el desarrollador, no una interfaz de uso diario.

\subsection{Wi-Fi y sincronización remota}

En el prototipo, Wi-Fi aparece solo desde el teléfono. El ESP32-S3 no lo usa para comunicarse con la ECU. Si el móvil tiene Internet, la app sincroniza perfiles, sesión, estado del vehículo e historial con Firebase. Si no hay red, el adaptador sigue leyendo por BLE y conserva el log.

El firmware conserva así una responsabilidad definida: leer el vehículo y entregar telemetría. La nube entra después, desde la app.

\begin{figure}[H]
    \centering
    \begingroup
    \shorthandoff{>}
    \begin{tikzpicture}[
        font=\small,
        node distance=12mm and 28mm,
        >=Latex,
        data/.style={draw=gray!65!black, rounded corners=4pt, fill=gray!8, line width=0.8pt, align=center, text width=3.0cm, minimum height=1.2cm, inner sep=5pt},
        core/.style={draw=blue!70!black, rounded corners=4pt, fill=blue!10, line width=0.9pt, align=center, text width=3.2cm, minimum height=1.3cm, inner sep=6pt},
        ble/.style={draw=violet!70!black, rounded corners=4pt, fill=violet!12, line width=0.9pt, align=center, text width=3.15cm, minimum height=1.25cm, inner sep=5pt},
        app/.style={draw=green!60!black, rounded corners=4pt, fill=green!12, line width=0.9pt, align=center, text width=3.1cm, minimum height=1.25cm, inner sep=5pt},
        cloud/.style={draw=teal!70!black, rounded corners=4pt, fill=teal!10, line width=0.9pt, align=center, text width=3.05cm, minimum height=1.25cm, inner sep=5pt},
        usb/.style={draw=orange!80!black, rounded corners=4pt, fill=orange!14, line width=0.9pt, align=center, text width=2.9cm, minimum height=1.15cm, inner sep=5pt},
        console/.style={draw=orange!60!black, rounded corners=4pt, fill=orange!8, line width=0.8pt, align=center, text width=3.0cm, minimum height=1.15cm, inner sep=5pt},
        link/.style={draw=black!75, line width=1pt, -{Latex[length=3mm,width=2mm]}}
    ]
        \node[data] (vehicle) {\autosenseicon{vehicle}\\Vehículo\\OBD-II / CAN};
        \node[core, below=of vehicle] (esp32) {\autosenseicon{esp32}\\ESP32-S3\\\scriptsize Firmware AutoSense\\\scriptsize LittleFS + políticas de lectura};

        \node[ble, below=of esp32] (ble) {\autosenseicon{ble}\\Bluetooth Low Energy\\\scriptsize Servicio GATT\\\scriptsize RX / TX};
        \node[app, below=of ble] (app) {\autosenseicon{phone}\\Aplicación móvil\\\scriptsize react-native-ble-plx\\\scriptsize Sesión y telemetría};
        \node[cloud, below=of app] (cloud) {\autosenseicon{firebase}\\Wi-Fi / Internet\\\scriptsize Firebase\\\scriptsize Sincronización remota};

        \node[usb, right=of esp32] (usb) {\autosenseicon{usb}\\USB-CDC\\\scriptsize Dashboard local};
        \node[console, below=of usb] (console) {\autosenseicon{dashboard}\\Monitor serial\\\scriptsize Depuración y estado};

        \draw[link] (vehicle) -- node[right, xshift=4pt, font=\scriptsize] {Lectura vehicular} (esp32);
        \draw[link, violet!80!black] (esp32) -- node[right, xshift=4pt, font=\scriptsize] {BLE} (ble);
        \draw[link, green!60!black] (ble) -- node[right, xshift=4pt, font=\scriptsize] {Comandos / telemetría} (app);
        \draw[link, teal!70!black] (app) -- node[right, xshift=4pt, font=\scriptsize] {Wi-Fi} (cloud);
        \draw[link, orange!85!black] (esp32) -- node[above, font=\scriptsize] {USB-CDC} (usb);
        \draw[link, orange!70!black] (usb) -- node[right, xshift=4pt, font=\scriptsize] {Dashboard} (console);
    \end{tikzpicture}
    \endgroup
    \caption{Diagrama vertical de los canales de comunicación de AutoSense. Elaboración propia.}
\end{figure}

\noindent BLE queda como canal de uso con el teléfono, USB-CDC como canal de prueba local y Wi-Fi como salida de la app hacia Firebase. Cada enlace tiene un propósito distinto dentro del prototipo.

\section{Aplicaciones móviles}

\label{sec:aplicaciones-moviles}

La aplicación móvil de AutoSense constituye la capa visible de interacción del sistema. Desde ella se administra el acceso, se selecciona el vehículo activo, se supervisa el estado de conexión y se consulta la telemetría que proviene del ESP32-S3. Su diseño no intenta duplicar la lógica embebida, sino presentar en una interfaz portátil la información que ya fue adquirida y validada en el borde. Para ello, el proyecto se apoya en React Native, Expo Router y Firebase, con una organización que permite mantener un mismo flujo funcional en Android e iOS.

\subsection{Arquitectura interna de la aplicación}

La navegación se decide a partir de la sesión. Si no hay usuario autenticado, Expo Router muestra el grupo \texttt{Auth}, donde están ingreso, registro y recuperación de cuenta. Si la sesión ya existe, la app entra al grupo \texttt{App}, con las pantallas de uso diario. \texttt{SessionProvider} escucha Firebase Auth, carga los datos del usuario desde Firestore y guarda el estado en SecureStore para no pedir credenciales en cada apertura. Con esa información, \texttt{AuthLayout} y \texttt{AppLayout} solo encaminan al usuario al flujo que corresponde.

Sobre esa base, la app utiliza una navegación por pestañas con tres destinos principales: \texttt{Inicio}, \texttt{Tiempo real} y \texttt{Perfil}. Esta organización simplifica el uso diario porque concentra en pocos accesos las tareas más frecuentes: revisar el estado general del vehículo, enlazar el dispositivo OBD-II por BLE y administrar los datos de la cuenta. El resto de pantallas se despliega como rutas secundarias, de modo que la experiencia principal permanece limpia y con una jerarquía clara.

\begin{figure}[H]
    \centering
    \begingroup
    \shorthandoff{>}
    \begin{tikzpicture}[
        font=\small,
        node distance=7mm,
        >=Latex,
        accessbox/.style={draw=blue!70!black, rounded corners=5pt, fill=blue!9, line width=0.9pt, align=center, text width=11.3cm, minimum height=1.2cm, inner sep=6pt},
        session/.style={draw=violet!75!black, rounded corners=5pt, fill=violet!12, line width=0.9pt, align=center, text width=11.3cm, minimum height=1.25cm, inner sep=6pt},
        nav/.style={draw=green!60!black, rounded corners=5pt, fill=green!11, line width=0.9pt, align=center, text width=11.3cm, minimum height=1.25cm, inner sep=6pt},
        screens/.style={draw=orange!80!black, rounded corners=5pt, fill=orange!12, line width=0.9pt, align=center, text width=11.3cm, minimum height=1.4cm, inner sep=6pt},
        integrations/.style={draw=teal!70!black, rounded corners=5pt, fill=teal!11, line width=0.9pt, align=center, text width=11.3cm, minimum height=1.45cm, inner sep=6pt},
        link/.style={draw=black!70, line width=1pt, -{Latex[length=3mm,width=2mm]}}
    ]
        \node[accessbox] (access) {\autosenseicon{user}\\Acceso y registro\\\scriptsize Inicio de sesión con correo y Google, recuperación de cuenta y registro guiado en tres pasos};
        \node[session, below=of access] (sessionnode) {\autosenseicon{shield}\\Gestión de sesión\\\scriptsize \texttt{SessionProvider}, SecureStore y Firebase Auth para mantener el estado del usuario};
        \node[nav, below=of sessionnode] (navigation) {\autosenseicon{phone}\\Navegación principal\\\scriptsize \texttt{Auth} / \texttt{App} y pestañas \texttt{Inicio}, \texttt{Tiempo real} y \texttt{Perfil}};
        \node[screens, below=of navigation] (screens) {\autosenseicon{dashboard}\\Pantallas operativas\\\scriptsize Inicio con tablero, eficiencia, viajes y alertas; emparejamiento BLE; vista en vivo; perfil, vehículo, datos personales y ajustes};
        \node[integrations, below=of screens] (integrations) {\autosenseicon{firebase}\\Integraciones de la app\\\scriptsize BLE nativo con \texttt{react-native-ble-plx} y sincronización con Firebase Auth, Firestore, Functions y Storage};

        \draw[link] (access) -- (sessionnode);
        \draw[link] (sessionnode) -- (navigation);
        \draw[link] (navigation) -- (screens);
        \draw[link] (screens) -- (integrations);
    \end{tikzpicture}
    \endgroup
    \caption{Flujo vertical de la aplicación móvil de AutoSense. Elaboración propia.}
    \label{fig:autosense-mobile-architecture}
\end{figure}

La figura ordena el recorrido de la app: acceso, sesión, navegación principal e integraciones. Cada pantalla depende del usuario autenticado y del vehículo activo.

\subsection{Pantallas y flujo de uso}

La pantalla inicial permite iniciar sesión con correo y contraseña o con Google. También incluye recuperación de cuenta. El registro se divide en tres pasos: correo, datos básicos y contraseña. Esa separación coincide con la forma en que la app crea la sesión y luego completa el perfil de usuario.

Una vez autenticado, el apartado \texttt{Inicio} funciona como tablero principal. Allí aparecen el vehículo activo, el nivel de combustible estimado, la eficiencia, el estilo de conducción, el viaje actual y las alertas prioritarias. Si todavía no hay dispositivo enlazado o telemetría suficiente, la pantalla solicita conectar el AutoSense ESP32 y pasar a \texttt{Tiempo real}. De esta manera, el usuario distingue entre datos leídos del prototipo y datos pendientes de conexión.

El módulo de \texttt{Tiempo real} tiene dos momentos. Primero, la app busca el dispositivo \texttt{AutoSense OBD2}, conecta por Bluetooth y resuelve el perfil vehicular. Si el VIN está disponible, lo usa para aplicar el perfil correcto. Luego aparece la vista en vivo, con velocidad, RPM, temperatura del motor, consumo estimado, carga, voltaje, acelerador y temperatura de admisión. La sesión también actualiza el seguimiento del viaje y deja datos para el historial del usuario.

El apartado \texttt{Perfil} concentra cuenta y vehículo. Desde allí se revisa el vehículo actual, se editan datos personales, se cambia correo o contraseña, se ajustan unidades, alertas y privacidad, y se cierra sesión. La pantalla de vehículo maneja el perfil base del automóvil y el estado de enlace.

Las vistas de eficiencia, viajes y alertas amplían el tablero con trayectos previos, análisis de conducción y avisos puntuales. La app muestra valores, pero también conserva la relación entre usuario, vehículo y sesión activa.

\subsection{Compatibilidad y despliegue}

La aplicación se desarrolla con Expo SDK 56 y React Native 0.85.3, por lo que su compatibilidad se define por la plataforma y por la capacidad del dispositivo, no por un modelo específico de teléfono. De acuerdo con la documentación oficial de Expo, el SDK vigente soporta Android 7+ e iOS 16.4+ \cite{expo2026_sdk_support}. En la práctica, esto significa que AutoSense puede desplegarse en terminales que ejecuten esas versiones o superiores y que además dispongan de Bluetooth Low Energy para sostener el enlace con el ESP32-S3.

El uso de BLE nativo obliga a trabajar con un \textit{development build} y no con Expo Go. La propia documentación de Expo indica que los \textit{development builds} incluyen \texttt{expo-dev-client} y permiten usar bibliotecas nativas que no están disponibles en Expo Go \cite{expo2026_development_build}. Esta condición no es un detalle de implementación menor: es la que permite que AutoSense use \texttt{react-native-ble-plx} para descubrir, conectar y notificar telemetría sin renunciar al resto del ecosistema de Expo.

En operación, la compatibilidad depende de tres requisitos mínimos: sistema operativo soportado, Bluetooth Low Energy disponible y permisos habilitados para conectar con el dispositivo. Con esos requisitos, la app mantiene una experiencia estable en Android y iOS, y la misma base de código cubre ambas plataformas sin obligar a mantener dos implementaciones separadas.

Con esta base, la siguiente sección desarrolla la inteligencia artificial, que aprovecha la telemetría ya capturada por la aplicación para construir análisis de comportamiento y apoyo al diagnóstico.

\section{Inteligencia artificial}

\label{sec:inteligencia-artificial}

La inteligencia artificial de AutoSense corre en el borde y usa las muestras compactas que ya produce el firmware. No depende de enviar cada lectura a una nube para reinterpretarla después. Este enfoque coincide con la literatura sobre inteligencia móvil en el Internet de los Vehículos y telemática de tiempo real, donde el análisis cercano a la fuente reduce latencia y conserva contexto \cite{zhang2020_iov_edge,selvaraj2023_advanced_telematics,kalalas2025_ai_driven_monitoring}. En este proyecto, la IA no entrega un diagnóstico mecánico final; detecta desviaciones, mantiene una línea base por vehículo y explica la alerta con severidad, área probable y señales más alejadas de lo normal \cite{michailidis2025_obdii_review,tormos2025_explainable_ai,mahale2025_review_predictive_maintenance}.

\subsection{Arquitectura de la IA local}

El módulo de IA de AutoSense consume el mismo \texttt{ObdCompactSample} que ya alimenta el logger binario y la telemetría BLE. Esto evita enviar nuevas solicitudes OBD al vehículo y mantiene el costo computacional bajo, porque la inferencia se ejecuta sobre una muestra ya depurada. En la práctica, el firmware llama al detector cada \texttt{anomaly.interval\_seconds} segundos, que por defecto es 10 s, y solo si existe al menos una muestra válida. Si no hay datos suficientes o la función está deshabilitada, el detector devuelve un resultado vacío y no fuerza decisiones artificiales.

\begin{figure}[H]
    \centering
    \begingroup
    \shorthandoff{>}
    \begin{tikzpicture}[
        font=\small,
        node distance=7mm,
        >=Latex,
        sample/.style={draw=gray!70!black, rounded corners=5pt, fill=gray!10, line width=0.9pt, align=center, text width=11.2cm, minimum height=1.05cm, inner sep=6pt},
        stats/.style={draw=blue!70!black, rounded corners=5pt, fill=blue!10, line width=0.9pt, align=center, text width=11.2cm, minimum height=1.15cm, inner sep=6pt},
        zscore/.style={draw=violet!75!black, rounded corners=5pt, fill=violet!12, line width=0.9pt, align=center, text width=11.2cm, minimum height=1.15cm, inner sep=6pt},
        forest/.style={draw=green!60!black, rounded corners=5pt, fill=green!11, line width=0.9pt, align=center, text width=11.2cm, minimum height=1.15cm, inner sep=6pt},
        output/.style={draw=orange!80!black, rounded corners=5pt, fill=orange!12, line width=0.9pt, align=center, text width=11.2cm, minimum height=1.2cm, inner sep=6pt},
        link/.style={draw=black!70, line width=1pt, -{Latex[length=3mm,width=2mm]}}
    ]
        \node[sample] (sample) {\autosenseicon{stats}\\Muestra compacta\\\scriptsize RPM, velocidad, temperatura, carga, throttle, combustible, MAF, MAP, voltaje, admisión y spark advance};
        \node[stats, below=of sample] (statsnode) {\autosenseicon{route}\\Baseline por vehículo y contexto\\\scriptsize Welford global + Welford por contexto; la identidad del perfil reinicia el modelo cuando cambia el vehículo};
        \node[zscore, below=of statsnode] (znode) {\autosenseicon{stats}\\Z-score por señal\\\scriptsize desviación respecto de la media local con piso mínimo de desviación estándar};
        \node[forest, below=of znode] (forestnode) {\autosenseicon{ai}\\Isolation Forest pequeño\\\scriptsize 16 árboles, sample size 64, profundidad máxima 6, inferencia sin memoria dinámica};
        \node[output, below=of forestnode] (outputnode) {\autosenseicon{dashboard}\\Salida interpretada\\\scriptsize score, severidad, areaMask, topSignals, baselineReady y modelReady};

        \draw[link] (sample) -- (statsnode);
        \draw[link] (statsnode) -- (znode);
        \draw[link] (znode) -- (forestnode);
        \draw[link] (forestnode) -- (outputnode);
    \end{tikzpicture}
    \endgroup
    \caption{Pipeline local de inteligencia artificial de AutoSense. Elaboración propia.}
    \label{fig:autosense-ai-pipeline}
\end{figure}

La figura resume el recorrido de la IA local. El firmware consolida la muestra, actualiza la referencia estadística, calcula la distancia de cada señal respecto de esa referencia y genera un score interpretado por el sistema. La separación entre medición, referencia y decisión mantiene el análisis ligero sin convertirlo en una caja negra completa.

\subsection{Baseline estadístico y Z-score}

La primera capa del detector es estadística. El firmware mantiene una línea base por vehículo y por contexto de conducción mediante el método de Welford, que actualiza media y varianza de forma incremental \cite{welford1962_corrected_sums}. No necesita guardar todas las muestras históricas. El baseline tiene una referencia global y referencias para \texttt{IDLE}, \texttt{CRUISING}, \texttt{ACCELERATING}, \texttt{DECELERATING} y \texttt{UNKNOWN}. Si el contexto ya tiene suficientes muestras, el detector usa esa referencia; si no, usa la global. Con ello evita que una condición poco frecuente distorsione todo el comportamiento aprendido.

\texttt{baselineReady} aparece solo cuando ya hay material suficiente para comparar. Por defecto, el firmware espera al menos 300 muestras válidas antes de tratar la referencia como estable. Antes de ese punto, el sistema sigue aprendiendo y evita convertir una lectura aislada en alerta. El perfil activo también forma parte de la identidad del modelo; si cambia el vehículo o el hash del perfil, la línea base empieza de nuevo.

La puntuación Z-score se calcula señal por señal como la distancia respecto de la media local normalizada por su desviación estándar. En AutoSense se usa un piso mínimo de desviación por señal para evitar que una variable casi constante produzca falsos positivos. En términos conceptuales, el cálculo puede expresarse así:

\begin{equation}
z_i = \frac{|x_i - \mu_i|}{\max(\sigma_i,\sigma_{\min,i})}
\end{equation}

\begin{equation}
score_z = \mathrm{clamp}\left(\mathrm{rms}(z_i)\cdot 12.5,\ 0,\ 100\right)
\end{equation}

El resultado no se interpreta como una avería concreta. Es una medida de separación frente al comportamiento habitual del vehículo. Para que el usuario no reciba solo un número, el sistema conserva tres datos adicionales: señales con mayor desviación, área funcional aproximada y estado de confianza del baseline.

\subsection{Isolation Forest compacto}

La segunda capa usa un \textit{Isolation Forest}, método que aísla observaciones raras mediante árboles aleatorios y submuestreo \cite{liu2008_isolation_forest}. En AutoSense se usa una versión pequeña para el ESP32-S3: 16 árboles, muestra de 64, ventana circular de 128 muestras y profundidad máxima de 6. El objetivo no es competir con un modelo de laboratorio, sino mantener una detección local que pueda correr durante la conducción.

El bosque no aprende con cualquier muestra. El firmware prefiere estados \texttt{IDLE} o \texttt{CRUISING}, porque son más estables que una aceleración fuerte o una maniobra irregular. Si el bosque todavía no está listo, el puntaje sale solo del Z-score. Cuando ya hay modelo, el firmware combina 70\% de Z-score y 30\% de Isolation Forest. Así mantiene una base estadística fácil de explicar y añade una señal multivariable para patrones menos evidentes.

\begin{table}[H]
    \centering
    \caption{Parámetros por defecto del detector de anomalías local. Elaboración propia con base en el firmware AutoSense.}
    \label{tab:ai-anomaly-defaults}
    \renewcommand{\arraystretch}{1.08}
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{p{0.26\textwidth}p{0.18\textwidth}p{0.48\textwidth}}
    \toprule
    Parámetro & Valor por defecto & Función \\
    \midrule
    Inferencia local & 10 s & Frecuencia de inferencia local. \\
    Baseline mínimo & 300 muestras & Cantidad mínima de muestras para estabilizar el baseline. \\
    Persistencia & 300 s & Intervalo de persistencia del baseline y del modelo. \\
    Ponderación Z & 70\% & Peso del componente estadístico. \\
    Ponderación IF & 30\% & Peso del componente de bosque de aislamiento. \\
    Bosque pequeño & 16 árboles / 64 muestras / profundidad 6 & Configuración compacta para inferencia en el borde. \\
    \bottomrule
    \end{tabular}
\end{table}

El bosque y la línea base se persisten en almacenamiento local para no empezar desde cero en cada reinicio. El baseline se guarda en la partición de preferencias del firmware y el modelo del bosque se exporta como un archivo binario compacto en LittleFS. Ambos artefactos están ligados a la identidad del vehículo y del perfil activo, de modo que AutoSense puede recuperar el estado anterior si no cambió el contexto de trabajo.

\subsection{Salida interpretada y clasificación de ruta}

La app no necesita conocer todo el estado interno del modelo. Recibe una salida resumida: valor de anomalía, nivel de severidad, área probable, señales más relevantes y estado de preparación del análisis. La severidad avanza desde operación normal hasta alerta crítica. Una lectura rara no sube de nivel por sí sola. Por ejemplo, una aceleración breve puede elevar RPM y carga durante unos segundos; el \textit{debounce} evita tratar ese cambio normal como alerta fuerte.

\texttt{areaMask} indica la zona funcional asociada a la desviación, como motor, admisión, combustible, sistema eléctrico, temperatura, conducción o sensor. \texttt{topSignals} muestra cuáles señales pesaron más. La app recibe esos campos por BLE y los presenta como seguimiento. La salida no ordena una reparación; marca un patrón para revisar si aparece varias veces.

El firmware incorpora también un clasificador de ruta. Con velocidad, RPM, carga del motor y posición del acelerador distingue ciudad, autopista y estados intermedios. Trabaja con 24 muestras a una cadencia de 10 s. Antes de confirmar un cambio conserva estados como \textit{HighwayCandidate}. Ese dato no diagnostica fallas; ubica la lectura dentro del contexto de conducción.

La IA local se mantiene pequeña por diseño. Aprende con el vehículo real, guarda una referencia por perfil y entrega una explicación breve a la interfaz. En esta tesis, su función es reconocer desviaciones repetibles y relacionarlas con el historial de conducción.

\section{Mantenimiento predictivo}

\label{sec:mantenimiento-predictivo}

El mantenimiento predictivo reúne métodos de supervisión orientados a identificar degradaciones antes de que se conviertan en una falla funcional. A diferencia del mantenimiento correctivo, que actúa después del daño, y del mantenimiento preventivo, que depende de intervalos fijos, este enfoque utiliza datos reales de operación para reconocer patrones persistentes, desviaciones graduales y combinaciones de variables que ameritan revisión técnica \cite{mahale2025_review_predictive_maintenance,shafi2018_vehicle_remote_health}. En el contexto automotriz, la literatura reciente coincide en que la telemetría, el análisis en el borde y los modelos ligeros permiten observar el estado del vehículo con menor latencia y mayor cercanía al uso cotidiano \cite{selvaraj2023_advanced_telematics,kalalas2025_ai_driven_monitoring}.

En AutoSense, el mantenimiento predictivo no se formula como una predicción exacta de la avería ni como una estimación cerrada de vida útil remanente. El alcance del sistema es más prudente y, al mismo tiempo, más útil para una solución embebida: detectar cambios repetidos respecto del comportamiento habitual del vehículo, ubicar esas desviaciones en áreas funcionales aproximadas y conservar evidencia histórica para apoyar la decisión del usuario o del técnico. Esta orientación es coherente con trabajos recientes basados en OBD-II e inteligencia artificial, donde el valor principal está en interpretar datos vehiculares de manera explicable y accionable, no en prometer diagnósticos absolutos \cite{michailidis2025_obdii_review,tormos2025_explainable_ai}.

\subsection{Mantenimiento asistido por AutoSense}

AutoSense construye ese soporte predictivo a partir de la telemetría que ya produce el firmware. Las variables compactas de motor, velocidad, temperatura del refrigerante, posición del acelerador, carga del motor, MAP, MAF, voltaje de la ECU, temperatura de admisión y avance de chispa constituyen la base observable sobre la que se identifica el comportamiento normal del vehículo. Sobre esa base, el detector local calcula una referencia por vehículo y por contexto de conducción, de modo que el análisis no depende de una sola instantánea sino del patrón que se repite a lo largo del uso \cite{singh2021_obdii_iot,dabarera2022_iot_vehicle_management}.

El sistema no marca como anómalo cualquier valor alto o bajo. Primero identifica el contexto de conducción: reposo, crucero, aceleración o desaceleración. Luego compara la muestra con el historial local. Al final resume el resultado en una severidad que la aplicación puede mostrar. Esta cadena reduce falsos avisos, porque un cambio normal de manejo no debe tratarse igual que una desviación repetida fuera del perfil aprendido.

El módulo de clasificación de ruta complementa esta interpretación. Aunque no es un sistema de mantenimiento en sí mismo, ayuda a contextualizar el uso del vehículo al distinguir trayectos urbanos y de autopista. Ese contexto importa porque el desgaste, la carga y la temperatura operan de manera distinta según el tipo de recorrido. Con ello, AutoSense evita atribuir a falla lo que puede ser una consecuencia natural del estilo de conducción, y al mismo tiempo conserva una base más consistente para comparar sesiones \cite{camposferreira2023_vehicle_driver_monitoring}.

\begin{table}[H]
    \centering
    \caption{Relación entre los componentes analíticos de AutoSense y el mantenimiento predictivo. Elaboración propia.}
    \label{tab:autosense-predictive-maintenance}
    \renewcommand{\arraystretch}{1.08}
    \setlength{\tabcolsep}{4pt}
    \small
    \begin{tabular}{p{0.22\textwidth}p{0.29\textwidth}p{0.39\textwidth}}
    \toprule
    Componente & Aporte al mantenimiento & Uso en AutoSense \\
    \midrule
    Baseline local y Z-score & Detecta variaciones respecto del comportamiento habitual y destaca señales fuera de rango & Genera alertas de seguimiento cuando una variable se aleja de forma sostenida de la media local \\
    Isolation Forest compacto & Refuerza la detección de combinaciones multivariables poco frecuentes & Aumenta la sensibilidad ante patrones raros que no siempre aparecen como un único valor extremo \\
    Severidad y área probable & Traduce el resultado técnico a una indicación funcional & Orienta la revisión hacia motor, admisión, combustible, sistema eléctrico, temperatura o sensores \\
    \texttt{topSignals} y contexto & Explica qué señales cambiaron y bajo qué condición de manejo & Ayuda a separar un cambio normal de conducción de una posible degradación mecánica \\
    Historial de sesiones & Conserva la evolución del vehículo en el tiempo & Permite comparar recorridos y reconocer recurrencias en sesiones distintas \\
    \bottomrule
    \end{tabular}
\end{table}

La tabla resume una idea central del proyecto: el mantenimiento predictivo no depende de una sola métrica, sino de la combinación entre referencia histórica, score de anomalía, contexto de conducción y explicación breve para el usuario. Ese enfoque evita una interpretación rígida de los datos y convierte la salida del firmware en una guía para decidir si conviene revisar el vehículo con mayor prioridad.

\subsection{Seguimiento, persistencia y alcance}

El componente predictivo necesita memoria. Por eso el baseline y el modelo compacto se guardan en el dispositivo. Si el ESP32-S3 se reinicia, no parte de cero siempre que el vehículo y el perfil sean los mismos. La app conserva sesiones y permite revisar si una alerta apareció una sola vez o si volvió a presentarse en otros recorridos. Ese seguimiento es comparable con sistemas de monitoreo remoto y gestión vehicular reportados en la literatura \cite{shafi2018_vehicle_remote_health,camposferreira2023_vehicle_driver_monitoring,dabarera2022_iot_vehicle_management}.

En la práctica, AutoSense privilegia una estrategia de apoyo al mantenimiento más que un pronóstico mecánico cerrado. El sistema puede señalar que una temperatura, un patrón de carga o una combinación de señales se aparta del comportamiento aprendido, pero no sustituye la inspección técnica ni determina por sí mismo la causa exacta de la anomalía. Esa limitación es deliberada: mantiene la solución dentro de un margen confiable, explica su salida con señales concretas y evita atribuir al modelo una capacidad diagnóstica que no corresponde a su diseño \cite{mahale2025_review_predictive_maintenance,michailidis2025_obdii_review}.

Con ese alcance, el mantenimiento predictivo en AutoSense actúa como una capa de interpretación sobre la telemetría vehicular: detecta desviaciones, conserva contexto, prioriza revisión y facilita seguimiento longitudinal. La siguiente sección presenta los trabajos relacionados que sustentan esta línea de desarrollo y permiten comparar la propuesta con otros enfoques de diagnóstico y monitoreo vehicular.

\section{Trabajos relacionados}

\label{sec:trabajos-relacionados}

La literatura reciente sobre monitoreo vehicular muestra una convergencia entre tres líneas de trabajo: la computación en el borde para reducir latencia, los sistemas OBD-II conectados para extraer datos de diagnóstico y los modelos de aprendizaje automático orientados al mantenimiento y la conducción eficiente. Estos trabajos evidencian que el vehículo moderno puede tratarse como una fuente continua de datos, siempre que el sistema de captura, transmisión e interpretación se mantenga alineado con las limitaciones reales del hardware y del contexto de uso \cite{zhang2020_iov_edge,selvaraj2023_advanced_telematics,kalalas2025_ai_driven_monitoring}.

\subsection{Edge computing e inteligencia en el vehículo conectado}

Zhang y Letaief plantean el Internet de los Vehículos como una red donde parte del cómputo se mueve cerca de la fuente de datos \cite{zhang2020_iov_edge}. En telemática vehicular, esa idea ayuda a responder con menos demora y a conservar contexto de manejo antes de enviar información a servicios externos \cite{selvaraj2023_advanced_telematics,kalalas2025_ai_driven_monitoring}. AutoSense usa esa misma lógica: el ESP32-S3 calcula una primera interpretación y no deja todo el análisis a servidores remotos.

Estos trabajos no se reducen a velocidad de cómputo. También muestran un criterio de diseño útil para vehículos: si la conexión falla, el sistema debe seguir leyendo y conservando datos. AutoSense adopta esa lógica. El vehículo genera la muestra, el adaptador la resume en el borde y la app recibe una salida ya preparada, sin esperar a que una nube decida cada evento.

\subsection{OBD-II, IoT y diagnóstico conectado}

Un segundo grupo de trabajos se concentra en el uso de OBD-II como puerta de entrada a servicios conectados. Singh, Singh y Sharma presentan un sistema diagnóstico basado en IoT que emplea OBD-II para capturar datos vehiculares y enviarlos a un entorno conectado, destacando la utilidad del estándar para habilitar monitoreo remoto y apoyo al conductor \cite{singh2021_obdii_iot}. De forma complementaria, Dabarera et al. proponen un sistema de gestión vehicular que combina rastreo y diagnóstico OBD-II con transmisión hacia la nube mediante tecnologías de mensajería, lo que confirma que la telemetría del vehículo puede integrarse a una plataforma de gestión más amplia \cite{dabarera2022_iot_vehicle_management}.

Yen et al. amplían esa base al combinar un módulo OBD-II universal con aprendizaje profundo para análisis de eco-conducción y consumo de combustible \cite{yen2021_universal_obdii}. Su propuesta es valiosa porque muestra que la información OBD no solo sirve para leer códigos de falla, sino también para caracterizar el comportamiento de conducción y construir indicadores derivados. En la misma dirección, la revisión de Michailidis, Panagiotopoulou y Papadakis confirma que las aplicaciones de aprendizaje automático sobre OBD-II abarcan escenarios de eficiencia, seguridad y diagnóstico, aunque aún existe dispersión en los enfoques y en la forma de explotar los datos \cite{michailidis2025_obdii_review}.

La relación de estos estudios con AutoSense es directa pero no idéntica. Todos parten del mismo principio de acceso a datos vehiculares; sin embargo, AutoSense no se limita a transportar la información a la nube ni a generar una sola métrica de conducción. El sistema integra lectura, preprocesamiento, análisis local y visualización móvil, de modo que la lectura OBD-II se convierte en un flujo completo de observación y no en una simple consulta aislada.

\subsection{Mantenimiento predictivo e interpretabilidad}

El tercer grupo de trabajos enfoca el valor de los datos OBD y de la telemetría en el mantenimiento predictivo. Shafi et al. presentan un sistema de monitoreo remoto de salud vehicular y mantenimiento prognóstico basado en metodología guiada por datos, lo que anticipa el uso de series temporales para advertir degradaciones antes de que se vuelvan fallas visibles \cite{shafi2018_vehicle_remote_health}. Campos-Ferreira et al. extienden esta visión al integrar sensores a bordo y remotos en un sistema de monitoreo de vehículo y conductor, subrayando que el contexto operativo es tan importante como la medición puntual \cite{camposferreira2023_vehicle_driver_monitoring}.

Más recientemente, Tormos et al. proponen un caso de uso de inteligencia artificial explicable sobre datos OBD para gestión del mantenimiento en autobuses urbanos \cite{tormos2025_explainable_ai}. Su interés no está solo en clasificar estados, sino en justificar por qué una señal o combinación de señales merece atención técnica. Esa orientación coincide con la revisión de Mahale, Kolhar y More, que resume el crecimiento del mantenimiento predictivo impulsado por IA en vehículos y destaca, entre otros retos, la necesidad de explicabilidad, estabilidad y adaptación al contexto real de operación \cite{mahale2025_review_predictive_maintenance}.

AutoSense traslada esa preocupación a una salida legible en el borde: baseline por vehículo, Z-score por señal, Isolation Forest compacto, severidad y señales influyentes. La salida no reemplaza una inspección mecánica. Su función es priorizar con datos: indicar cuándo revisar el vehículo y qué área merece atención primero.

\begin{table}[H]
    \centering
    \caption{Trabajos representativos relacionados con AutoSense. Elaboración propia con base en las fuentes citadas.}
    \label{tab:trabajos-relacionados-autosense}
    \renewcommand{\arraystretch}{1.08}
    \setlength{\tabcolsep}{4pt}
    \small
    \begin{tabular}{p{0.24\textwidth}p{0.24\textwidth}p{0.22\textwidth}p{0.20\textwidth}}
    \toprule
    Trabajo & Enfoque principal & Aporte relevante & Relación con AutoSense \\
    \midrule
    Zhang y Letaief \cite{zhang2020_iov_edge} & Edge computing para el Internet de los Vehículos & Reduce la dependencia exclusiva de la nube y acerca el análisis al vehículo & Sustenta el procesamiento local en el ESP32-S3 \\
    Singh et al. \cite{singh2021_obdii_iot} & Diagnóstico OBD-II con IoT & Integra telemetría vehicular y monitoreo remoto & Valida el uso de OBD-II como fuente de datos conectados \\
    Yen et al. \cite{yen2021_universal_obdii} & OBD-II universal y aprendizaje profundo & Usa datos OBD para análisis de eco-conducción y consumo & Respaldan el uso de modelos sobre señales compactas \\
    Dabarera et al. \cite{dabarera2022_iot_vehicle_management} & Gestión vehicular con rastreo y diagnóstico & Combina diagnóstico OBD-II con envío a la nube & Se aproxima al flujo vehículo--móvil--cloud \\
    Tormos et al. \cite{tormos2025_explainable_ai} & IA explicable para mantenimiento & Enfatiza interpretación de datos OBD para mantenimiento & Refuerza la necesidad de salida explicable \\
    Mahale et al. \cite{mahale2025_review_predictive_maintenance} & Revisión de mantenimiento predictivo con IA & Resume técnicas, retos y líneas futuras & Justifica el módulo predictivo local \\
    \bottomrule
    \end{tabular}
\end{table}

La comparación anterior permite ubicar la propuesta en el cruce entre adquisición OBD-II, procesamiento embebido y presentación móvil. A diferencia de los trabajos que privilegian una sola dimensión del problema, AutoSense articula el flujo completo: hardware propio OBD-II/CAN, política de solo lectura, registro binario local, análisis ligero en el ESP32-S3, aplicación móvil y respaldo en Firebase cuando está disponible. El vacío que atiende esta tesis no es la creación de un algoritmo aislado, sino la integración verificable de esas capas en un prototipo funcional validado con simulador y vehículo real.

\newpage

% =========================
% CAPÍTULO 3
% =========================
\chapter{Marco metodológico}

\section{Tipo de investigación}

La investigación es aplicada. Toma conocimientos de OBD-II, computación en el borde, comunicación inalámbrica y desarrollo móvil para resolver un problema concreto: monitorear un vehículo con datos que ya están disponibles en sus módulos electrónicos.

También tiene carácter tecnológico y descriptivo. Es tecnológico porque construye e integra una placa, firmware, aplicación móvil y servicios de soporte. Es descriptivo porque organiza las lecturas del vehículo para mostrar cómo se comporta el sistema durante las pruebas. Además, incluye trabajo de campo con un Volkswagen Passat 2016 y pruebas controladas con simulador OBD-II.

El resultado esperado no es una teoría nueva sobre diagnóstico vehicular, sino un prototipo que pueda ejecutarse, medirse y corregirse. Esa condición define el tipo de investigación usado en la tesis.

\section{Enfoque de la investigación}

\label{sec:enfoque-investigacion}

El enfoque es principalmente cuantitativo. La validación se apoya en lecturas, contadores, tasas de respuesta, registros binarios, clasificación de ruta y datos exportados desde el sistema. En AutoSense importan señales medibles: \texttt{can=UP}, respuestas por segundo, decodificaciones, avance del log, máscara de validez, telemetría BLE y registros por contexto \texttt{city}/\texttt{highway}.

La interpretación de esos datos tiene un apoyo descriptivo. Las cifras no se analizan como hechos aislados; se usan para explicar si el prototipo lee, transmite, almacena y visualiza información de forma consistente. No se aplicaron encuestas ni entrevistas, porque la fuente del estudio es técnica: vehículo, firmware, app y archivos de salida.

\section{Diseño metodológico}

\label{sec:diseno-metodologico}

El diseño metodológico de la investigación es de carácter no experimental, debido a que no busca manipular variables independientes para demostrar relaciones causales en un entorno controlado, sino observar el comportamiento de un prototipo tecnológico durante su ejecución real. En AutoSense, el interés principal no se centra en alterar el vehículo como objeto de estudio, sino en construir una solución que lea datos OBD-II, los procese localmente y los presente de forma útil para el usuario.

Este diseño se enmarca además en una lógica de desarrollo tecnológico y validación funcional. La investigación avanza a partir de la identificación de necesidades concretas, el diseño de la arquitectura, la implementación de los módulos y la verificación de su comportamiento mediante pruebas sucesivas. De esta manera, el proceso metodológico no se limita a describir el sistema, sino que lo lleva a una forma operativa que puede ser evaluada por componentes y como conjunto.

La unidad de análisis es el sistema AutoSense en su totalidad, entendido como la integración del firmware del ESP32-S3, la comunicación BLE, la aplicación móvil y los servicios de soporte. Por ello, el diseño metodológico privilegia la trazabilidad de cada etapa y la revisión continua de resultados, de manera que cualquier ajuste realizado en un módulo pueda reflejarse de forma controlada en el comportamiento general del prototipo.

\section{Metodología de desarrollo}

\label{sec:metodologia-desarrollo}

El desarrollo fue iterativo. Primero se construyó una versión mínima capaz de leer y mostrar datos; después se agregaron perfiles, registro binario, BLE, app móvil, Firebase y análisis local. Cada módulo se probó antes de integrarlo con el siguiente.

El trabajo se dividió en cinco etapas: definición de requisitos, diseño de arquitectura, implementación modular, integración y validación. En la primera etapa se fijó el alcance de solo lectura y el vehículo de referencia. En la segunda se definieron los roles del ESP32-S3, la app y Firebase. En la tercera se implementaron firmware, BLE, navegación móvil y servicios remotos. En la cuarta se conectaron los módulos. En la quinta se corrigieron lecturas, sincronización y presentación de datos con base en pruebas de banco, simulador y vehículo real.

\section{Técnicas de recolección de datos}

\label{sec:tecnicas-recoleccion-datos}

La recolección de datos en AutoSense se fundamenta en la observación directa instrumentada mediante el firmware del ESP32-S3. En lugar de aplicar instrumentos dirigidos a personas, la investigación captura información técnica generada por el vehículo a través del enlace OBD-II, bajo una política de solo lectura que evita cualquier intervención sobre los módulos electrónicos. Este enfoque es coherente con el propósito del prototipo, que consiste en leer, interpretar y conservar información vehicular de manera automatizada y trazable \cite{sae2022_j1978_obdii_scan_tool,sae2025_j1979_diagnostic_test_modes,epa2016_vehicle_emissions_obd}.

Al iniciar, el firmware no asume que todos los PIDs existen. Primero descubre los parámetros soportados y después arma el plan de sondeo. En esa rutina consulta datos actuales y, cuando el perfil lo permite, información diagnóstica de solo lectura: códigos de falla, datos congelados, monitores a bordo e identificación del vehículo. La recolección queda entonces en manos del sistema, no de capturas manuales tomadas al azar \cite{sae2025_j1979_diagnostic_test_modes,epa2013_obd_readiness_best_practices}.

Una vez recibidas las respuestas, AutoSense normaliza los bytes crudos en una estructura compacta denominada \texttt{ObdCompactSample}. Esta estructura agrupa las magnitudes útiles para la validación del prototipo, como régimen del motor, velocidad, temperatura del refrigerante, apertura del acelerador, nivel de combustible, carga del motor, presión absoluta del múltiple, flujo de aire, voltaje de la ECU, temperatura de admisión y avance de chispa. Cada muestra incorpora una máscara de validez y se acepta únicamente si la señal fue actualizada dentro de una ventana temporal definida; con ello se evita que valores antiguos sean tratados como observaciones vigentes dentro del análisis posterior.

La información recolectada no se conserva solo en memoria volátil. El firmware la escribe además en un historial binario circular sobre LittleFS, junto con marcas de secuencia y control de integridad, lo que permite reconstruir sesiones y contrastar el comportamiento del sistema en diferentes momentos de prueba. Esta misma base de datos operativa se expone por BLE y por USB-CDC, de forma que el monitoreo en vivo, la depuración local y la persistencia histórica puedan compararse entre sí sin alterar la fuente original de datos. En términos prácticos, el prototipo utiliza múltiples vistas del mismo conjunto de datos para verificar consistencia entre adquisición, transmisión y registro \cite{shafi2018_vehicle_remote_health,camposferreira2023_vehicle_driver_monitoring}.

Además de las comprobaciones estacionarias iniciales, se hicieron trayectos reales en ciudad y carretera. La telemetría de esa etapa se consolidó en \path{vw_passat_2016_raw_route_classified_60d_10s.xlsx} con las etiquetas \texttt{city}, \texttt{highway} y \texttt{unknown}. La campaña cubrió aproximadamente 59 días, del 5 de mayo al 3 de julio de 2026, y dejó 86\,998 registros tomados cada 10 segundos, equivalentes a 241.7 horas efectivas de observación.

La técnica empleada, por tanto, corresponde a una captura técnica automatizada y no a un procedimiento social de encuesta o entrevista. La unidad observada es el vehículo en funcionamiento y el sistema embebido que lo instrumenta; por ello, la validez de la recolección depende de la continuidad del sondeo, la coherencia de las respuestas, la correcta decodificación de las señales y la integridad del almacenamiento. Esta delimitación metodológica asegura que los datos analizados en el resto del estudio provengan del propio sistema AutoSense y de su comportamiento en condiciones reales de uso.

\begin{table}[H]
\centering
\caption{Fuentes de recolección de datos empleadas en AutoSense. Elaboración propia.}
\label{tab:fuentes-recoleccion-datos}
\renewcommand{\arraystretch}{1.08}
\small
\begin{tabular}{p{0.24\textwidth}p{0.36\textwidth}p{0.27\textwidth}}
\toprule
Fuente & Dato recolectado & Uso en la validación \\
\midrule
Consultas OBD-II de solo lectura & Respuestas de PIDs soportados y modos diagnósticos permitidos & Verificar cobertura de lectura y consistencia de respuestas \\
\texttt{ObdCompactSample} & Señales normalizadas con máscara de validez y vigencia temporal & Alimentar el historial, la app y el análisis local \\
LittleFS y log binario & Secuencia, tiempo de captura y muestra compacta & Reproducir sesiones y contrastar resultados \\
BLE y USB-CDC & Vista en vivo del mismo estado recolectado & Comparar telemetría, depuración y comportamiento operativo \\
\bottomrule
\end{tabular}
\end{table}

\section{Herramientas utilizadas}

\label{sec:herramientas-utilizadas}

La investigación se apoyó en una cadena de herramientas orientada a adquisición vehicular, procesamiento embebido, comunicación móvil y sincronización en la nube. El núcleo físico del sistema está conformado por un ESP32-S3 con transceptor CAN compatible, configurado para trabajar con el framework Arduino sobre PlatformIO y con LittleFS como sistema de archivos interno. Esta base permite inicializar el bus CAN, levantar el dashboard por USB-CDC y habilitar la comunicación Bluetooth Low Energy con la aplicación móvil \cite{espressif2026_esp32s3_get_started,espressif2026_esp32s3_usb_otg_console,espressif2026_esp32s3_ble_overview}.

En la capa de software móvil se utilizó React Native con Expo Router y un development build, debido a que el proyecto requiere BLE nativo, el cual no está disponible en Expo Go. La autenticación, la persistencia de sesión y la sincronización de usuario se sostienen sobre Firebase Auth, Firestore, Functions y Storage, mientras que `expo-secure-store` permite conservar la sesión local de forma segura entre aperturas de la aplicación \cite{expo2026_sdk_support,expo2026_development_build}.

Como herramientas de apoyo se emplearon los scripts del propio repositorio para validar perfiles vehiculares, capturar reportes seriales y exportar el log binario de la partición histórica. Estas utilidades no forman parte de la solución final para el usuario, pero sí fueron necesarias para comprobar que la lectura, la persistencia y la sincronización del sistema se mantenían consistentes durante las pruebas.

\begin{table}[H]
\centering
\caption{Herramientas principales utilizadas en AutoSense. Elaboración propia.}
\label{tab:herramientas-utilizadas}
\renewcommand{\arraystretch}{1.08}
\small
\begin{tabular}{p{0.24\textwidth}p{0.28\textwidth}p{0.38\textwidth}}
\toprule
Herramienta & Función & Uso en la investigación \\
\midrule
ESP32-S3 + transceptor CAN & Adquisición embebida y enlace físico con el vehículo & Ejecutar el firmware, leer datos OBD-II y transmitir telemetría por BLE y USB-CDC \\
PlatformIO + Arduino & Compilación y despliegue del firmware & Construir el prototipo, cargar LittleFS y monitorear la ejecución \\
React Native + Expo Router & Interfaz móvil multiplataforma & Presentar autenticación, navegación, emparejamiento y datos en tiempo real \\
Expo development build + BLE nativo & Canal móvil de comunicación & Permitir la conexión con \texttt{react-native-ble-plx} fuera de Expo Go \\
Firebase Auth, Firestore, Functions y Storage & Backend y persistencia remota & Gestionar sesión, perfiles vehiculares, sincronización y almacenamiento \\
LittleFS + log binario & Persistencia local en el ESP32-S3 & Conservar el historial de lecturas y el perfil activo aplicado \\
Scripts de validación y captura & Control de calidad y evidencia & Validar perfiles, capturar reportes seriales y exportar muestras \\
\bottomrule
\end{tabular}
\end{table}

\section{Población o entorno de prueba}

\label{sec:poblacion-entorno-prueba}

La población de estudio no está constituida por personas, sino por las lecturas vehiculares obtenidas durante las sesiones de prueba de AutoSense. En términos metodológicos, la unidad de observación corresponde a cada muestra generada por el sistema embebido, cada respuesta OBD-II capturada por el firmware y cada telemetría entregada a la aplicación móvil. Esta definición es coherente con la naturaleza técnica del proyecto, ya que el interés del análisis se centra en el comportamiento del vehículo y en la estabilidad del sistema que lo instrumenta.

La unidad principal de validación es el Volkswagen Passat 2016 asociado al perfil \texttt{vw\_passat\_2016}, debido a que es el vehículo verificado en el repositorio y el que permite recorrer el flujo completo de AutoSense: lectura de VIN, resolución de perfil, aplicación del perfil activo, adquisición de datos, persistencia local y visualización en tiempo real. Los perfiles genéricos y el simulador OBD-II se consideran apoyos de desarrollo, útiles para ajustes preliminares, pero no reemplazan la validación principal sobre el vehículo de referencia. La campaña de observación cubrió aproximadamente 59 días y acumuló 241.7 horas efectivas de registro, combinando recorridos urbanos y de carretera para obtener trayectos clasificados como \texttt{city}, \texttt{highway} y \texttt{unknown}.

El entorno de prueba se manejó con una regla fija: primero seguridad, después recolección. Las primeras lecturas se hicieron con el vehículo estacionado, freno de mano activado, encendido en posición \texttt{ON} y motor apagado. Luego se repitió la prueba con motor encendido, manteniendo el vehículo detenido en \texttt{parking} o \texttt{neutral}. Cuando el enlace se mantuvo estable, la recolección pasó a conducción real en ciudad y carretera.

\section{Procedimiento metodológico}

\label{sec:procedimiento-metodologico}

El procedimiento fue incremental. Cada fase debía quedar estable antes de pasar a la siguiente. Si fallaba la configuración, la comunicación o el registro, el ajuste se hacía en ese punto y no al final de la campaña.

\begin{enumerate}
    \item \textbf{Preparación del firmware y del almacenamiento.} Se compiló el firmware, se cargó la configuración y se verificó LittleFS. Antes de conectar el vehículo se revisó que el log binario, el perfil activo y la política de solo lectura estuvieran disponibles.

    \item \textbf{Inicio de la aplicación y autenticación.} La app se ejecutó en un \textit{development build} de Expo. Firebase Auth validó la cuenta y SecureStore restauró la sesión local.

    \item \textbf{Conexión BLE y lectura de VIN.} La app buscó \texttt{AutoSense OBD2}, abrió la conexión BLE y pidió información del adaptador. Después solicitó el VIN por Mode 09 PID 02 para resolver el perfil.

    \item \textbf{Resolución y aplicación del perfil.} Con el VIN disponible, Firebase Functions resolvió el perfil. La app descargó el JSON desde Storage, revisó tamaño, esquema y SHA-256, y lo envió al ESP32-S3 en fragmentos. El firmware guardó \texttt{/active\_profile.json} y reconstruyó el sondeo de \texttt{signals[]}.

    \item \textbf{Recolección, almacenamiento y sincronización.} Con el perfil activo, el firmware leyó el vehículo, compactó las señales en \texttt{ObdCompactSample}, escribió el historial binario y expuso telemetría por BLE y USB-CDC. La app actualizó la vista en vivo y sincronizó estado de usuario, vehículo y conexión.
    
    Durante las sesiones de manejo real, esta etapa también permitió separar la información por contexto de ruta. El sistema fue registrando trayectos urbanos y de carretera, de modo que la exportación final pudiera organizarse como \texttt{city}, \texttt{highway} y \texttt{unknown} en el archivo \path{vw_passat_2016_raw_route_classified_60d_10s.xlsx}, especialmente en las hojas \texttt{Telemetry\_Route}, \texttt{City\_Only}, \texttt{Highway\_Only} y \texttt{Route\_Summary}.

    \item \textbf{Verificación y contraste de evidencia.} Se generaron reportes con \texttt{capture\_obd\_report.py} y se exportó el log con \texttt{dump\_obd\_log.py}. La comparación cruzó tres vistas del mismo dato: consola serial, telemetría BLE e historial almacenado.
\end{enumerate}

La validación mantuvo la misma secuencia de seguridad. Primero se verificó que no hubiera comandos de escritura. Luego se confirmaron lecturas con el vehículo detenido. Solo después se pasó a sesiones extendidas. Si aparecía un bloqueo del guard, un perfil incorrecto o telemetría incompleta, la prueba se detenía y se corregía la configuración. En manejo real se revisó además que los recorridos urbanos y de carretera quedaran representados en la exportación final.

\section{Criterios de evaluación}

\label{sec:criterios-evaluacion}

La evaluación se basó en señales observables. Una sesión era válida si el enlace CAN estaba activo, llegaban respuestas OBD-II, había decodificaciones, \texttt{guard=0}, \texttt{validMask != 0}, el log avanzaba y la app recibía telemetría. Esas evidencias salieron del dashboard USB-CDC, de la app, del registro binario y de Firebase. Quedaron pendientes mediciones más finas, como latencia extremo a extremo, pérdida de paquetes BLE y tiempo exacto de sincronización histórica.

\begin{table}[H]
\centering
\caption{Criterios de evaluación del sistema AutoSense. Elaboración propia.}
\label{tab:criterios-evaluacion}
\renewcommand{\arraystretch}{1.08}
\small
\begin{tabular}{p{0.24\textwidth}p{0.29\textwidth}p{0.37\textwidth}}
\toprule
Criterio & Indicador & Evidencia \\
\midrule
Disponibilidad del enlace vehicular & \texttt{can=UP}, \texttt{rsp/s > 0}, \texttt{decoded/s > 0} & Dashboard USB-CDC y reporte de captura serial \\
Política de solo lectura & \texttt{guard=0} y ausencia de comandos bloqueados & Registros del firmware y reporte \texttt{capture\_obd\_report.py} \\
Aplicación correcta del perfil & Perfil activo igual a \texttt{vw\_passat\_2016} y archivo \texttt{/active\_profile.json} presente & Respuesta BLE \texttt{GET\_ACTIVE\_PROFILE} y mensaje de arranque \\
Cobertura de trayectos reales & Presencia de etiquetas \texttt{city} y \texttt{highway} en la exportación & Hojas \texttt{Telemetry\_Route}, \texttt{City\_Only}, \texttt{Highway\_Only} y \texttt{Route\_Summary} \\
Coherencia de clasificación & \texttt{route\_type}, \texttt{route\_state} y \texttt{route\_reason} compatibles entre sí & Archivo Excel exportado \\
Vigencia de la muestra & \texttt{validMask != 0} y señales dentro de la ventana de actualización configurada & Telemetría BLE y muestra compacta usada por el logger \\
Persistencia local & Secuencia del log incrementando y registro histórico disponible & \texttt{dump\_obd\_log.py} y contador \texttt{log seq} del dashboard \\
Estabilidad de la telemetría móvil & Notificaciones BLE regulares y pantalla de tiempo real actualizada & Vista Tiempo real de la aplicación \\
Sincronización remota & Estado del usuario, vehículo y conexión reflejado en Firebase & Documentos en Firestore y llamadas a Functions/Storage \\
Continuidad de la campaña & Marcas de tiempo distribuidas durante el periodo de observación & Rango temporal del libro de cálculo y secuencia de muestras \\
\bottomrule
\end{tabular}
\end{table}

Si un criterio fallaba, la sesión no se aceptaba como evidencia. Primero se corregía firmware, perfil o configuración de la app, y luego se repetía la prueba.

\subsection*{Tratamiento de fuentes y datos}

La redacción de la tesis se preparó siguiendo un criterio de trazabilidad académica: las fuentes externas se citan en formato IEEE, las referencias empleadas se listan en la bibliografía y las figuras o tablas se identifican como elaboración propia, adaptación de una fuente técnica o visualización generada con datos del proyecto. Los resultados experimentales incluidos en el documento se presentan como evidencia propia derivada del firmware, la aplicación, los reportes de prueba y el archivo Excel de salida.

\newpage

% =========================
% CAPÍTULO 4
% =========================
\chapter{Desarrollo de la propuesta}

\section{Descripción general del sistema}

\label{sec:descripcion-general-sistema}

AutoSense se compone de vehículo, adaptador ESP32-S3, aplicación móvil y Firebase. El vehículo entrega datos por OBD-II/CAN. El adaptador los lee sin escribir sobre la ECU, los compacta y los analiza. La app muestra información de sesión, perfil e historial. Firebase respalda usuarios, perfiles y archivos sincronizados.

El flujo empieza con la identificación del vehículo. El firmware intenta leer el VIN y la app usa ese dato para resolver el perfil. Si coincide con el caso validado, aplica \texttt{vw\_passat\_2016}; si no, utiliza un perfil genérico. Luego el firmware sondea parámetros, consolida telemetría y registra el historial binario.

La aplicación móvil no reemplaza al firmware. Autentica al usuario, solicita perfiles, recibe notificaciones y presenta lecturas en tiempo real. El firmware conserva lectura y análisis local; la app organiza consulta y sincronización.

\section{Arquitectura del sistema}

\label{sec:arquitectura-sistema}

\subsection{Visión por capas}

AutoSense se diseñó como una arquitectura local-first. Cada lectura nace en el vehículo, pasa por el ESP32-S3 y solo después llega a la app. La capa vehicular aporta datos OBD-II/CAN bajo una política de solo lectura. La capa embebida interpreta respuestas, aplica el perfil activo y consolida las señales útiles. El ESP32-S3 se ajusta a este diseño porque Espressif documenta soporte para BLE y consola USB OTG/CDC en esa familia, que son precisamente los canales usados por el prototipo \cite{espressif2026_esp32s3_get_started,espressif2026_esp32s3_usb_otg_console,espressif2026_esp32s3_ble_overview}.

La aplicación móvil es la capa visible para el usuario. Está construida con React Native y Expo Router, pero se ejecuta como \emph{development build} para habilitar BLE nativo fuera de Expo Go, según la documentación de Expo \cite{expo2026_sdk_support,expo2026_development_build}. Firebase queda por encima como infraestructura de identidad, perfiles, metadatos y sincronización; no recibe cada muestra en tiempo real. En paralelo, LittleFS conserva perfil activo e historial local para que el adaptador siga funcionando ante reinicios o pérdidas temporales de conexión.

\begin{figure}[H]
\centering
\begingroup
\shorthandoff{<>}
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    node distance=10mm,
    every node/.style={font=\small},
    autosenseblock/.style={
        draw,
        rounded corners=2mm,
        align=center,
        text width=3.7cm,
        minimum height=1.25cm,
        inner sep=5pt
    },
    veh/.style={autosenseblock, fill=gray!10},
    esp/.style={autosenseblock, fill=blue!10},
    app/.style={autosenseblock, fill=green!12},
    cloud/.style={autosenseblock, fill=orange!12},
    store/.style={autosenseblock, fill=yellow!18},
    serial/.style={autosenseblock, fill=cyan!12}
]
    \node[veh] (veh) {\autosenseicon{vehicle}\\Vehículo\\OBD-II / CAN};
    \node[esp, below=of veh] (esp) {\autosenseicon{esp32}\\ESP32-S3\\Firmware AutoSense\\\footnotesize adquisición, guardia de solo lectura, compactación};
    \node[app, below=of esp] (app) {\autosenseicon{phone}\\Aplicación móvil\\React Native / Expo\\\footnotesize sesión, telemetría y navegación};
    \node[cloud, below=of app] (cloud) {\autosenseicon{firebase}\\Firebase\\Auth, Firestore, Functions y Storage};

    \node[store, left=18mm of esp] (lfs) {\autosenseicon{storage}\\LittleFS\\\footnotesize perfil activo\\historial binario};
    \node[serial, right=18mm of esp] (usb) {\autosenseicon{usb}\\USB-CDC\\dashboard local\\monitor serial};

    \draw[->, thick] (veh) -- node[midway, right, font=\scriptsize]{OBD-II/CAN} (esp);
    \draw[<->, thick] (esp) -- node[midway, right, font=\scriptsize]{BLE} (app);
    \draw[<->, thick] (app) -- node[midway, right, font=\scriptsize]{Wi-Fi / Internet} (cloud);
    \draw[->, thick] (esp) -- node[midway, above, font=\scriptsize]{persistencia local} (lfs);
    \draw[->, thick] (esp) -- node[midway, above, font=\scriptsize]{salida serial} (usb);
\end{tikzpicture}
}
\endgroup
    \caption{Arquitectura lógica de AutoSense y relación entre sus capas principales. Elaboración propia.}
\label{fig:arquitectura-autosense}
\end{figure}

La figura muestra una regla básica del diseño: Firebase no habla directamente con el ESP32-S3. La app es el puente entre BLE y los servicios remotos.

\subsection{Flujo de datos y responsabilidades}

La sesión arranca desde la app. El usuario se autentica, el teléfono restaura la sesión local y se conecta al dispositivo \texttt{AutoSense OBD2}. Luego solicita información básica al firmware y pide la lectura del VIN. Si el VIN corresponde al caso validado, el backend devuelve \texttt{vw\_passat\_2016}; si no, la app usa el perfil genérico. Las reglas de identificación quedan en la nube y no se duplican en firmware.

Después de resolver el perfil, la app descarga el JSON, revisa tamaño e integridad y lo envía al ESP32-S3 por BLE. El firmware valida el archivo, lo guarda en LittleFS y reconstruye el plan de \texttt{signals[]}. Luego \texttt{ObdService} consulta OBD-II, el perfil filtra señales válidas, el clasificador estima el contexto de ruta y el detector calcula anomalías.

La misma sesión tiene dos salidas. BLE alimenta la pantalla de tiempo real. USB-CDC muestra un dashboard técnico para pruebas. Si una lectura no aparece en la app, la consola ayuda a ubicar si el problema está en BLE, decodificación o registro interno.

\subsection{Persistencia y sincronización}

La persistencia se reparte entre adaptador, teléfono y nube. En el ESP32-S3 quedan perfil activo, configuración e historial binario. El teléfono conserva la sesión Firebase y una copia del perfil. Si el teléfono se desconecta, el adaptador sigue registrando; al reconectar, la app descarga lo pendiente y respalda el binario en Firebase Storage.

Firebase cubre control y respaldo. Authentication maneja acceso, Firestore guarda documentos e índices, Functions resuelve VIN/perfil y Storage hospeda perfiles e historial binario sincronizado. La telemetría rápida permanece en firmware y app; la nube conserva lo necesario para consulta posterior.

\begin{table}[H]
\centering
\caption{Componentes principales de la arquitectura de AutoSense. Elaboración propia.}
\label{tab:componentes-arquitectura}
\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{4pt}
\small
\begin{tabular}{@{}p{0.17\textwidth}p{0.21\textwidth}p{0.25\textwidth}p{0.25\textwidth}@{}}
\toprule
Componente & Función & Entrada & Salida o persistencia \\
\midrule
Vehículo OBD-II/CAN & Fuente de datos diagnósticos y de telemetría & Solicitudes OBD-II de solo lectura & Tramas CAN, VIN, DTC y respuestas de PIDs \\
ESP32-S3 y firmware & Adquisición, control y compactación de datos & Tramas CAN, perfil activo y parámetros de ejecución & Muestras compactas, BLE, USB-CDC y log binario \\
Aplicación móvil & Interfaz de usuario y puente hacia la nube & BLE, sesión autenticada y perfil descargado & Pantallas, comandos de sesión y sincronización \\
Firebase & Identidad, perfiles y sincronización & VIN, documentos y metadatos del usuario & Auth, Firestore, Functions y Storage \\
LittleFS & Persistencia local embebida & Perfil activo, configuración y eventos de log & Perfil activo e historial binario \\
USB-CDC & Consola local de monitoreo & Estado interno del firmware & Dashboard serial y diagnóstico local \\
\bottomrule
\end{tabular}
\end{table}

Esta arquitectura desacopla los bloques principales: el firmware puede ajustar el sondeo, la app puede cambiar su navegación y Firebase puede ampliar perfiles sin alterar la política de solo lectura del ESP32-S3. Esa separación sostiene las subsecciones técnicas siguientes.

\section{Diseño del hardware}

El diseño de hardware de AutoSense corresponde a una placa impresa propia para conectar el prototipo directamente al puerto OBD-II del vehículo, acondicionar la comunicación CAN y alojar el microcontrolador ESP32-S3 que ejecuta el firmware del sistema. A diferencia de una solución basada en módulos sueltos, la placa integra en un mismo circuito el conector OBD-II, el transceptor CAN, las etapas de alimentación, la interfaz USB-C, el selector de modo de operación y el módulo ESP32-S3-WROOM-1. Esta integración reduce cableado externo, mejora la repetibilidad de las pruebas y permite que el dispositivo se comporte como un adaptador compacto para uso dentro del vehículo.

La propuesta tomó como referencia funcional el \emph{ESP32 CAN Bus Shield v1.2} de MrDIY, el cual combina un ESP32, un transceptor CAN, conector OBD-II, conversión de alimentación a 3.3 V, protección del bus y selector de modo de lectura/escritura \cite{mrdiy2026_esp32_can_shield_v12}. Sin embargo, AutoSense no replica el shield de forma directa: el diseño fue adaptado para integrar el ESP32-S3-WROOM-1 en la PCB, exponer USB-C para programación y depuración, separar redes internas de 3.3 V y mantener la política de uso orientada a lectura segura del bus vehicular. Por diseño, el hardware no se plantea como una tarjeta genérica de experimentación CAN, sino como la base física del sistema AutoSense.

\subsection{Referencia de diseño y bloques funcionales}

El hardware se organizó en bloques para que cada función crítica quedara separada dentro del esquemático: entrada OBD-II, protección y alimentación desde el vehículo, conversión DC-DC, regulación de 3.3 V, transceptor CAN, núcleo ESP32-S3, USB-C y elementos de control manual. Esta división facilita la lectura del circuito y permite relacionar cada componente con una responsabilidad concreta dentro del prototipo.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.58\textheight,keepaspectratio]{Figures/hardware_schematic_autosense.pdf}
\caption{Esquemático general del hardware AutoSense generado en KiCad. Elaboración propia.}
\label{fig:hardware-schematic-autosense}
\end{figure}

En la figura \ref{fig:hardware-schematic-autosense} se observa que el conector OBD-II alimenta el sistema y entrega las líneas CAN al transceptor. El ESP32-S3 no se conecta directamente al par diferencial del vehículo; esa función la realiza el SN65HVD233, que traduce entre las señales lógicas del microcontrolador y el bus CAN diferencial. De igual manera, la alimentación del automóvil no entra directamente al módulo ESP32-S3, sino que pasa por protección, filtrado y regulación antes de llegar a las redes internas de 3.3 V.

\begin{table}[H]
\centering
\caption{Componentes principales del hardware AutoSense. Elaboración propia con base en el esquemático KiCad.}
\label{tab:hardware-componentes-principales}
\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{4pt}
\small
\begin{tabular}{@{}p{0.17\textwidth}p{0.24\textwidth}p{0.49\textwidth}@{}}
\toprule
Referencia & Componente & Función dentro del diseño \\
\midrule
J1 & C-OBD-II-16M & Conector macho OBD-II de 16 pines para alimentación, masa y acceso a CAN del vehículo. \\
U1 & SN65HVD233 & Transceptor CAN de 3.3 V que adapta las señales lógicas del ESP32-S3 al par diferencial CANH/CANL. \\
U2 & ESP32-S3-WROOM-1 & Núcleo de procesamiento, comunicación BLE, USB y ejecución del firmware AutoSense. \\
U5 & TPS54331DR & Convertidor reductor para obtener una etapa regulada desde la alimentación vehicular. \\
U4 & AMS1117-3.3 & Regulador lineal usado para una red local de 3.3 V dentro de la placa. \\
D9 & NUP2105LT1G & Protección ESD y transitorios sobre las líneas CANH/CANL. \\
F1 & Fusible 500 mA / 24 V & Protección básica ante sobrecorriente en la entrada de alimentación. \\
J2 & USB-C USB 2.0 & Programación, alimentación por USB y consola local de depuración. \\
SW1 & Selector R/RW & Conmutación física entre modo de lectura y modo lectura/escritura del circuito CAN. \\
SW3/SW4 & Pulsadores & Botones de BOOT y RESET para carga de firmware y reinicio manual. \\
\bottomrule
\end{tabular}
\end{table}

\begin{longtable}{@{}p{0.20\textwidth}p{0.08\textwidth}p{0.20\textwidth}p{0.42\textwidth}@{}}
\caption{Lista de materiales agrupada del PCB AutoSense. Elaboración propia con base en la lista de materiales del proyecto.}
\label{tab:hardware-bom-agrupado}\\
\toprule
Referencias & Cant. & Valor & Función en la placa \\
\midrule
\endfirsthead
\toprule
Referencias & Cant. & Valor & Función en la placa \\
\midrule
\endhead
C1, C2 & 2 & 4.7 uF & Filtrado y estabilidad en etapas de alimentación. \\
C3, C4 & 2 & 10 nF & Desacoplo de alta frecuencia y control de ruido. \\
C5 & 1 & 1 nF & Filtrado puntual de señales o compensación local. \\
C6, C8, C12, C14 & 4 & 100 nF & Desacoplo cercano a circuitos integrados. \\
C7 & 1 & 47 nF & Filtrado asociado a red de control o estabilidad. \\
C9, C10 & 2 & 47 uF & Reserva de energía y suavizado de la etapa de potencia. \\
C11 & 1 & 10 uF & Filtrado local de alimentación. \\
C13, C15 & 2 & 22 uF & Filtrado de salida y soporte ante variaciones de carga. \\
C16 & 1 & 1 uF & Desacoplo local de baja capacidad. \\
D1, D3 & 2 & LED & Indicadores visuales de estado o alimentación. \\
D2, D5, D8 & 3 & 1N5822 & Diodos Schottky para protección y conducción controlada. \\
D6, D7 & 2 & SS14 & Diodos Schottky SMD para protección de alimentación. \\
D9 & 1 & NUP2105LT1G & Protección ESD/transitorios en las líneas CAN. \\
F1 & 1 & 500 mA / 24 V & Fusible de entrada para limitar sobrecorriente. \\
J1 & 1 & C-OBD-II-16M & Conector físico hacia el puerto OBD-II del vehículo. \\
J2 & 1 & USB-C USB 2.0 & Programación, consola USB-CDC y alimentación de pruebas. \\
JP1--JP4 & 4 & Solder jumper abierto & Configuración opcional de rutas y funciones del hardware. \\
L1 & 1 & 6.8 uH & Inductor de la etapa buck de alimentación. \\
R1, R3 & 2 & 1 k & Limitación de corriente o polarización local. \\
R2, R19, R26 & 3 & 10 k & Pull-up/pull-down y polarización de señales de control. \\
R4, R5, R12, R25 & 4 & 100 & Resistencias serie para protección y acondicionamiento de señal. \\
R8 & 1 & 330 k & Red divisora o ajuste de umbral en alimentación. \\
R9 & 1 & 2 k & Red de polarización o indicador. \\
R10 & 1 & 68 k & Red divisora o realimentación de regulación. \\
R11 & 1 & 20 & Resistencia serie de baja impedancia. \\
R13 & 1 & 30 k & Red de ajuste o realimentación. \\
R15 & 1 & 1 M & Polarización de alta impedancia. \\
R16 & 1 & 220 k & Red divisora o ajuste de umbral. \\
R17 & 1 & 47 k & Polarización de control. \\
R18 & 1 & 0 & Puente de configuración o enlace de red. \\
R20 & 1 & 1.5 k & Limitación/polarización asociada a indicador o control. \\
R21 & 1 & 3 k & Polarización local. \\
R22 & 1 & 220 ohm & Limitación de corriente para indicador o señal. \\
R23, R24 & 2 & 5.1 k & Resistencias CC del conector USB-C para identificación del dispositivo. \\
SW1 & 1 & SW\_SPDT & Selector físico de modo R/RW. \\
SW3, SW4 & 2 & TS-1088R-02026 & Botones BOOT y RESET. \\
U1 & 1 & SN65HVD233 & Transceptor CAN de 3.3 V. \\
U2 & 1 & ESP32-S3-WROOM-1 & Microcontrolador y radio BLE/Wi-Fi del prototipo. \\
U4 & 1 & AMS1117-3.3 & Regulador lineal de 3.3 V. \\
U5 & 1 & TPS54331DR & Convertidor buck principal desde la entrada vehicular. \\
\bottomrule
\end{longtable}

\subsection{Conexión OBD-II y entrada vehicular}

El punto de entrada del sistema es el conector J1, correspondiente a un conector OBD-II macho de 16 pines. Este conector permite que el prototipo se inserte directamente en el puerto de diagnóstico del automóvil sin cables intermedios. La selección de pines se alinea con el uso típico del diagnóstico OBD-II sobre CAN: el pin 6 se emplea para CAN High, el pin 14 para CAN Low, el pin 16 para alimentación de batería y los pines 4 y 5 para referencia de tierra. Esta disposición se relaciona con la función del conector de diagnóstico definida por SAE J1962 \cite{sae2016_j1962_diagnostic_connector}.

\begin{table}[H]
\centering
\caption{Pines OBD-II utilizados por AutoSense. Elaboración propia con base en el esquemático KiCad y la función del conector OBD-II.}
\label{tab:hardware-pines-obd}
\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{5pt}
\small
\begin{tabular}{@{}p{0.15\textwidth}p{0.22\textwidth}p{0.53\textwidth}@{}}
\toprule
Pin OBD-II & Señal en la PCB & Uso en AutoSense \\
\midrule
4 & GND & Referencia de tierra del sistema y retorno de alimentación. \\
5 & GND & Referencia de señal asociada al puerto de diagnóstico. \\
6 & \texttt{CAN\_H} & Línea diferencial alta del bus CAN del vehículo. \\
14 & \texttt{CAN\_L} & Línea diferencial baja del bus CAN del vehículo. \\
16 & \texttt{+BATT} & Entrada de alimentación proveniente del vehículo. \\
\bottomrule
\end{tabular}
\end{table}

Desde el punto de vista del diseño, la conexión OBD-II cumple dos funciones: suministra energía al dispositivo y entrega el medio físico de comunicación. Por esa razón, el trazado de la PCB concentra cerca del conector los elementos de protección y la etapa inicial de potencia. Esta ubicación reduce recorridos innecesarios de señales sensibles y mantiene separados los caminos de alimentación, CAN y control digital.

\subsection{Acondicionamiento CAN y protección del bus}

El acondicionamiento de la comunicación CAN se realiza mediante el transceptor U1, identificado en el esquemático como SN65HVD233. Este circuito integrado opera con alimentación de 3.3 V y permite comunicar un controlador CAN lógico con el bus diferencial definido por la capa física CAN. La documentación de Texas Instruments describe esta familia como transceptores CAN compatibles con ISO 11898-2, con tasas de señalización de hasta 1 Mbps y protección de pines de bus ante condiciones eléctricas adversas \cite{ti2026_sn65hvd23x_datasheet}. En AutoSense, su función es aislar al ESP32-S3 de la conexión directa al par CANH/CANL y entregar una interfaz eléctrica adecuada para el vehículo.

El diseño incorpora además el dispositivo D9, NUP2105LT1G, conectado a las líneas CAN. Este componente se utiliza como protector bidireccional contra descargas electrostáticas y transitorios sobre dos líneas de bus CAN. onsemi lo identifica como un protector ESD de doble línea para CAN, orientado a mejorar la confiabilidad del sistema ante eventos eléctricos dañinos \cite{onsemi2026_nup2105l}. En el contexto de AutoSense, esta protección es relevante porque el prototipo se conecta a un entorno automotriz real, donde pueden existir picos de tensión, ruido y desconexiones durante pruebas.

El selector SW1 representa una decisión importante del diseño: mantener una diferencia física entre el modo de lectura y el modo lectura/escritura. Aunque el firmware del proyecto aplica una política de consulta de solo lectura, el hardware conserva el selector R/RW inspirado en el diseño de referencia. Esto permite que, durante el uso normal del prototipo, el dispositivo se mantenga en una condición orientada a observación del bus, mientras que cualquier modo más activo queda limitado a pruebas controladas.

\subsection{Alimentación y regulación}

La alimentación principal ingresa desde el pin 16 del conector OBD-II como red \texttt{+BATT}. Debido a que esta tensión proviene del vehículo, no se utiliza directamente para alimentar al microcontrolador. Primero pasa por elementos de protección y por la etapa de conversión que adapta el nivel eléctrico a las necesidades internas de la placa. El diseño incluye un fusible F1 de 500 mA / 24 V, diodos Schottky y capacitores de filtrado, los cuales contribuyen a limitar fallos por sobrecorriente, polaridad o transitorios.

El regulador principal de conmutación es U5, TPS54331DR. De acuerdo con Texas Instruments, el TPS54331 es un convertidor reductor de entrada hasta 28 V, corriente de salida de hasta 3 A y frecuencia de conmutación nominal de 570 kHz \cite{ti2026_tps54331_product}. En AutoSense, esta etapa permite derivar una alimentación estable desde la línea vehicular antes de alimentar los circuitos digitales. El uso de un convertidor tipo buck es coherente con el entorno de trabajo, porque la tensión disponible en el automóvil excede ampliamente los 3.3 V requeridos por el ESP32-S3 y el transceptor CAN.

La PCB separa redes como \texttt{3V3\_SYS}, \texttt{3V3\_OBD} y \texttt{3V3\_USB}. Esta separación no significa que existan sistemas independientes, sino que el diseño distingue dominios de alimentación según su función: sistema principal, interfaz OBD/CAN y entrada USB. El regulador U4 AMS1117-3.3 complementa esta arquitectura al proporcionar una etapa lineal local de 3.3 V. Los capacitores de 100 nF, 1 uF, 4.7 uF, 10 uF, 22 uF y 47 uF distribuidos en el esquemático se utilizan para desacoplo, estabilidad de regulación y reducción de ruido.

\subsection{Núcleo ESP32-S3 y señales de control}

El núcleo de procesamiento de la placa es U2, ESP32-S3-WROOM-1. Este módulo concentra el microcontrolador, radio, memoria integrada del módulo y antena PCB. En AutoSense, el ESP32-S3 ejecuta el firmware de adquisición OBD-II, administra el perfil activo, procesa muestras compactas, expone datos por BLE y permite monitoreo local por USB. La elección del ESP32-S3 se relaciona con sus capacidades de comunicación y soporte oficial para BLE y consola USB, ya descritas por Espressif \cite{espressif2026_esp32s3_get_started,espressif2026_esp32s3_ble_overview,espressif2026_esp32s3_usb_otg_console}.

El diseño deja visible una zona de exclusión de antena en la PCB. Esta zona evita colocar cobre o componentes debajo del área de radiación del módulo, lo cual ayuda a conservar el desempeño de la comunicación inalámbrica. Como la app móvil se comunica con el dispositivo por BLE, una mala ubicación del módulo o una antena obstruida podría reducir alcance, estabilidad de conexión o calidad de emparejamiento.

Los pulsadores SW3 y SW4 corresponden a BOOT y RESET. Estos controles permiten ingresar al modo de carga de firmware y reiniciar el dispositivo sin desconectarlo físicamente. Además, la placa incluye resistencias de polarización, resistencias serie y LEDs de estado que apoyan la puesta en marcha y la verificación visual durante pruebas. Estos elementos no ejecutan el diagnóstico por sí mismos, pero reducen fricción durante programación, validación de hardware y depuración.

\subsection{USB-C, programación y depuración}

La interfaz J2 corresponde a un conector USB-C limitado a USB 2.0. En el diseño de AutoSense, este puerto cumple tres propósitos: alimentar la placa durante pruebas de escritorio, cargar el firmware al ESP32-S3 y abrir una consola local de depuración. Las líneas \texttt{D+} y \texttt{D-} conectan la comunicación USB hacia el microcontrolador, mientras que las resistencias de 5.1 k en las líneas CC permiten que el puerto sea reconocido correctamente como dispositivo USB.

Esta interfaz es especialmente útil antes de conectar el equipo al vehículo. Durante el desarrollo, el USB-C permite verificar que el ESP32-S3 arranca, que la consola responde y que el firmware se ejecuta antes de energizar la placa desde el puerto OBD-II. En pruebas reales, USB-CDC actúa como canal de contraste frente a BLE, porque permite observar mensajes de estado, telemetría y errores locales desde el monitor serial.

\subsection{Diseño físico de la PCB y proceso de ensamble}

La PCB fue diseñada para alojar el conector OBD-II en un extremo y el módulo ESP32-S3 en el cuerpo principal de la tarjeta. El área superior concentra la zona de conexión con el vehículo, potencia y CAN; la zona central agrupa regulación, protección y USB-C; y la parte inferior deja el módulo ESP32-S3 con su zona de antena libre. Esta distribución reduce interferencias entre potencia, radio y bus CAN, y permite que el dispositivo se inserte físicamente en el puerto OBD-II con una orientación compacta.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth,height=0.62\textheight,keepaspectratio]{Figures/hardware_pcb_layout.png}
\caption{Vista de layout de la PCB AutoSense en KiCad. Elaboración propia.}
\label{fig:hardware-pcb-layout}
\end{figure}

El ensamble avanzó de forma incremental para reducir el riesgo de fallas acumuladas. Primero se recibió y revisó la PCB fabricada, antes de instalar el módulo principal y el conector OBD-II. Esta inspección inicial permitió verificar la correspondencia entre el diseño y la placa física: huella del ESP32-S3, ubicación del USB-C, botones BOOT y RESET, pads de componentes SMD, serigrafía, taladros de fijación y espacio reservado para la antena. Cualquier error mecánico o de huella resulta más fácil de detectar antes de soldar componentes costosos o difíciles de retirar.

\begin{figure}[H]
\centering
\includegraphics[width=0.72\textwidth,height=0.52\textheight,keepaspectratio]{Figures/hardware_pcb_empty.jpg}
\caption{PCB AutoSense recibida antes del montaje del módulo ESP32-S3. Elaboración propia.}
\label{fig:hardware-pcb-empty}
\end{figure}

Después de la inspección de la tarjeta, se procedió con el montaje de los elementos básicos y del módulo ESP32-S3-WROOM-1. En esta etapa se cuidó la alineación del módulo sobre la huella central, la continuidad de los pads laterales y la limpieza del área de soldadura. El montaje del microcontrolador representa una fase crítica, porque concentra las señales de alimentación, USB, control, comunicación con el transceptor CAN y salida inalámbrica por BLE. Por ello, antes de avanzar al ensamble final, el prototipo debía conservar una unión mecánica estable y una soldadura visualmente uniforme.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth,height=0.60\textheight,keepaspectratio]{Figures/hardware_pcb_esp32_mount.jpg}
\caption{Montaje del módulo ESP32-S3-WROOM-1 sobre la PCB AutoSense. Elaboración propia.}
\label{fig:hardware-pcb-esp32-mount}
\end{figure}

Finalmente, se integró el conector OBD-II macho, completando la interfaz física con el vehículo. Esta última etapa convierte la PCB en un prototipo funcional capaz de insertarse directamente en el puerto de diagnóstico. Una vez ensamblado el conjunto, se revisó la fijación mecánica del conector, la separación entre pines, la orientación de la placa y la accesibilidad del puerto USB-C y de los botones de control. Con ello, el hardware quedó listo para pruebas eléctricas, carga de firmware y validación posterior en el vehículo.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth,height=0.60\textheight,keepaspectratio]{Figures/hardware_pcb_final_assembly.jpg}
\caption{Prototipo AutoSense ensamblado con conector OBD-II macho. Elaboración propia.}
\label{fig:hardware-pcb-final-assembly}
\end{figure}

El diseño de hardware cumple el papel de puente físico entre el vehículo y el sistema digital de AutoSense. El conector OBD-II proporciona acceso y alimentación; la etapa de protección reduce riesgos eléctricos; el transceptor CAN adapta el bus automotriz al microcontrolador; la regulación estabiliza la energía; el ESP32-S3 ejecuta la lógica embebida; y USB-C facilita programación y diagnóstico. Con esta base, la siguiente sección puede centrarse en el firmware, ya no como una abstracción aislada, sino como software ejecutándose sobre una plataforma electrónica específica.

\section{Diseño del firmware}

El firmware de AutoSense es la capa que convierte el hardware descrito en la sección anterior en un sistema operativo de adquisición, validación, almacenamiento y comunicación vehicular. Su diseño no se limita a leer un conjunto fijo de valores OBD-II; organiza una secuencia completa que inicializa el ESP32-S3, configura el bus CAN, descubre PIDs disponibles, aplica un perfil vehicular, decodifica respuestas, compacta muestras, registra historial local, transmite telemetría por BLE y mantiene una consola de supervisión por USB-CDC. Esta lógica se ejecuta sobre el ESP32-S3-WROOM-1, cuyas capacidades de comunicación BLE y consola USB son consistentes con la documentación oficial de Espressif \cite{espressif2026_esp32s3_get_started,espressif2026_esp32s3_ble_overview,espressif2026_esp32s3_usb_otg_console}.

Desde el punto de vista funcional, el firmware se diseñó bajo un criterio de seguridad conservador: consultar información, no modificar el vehículo. Por ello, todas las solicitudes salientes pasan por una política de solo lectura antes de enviarse por CAN. Este criterio es coherente con el alcance del proyecto, porque AutoSense busca monitorear el comportamiento del vehículo y apoyar la interpretación de datos, no borrar códigos de falla, reprogramar módulos ni ejecutar actuadores. Los modos de diagnóstico utilizados se mantienen dentro de servicios de lectura definidos para OBD-II, como datos actuales, información del vehículo, códigos de falla almacenados o pendientes, datos congelados y monitores a bordo \cite{sae2022_j1978_obdii_scan_tool,sae2025_j1979_diagnostic_test_modes}.

\subsection{Entorno de compilación y organización del firmware}

El proyecto de firmware se encuentra organizado como una aplicación PlatformIO para la placa ESP32-S3 DevKitM-1, con framework Arduino, flash de 8 MB, sistema de archivos LittleFS y tabla de particiones propia. La configuración de compilación activa USB-CDC desde el arranque, lo que permite utilizar la consola local sin depender de un adaptador serial externo. El mismo firmware puede usarse durante pruebas de escritorio y durante validaciones conectadas al puerto OBD-II del vehículo.

La tabla de particiones separa los espacios de trabajo del firmware. La aplicación principal ocupa la partición \texttt{app0}; \texttt{configfs} almacena configuración, perfil activo y modelo local de anomalías; \texttt{obdlog} funciona como partición binaria cruda para el historial circular; y \texttt{coredump} queda reservada para diagnóstico de fallos del propio ESP32-S3. Esta distribución evita guardar el historial como texto dentro del sistema de archivos y permite usar una partición optimizada para registros compactos.

\begin{table}[H]
\centering
\caption{Módulos principales del firmware AutoSense. Elaboración propia con base en el repositorio del proyecto.}
\label{tab:firmware-modulos}
\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{4pt}
\scriptsize
\begin{tabular}{@{}p{0.20\textwidth}p{0.28\textwidth}p{0.42\textwidth}@{}}
\toprule
Módulo & Archivo principal & Responsabilidad \\
\midrule
Aplicación principal & \texttt{app.cpp}, \texttt{main.cpp} & Inicializar servicios, ejecutar el ciclo principal y coordinar CAN, BLE, logger, dashboard, perfiles e IA local. \\
Configuración & \texttt{app\_config.cpp} & Leer \texttt{/config.ini} desde LittleFS y aplicar intervalos de logging, dashboard, anomalías, OBD y VAG. \\
Servicio OBD & \texttt{obd\_service.cpp} & Descubrir PIDs, planificar consultas, recibir tramas CAN, decodificar respuestas y construir muestras compactas. \\
Perfiles vehiculares & \texttt{vehicle\_profile.cpp} & Validar JSON, SHA-256, señales permitidas y guardar \texttt{/active\_profile.json}. \\
Guardia de lectura & \texttt{obd\_read\_only}\\\texttt{\_guard.cpp} & Permitir solo servicios diagnósticos seguros y bloquear servicios de escritura, control o reprogramación. \\
BLE & \texttt{obd\_ble\_protocol.cpp} & Exponer servicio GATT, recibir comandos de la app, transferir perfiles y notificar telemetría. \\
Logger binario & \texttt{obd\_binary}\\\texttt{\_logger.cpp} & Escribir registros compactos en la partición \texttt{obdlog} con secuencia y CRC8. \\
Dashboard & \texttt{obd\_dashboard.cpp} & Mostrar estado en USB-CDC, métricas, diagnósticos, VIN, DTC y contadores de validación. \\
Análisis local & \texttt{obd\_anomaly}\\\texttt{\_detector.cpp} & Procesar muestras con baseline, Z-score, Isolation Forest, severidad y señales principales. \\
Clasificación de ruta & \texttt{obd\_route}\\\texttt{\_classifier.cpp} & Estimar contexto \texttt{city}/\texttt{highway} a partir de velocidad, aceleración y estabilidad de manejo. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Secuencia de arranque}

El punto de entrada del firmware es deliberadamente simple. En \texttt{main.cpp}, la función \texttt{setup()} delega la inicialización a \texttt{appSetup()}, mientras que \texttt{loop()} delega la ejecución continua a \texttt{appLoop()}. Esta separación mantiene el archivo principal limpio y concentra la lógica operativa en \texttt{app.cpp}, donde se coordinan los servicios del sistema.

Durante el arranque, el firmware abre la consola serial a 115200 baudios, configura los LED de estado, coloca el pin \texttt{GPIO14} del transceptor CAN en modo normal, inicializa el servicio OBD, el escáner VW/VAG, el dashboard, el logger binario, el detector de anomalías y el protocolo BLE. Después lee la configuración de \texttt{/config.ini}, intenta cargar el perfil activo desde \texttt{/active\_profile.json}, imprime por USB la política de solo lectura y configura el bus CAN en \texttt{GPIO12} como RX, \texttt{GPIO13} como TX y 500000 baudios. Finalmente, comienza a anunciar BLE con el nombre \texttt{AutoSense OBD2}.

\begin{figure}[H]
\centering
\begingroup
\shorthandoff{<>}
\resizebox{0.88\textwidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    node distance=7mm,
    every node/.style={font=\small},
    stepbox/.style={
        draw,
        rounded corners=2mm,
        align=center,
        text width=7.2cm,
        minimum height=1.05cm,
        inner sep=5pt
    },
    boot/.style={stepbox, fill=blue!10, draw=blue!70!black},
    config/.style={stepbox, fill=yellow!18, draw=yellow!50!black},
    safety/.style={stepbox, fill=red!8, draw=red!55!black},
    data/.style={stepbox, fill=green!12, draw=green!50!black},
    io/.style={stepbox, fill=cyan!12, draw=cyan!60!black},
    ai/.style={stepbox, fill=violet!10, draw=violet!60!black},
    link/.style={->, thick}
]
    \node[boot] (start) {\autosenseicon{esp32}\\Arranque del ESP32-S3\\\scriptsize Serial 115200, LED, CAN RX/TX y servicios base};
    \node[config, below=of start] (cfg) {\autosenseicon{config}\\Configuración y perfil\\\scriptsize \texttt{/config.ini}, \texttt{/active\_profile.json}, intervalos y perfil \texttt{vw\_passat\_2016}};
    \node[safety, below=of cfg] (guard) {\autosenseicon{shield}\\Guardia de solo lectura\\\scriptsize permite OBD 01/02/03/06/07/09/0A y bloquea borrado, control o escritura};
    \node[data, below=of guard] (obd) {\autosenseicon{obd}\\Adquisición OBD-II por CAN\\\scriptsize discovery de PIDs, consultas Mode 01 y servicios diagnósticos seguros};
    \node[data, below=of obd] (sample) {\autosenseicon{stats}\\Muestra compacta\\\scriptsize \texttt{ObdCompactSample}, \texttt{validMask}, edad máxima de muestra y decodificación};
    \node[io, below left=8mm and 11mm of sample] (ble) {\autosenseicon{ble}\\BLE GATT\\\scriptsize comandos, perfil y telemetría};
    \node[io, below=8mm of sample] (usb) {\autosenseicon{usb}\\USB-CDC\\\scriptsize dashboard y depuración};
    \node[ai, below right=8mm and 11mm of sample] (logic) {\autosenseicon{ai}\\Ruta e IA local\\\scriptsize city/highway, Z-score e Isolation Forest};
    \node[config, below=18mm of usb] (log) {\autosenseicon{storage}\\Persistencia local\\\scriptsize log circular en \texttt{obdlog}, baseline y modelo en flash};

    \draw[link] (start) -- (cfg);
    \draw[link] (cfg) -- (guard);
    \draw[link] (guard) -- (obd);
    \draw[link] (obd) -- (sample);
    \draw[link] (sample) -- (ble);
    \draw[link] (sample) -- (usb);
    \draw[link] (sample) -- (logic);
    \draw[link] (ble) |- (log);
    \draw[link] (usb) -- (log);
    \draw[link] (logic) |- (log);
\end{tikzpicture}
}
\endgroup
    \caption{Flujo interno del firmware AutoSense desde el arranque hasta la persistencia y salida de datos. Elaboración propia.}
\label{fig:firmware-flujo-general}
\end{figure}

La figura \ref{fig:firmware-flujo-general} resume la lógica del firmware como una cadena de procesamiento. Primero se establece una condición inicial controlada; luego se cargan parámetros y perfil; después se aplica la guardia de seguridad antes de cualquier consulta; finalmente, la información obtenida se distribuye hacia BLE, USB-CDC, almacenamiento local y análisis embebido. Esta organización permite que el sistema falle de manera controlada: si no existe perfil activo, puede usar descubrimiento y fallback; si no hay datos frescos, no fuerza una muestra inválida; y si aparece un comando no permitido, lo bloquea antes de llegar al bus CAN.

\subsection{Ciclo principal de ejecución}

El ciclo \texttt{appLoop()} se ejecuta de forma repetitiva y breve. En cada iteración obtiene el tiempo con \texttt{millis()}, llama a \texttt{OBD.tick()} para decidir si corresponde enviar una consulta, atiende \texttt{BLE\_PROTO.tick()} para notificaciones y comandos, ejecuta \texttt{VAG.tick()} solo si la configuración y el perfil lo permiten, procesa todas las tramas CAN disponibles, actualiza estadísticas por segundo, compacta la muestra más reciente, actualiza la clasificación de ruta, escribe en el logger si corresponde, procesa anomalías locales y refresca el dashboard USB si está habilitado.

Este diseño evita un flujo bloqueante. En lugar de detener el microcontrolador esperando una respuesta específica, cada módulo avanza cuando le corresponde por tiempo o cuando existe información disponible. Esa estrategia es adecuada para un sistema conectado a CAN y BLE, porque permite consultar el vehículo, recibir respuestas, notificar al teléfono y mantener una salida serial sin depender de una sola operación larga. El pequeño retardo final de \texttt{delay(1)} deja margen al sistema operativo del ESP32-S3 sin cambiar la lógica de temporización de los módulos.

\begin{figure}[H]
\centering
\begingroup
\shorthandoff{<>}
\resizebox{0.88\textwidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    node distance=6mm and 10mm,
    every node/.style={font=\scriptsize},
    flowbox/.style={
        draw,
        rounded corners=2mm,
        align=center,
        text width=4.35cm,
        minimum height=1.0cm,
        inner sep=5pt
    },
    a/.style={flowbox, fill=blue!10, draw=blue!60!black},
    b/.style={flowbox, fill=green!12, draw=green!50!black},
    c/.style={flowbox, fill=orange!13, draw=orange!65!black},
    d/.style={flowbox, fill=violet!10, draw=violet!60!black},
    e/.style={flowbox, fill=cyan!12, draw=cyan!60!black},
    f/.style={flowbox, fill=gray!10, draw=gray!65!black},
    link/.style={->, thick},
    returnlink/.style={->, thick, dashed, gray!75!black}
]
    \node[a] (tick) {\autosenseiconsmall{obd}\\\texttt{OBD.tick()}\\planificación de consultas OBD-II};
    \node[b, below=of tick] (bletick) {\autosenseiconsmall{ble}\\\texttt{BLE\_PROTO.tick()}\\comandos y telemetría};
    \node[c, below=of bletick] (vag) {\autosenseiconsmall{shield}\\\texttt{VAG.tick()}\\UDS seguro si el perfil lo permite};
    \node[a, below=of vag] (can) {\autosenseiconsmall{can}\\Procesamiento CAN\\respuestas OBD y VAG};
    \node[e, below=of can] (sample) {\autosenseiconsmall{stats}\\Muestra compacta\\\texttt{ObdCompactSample} + \texttt{validMask}};

    \node[c, below left=9mm and 9mm of sample] (logger) {\autosenseiconsmall{storage}\\\texttt{LOGGER.tick()}\\historial binario};
    \node[d, below=9mm of sample] (analysis) {\autosenseiconsmall{ai}\\Ruta + anomalía\\contexto, score y severidad};
    \node[e, below right=9mm and 9mm of sample] (dash) {\autosenseiconsmall{dashboard}\\\texttt{DASH.tick()}\\dashboard USB-CDC};
    \node[f, below=13mm of analysis] (next) {\autosenseiconsmall{config}\\\texttt{delay(1)}\\siguiente iteración};

    \draw[link] (tick) -- (bletick);
    \draw[link] (bletick) -- (vag);
    \draw[link] (vag) -- (can);
    \draw[link] (can) -- (sample);
    \draw[link] (sample) -- (logger);
    \draw[link] (sample) -- (analysis);
    \draw[link] (sample) -- (dash);
    \draw[link] (analysis) -- (next);
    \draw[returnlink] (next.east) -- ++(3.0,0) |- (tick.east);
\end{tikzpicture}
}
\endgroup
    \caption{Ciclo lógico de ejecución de \texttt{appLoop()} en el firmware AutoSense. Elaboración propia.}
\label{fig:firmware-apploop}
\end{figure}

\subsection{Adquisición OBD-II y planificación de consultas}

El módulo \texttt{ObdService} concentra la lógica de adquisición OBD-II. Al iniciar, el firmware no asume que todos los PIDs están disponibles en el vehículo; primero ejecuta descubrimiento mediante los PIDs de soporte \texttt{0100}, \texttt{0120}, \texttt{0140}, \texttt{0160}, \texttt{0180}, \texttt{01A0} y \texttt{01C0}. Cada respuesta contiene un mapa de bits que indica qué parámetros puede responder el vehículo. Con ese resultado, el sistema reconstruye su plan de consulta para no depender de una lista rígida.

La planificación se divide en dos carriles. El carril rápido o \emph{key lane} opera cada 50 ms por solicitud y prioriza señales críticas para visualización, historial e inteligencia local, como RPM, velocidad, temperatura, carga del motor, posición del acelerador, MAF, MAP y voltaje ECU. El carril de fondo opera cada 350 ms por solicitud y cubre señales menos críticas o descubiertas dinámicamente. Esta separación permite mantener una vista en vivo responsiva sin saturar el bus con todas las señales al mismo ritmo.

Además de \texttt{Mode 01}, el firmware consulta servicios diagnósticos de solo lectura. \texttt{Mode 09} se usa para VIN, CALID, CVN, nombre de ECU e IPT cuando el vehículo responde; \texttt{Mode 03}, \texttt{Mode 07} y \texttt{Mode 0A} se utilizan para DTC almacenados, pendientes y permanentes; \texttt{Mode 02} recupera datos congelados; y \texttt{Mode 06} consulta monitores a bordo. La sección teórica de OBD-II ya explicó el significado general de estos modos; en el diseño del firmware, lo relevante es que se ejecutan con intervalos controlados y dentro de la política de lectura segura.

Cuando llegan tramas CAN, la rutina de procesamiento entrega cada frame al servicio OBD y al escáner VAG. El servicio OBD filtra respuestas estándar \texttt{0x7E8..0x7EF} y respuestas extendidas cuando corresponden, identifica el modo de respuesta y decodifica el contenido. Para \texttt{Mode 01}, el firmware conserva el valor crudo para el dashboard, aplica la fórmula del PID y actualiza la muestra compacta si la señal forma parte del conjunto útil para historial, BLE o análisis.

\subsection{Gestión de perfiles vehiculares}

El firmware no queda amarrado de forma permanente a un único vehículo. La lógica de perfiles se implementa mediante \texttt{ProfileManager}, el cual valida archivos JSON recibidos desde la aplicación móvil antes de aplicarlos. El flujo real inicia con la lectura del VIN por \texttt{Mode 09}; luego la app puede resolver el perfil correspondiente en la nube, descargarlo y transferirlo al ESP32-S3 por BLE en fragmentos. En el caso validado del proyecto, el perfil activo esperado es \texttt{vw\_passat\_2016}.

La transferencia del perfil no se acepta de manera ciega. El firmware valida tamaño, SHA-256, estructura JSON, existencia de \texttt{signals[]}, fórmulas permitidas y configuración extendida de solo lectura. Si el perfil pasa estas verificaciones, se guarda como \texttt{/active\_profile.json} en LittleFS y se reinicia el plan de sondeo a partir de las señales definidas. Esto permite que el comportamiento del firmware se adapte al vehículo sin recompilar el código.

El soporte VW/VAG extendido se mantiene bajo dos condiciones simultáneas: la configuración VAG debe estar habilitada y el perfil activo debe contener permisos extendidos de solo lectura. El ESP32-S3 no activa el escaneo extendido solo por detectar un VIN de Volkswagen. La regla evita consultas ampliadas sin autorización explícita del perfil y mantiene el diseño coherente con el principio de lectura segura.

\subsection{Política de solo lectura}

La seguridad operativa del firmware se implementa mediante el módulo de guardia de lectura. Antes de enviar una solicitud diagnóstica por CAN, el firmware evalúa si el servicio pertenece a la lista permitida. En OBD estándar se permiten \texttt{01}, \texttt{02}, \texttt{03}, \texttt{06}, \texttt{07}, \texttt{09} y \texttt{0A}; se bloquean explícitamente servicios como \texttt{04}, asociado a borrado o reinicio de información de emisiones, y \texttt{08}, asociado a operaciones de control. En UDS se permiten solo \texttt{0x19} y \texttt{0x22}, mientras que servicios de sesión, reset, seguridad, escritura, rutina, transferencia o control quedan bloqueados.

La guardia también permite frames de control de flujo ISO-TP cuando forman parte de una respuesta ya iniciada, porque son necesarios para recibir mensajes largos sin representar una orden de escritura sobre la ECU. Si un servicio no está dentro de la lista segura, el firmware lo bloquea y registra el evento por USB con el prefijo \texttt{[readOnlyGuard] BLOCK}. Durante una operación normal, el dashboard debe mantener \texttt{guard=0}; si ese contador aumenta, la sesión debe revisarse antes de continuar.

\begin{table}[H]
\centering
\caption{Servicios permitidos y bloqueados por la política de solo lectura. Elaboración propia con base en el firmware AutoSense.}
\label{tab:firmware-solo-lectura}
\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{5pt}
\small
\begin{tabular}{@{}p{0.18\textwidth}p{0.34\textwidth}p{0.38\textwidth}@{}}
\toprule
Familia & Permitido en AutoSense & Bloqueado por riesgo de escritura o control \\
\midrule
OBD-II estándar & \texttt{01}, \texttt{02}, \texttt{03}, \texttt{06}, \texttt{07}, \texttt{09}, \texttt{0A} & \texttt{04}, \texttt{08} y cualquier servicio no incluido en \texttt{SAFE\_READ}. \\
UDS & \texttt{0x19} y \texttt{0x22} & Sesión, reset, clear DTC, security access, write DID, IO control, routine control, transfer, memory access y servicios no permitidos. \\
ISO-TP & Flow control asociado a una respuesta recibida & Cualquier uso que no corresponda a recepción segura de una respuesta larga. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Compactación, almacenamiento y telemetría}

Después de decodificar las respuestas, el firmware no conserva todo el dashboard como historial. La estructura central es \texttt{ObdCompactSample}, que contiene únicamente las señales útiles para almacenamiento, BLE e IA local. Entre ellas se incluyen RPM, velocidad, temperatura de refrigerante, acelerador, combustible, carga del motor, MAP, MAF, voltaje ECU, temperatura de admisión y avance de chispa. El campo \texttt{validMask} indica qué señales están realmente presentes en la muestra, porque no todos los PIDs se actualizan con la misma frecuencia ni todos los vehículos responden al mismo conjunto de parámetros.

Para registrar historial, el firmware usa la partición cruda \texttt{obdlog}. Cada registro versión 1 mide 24 bytes e incluye magic \texttt{AS}, versión, \texttt{validMask}, secuencia, \texttt{uptimeSeconds}, señales principales y CRC8. El logger escribe solo si existe al menos una señal válida y respeta \texttt{logging.interval\_seconds}, que por defecto es 10 s. Al llegar al final de la partición, vuelve al inicio, por lo que el historial funciona como un log circular.

La capacidad actual se calcula a partir de una partición de \texttt{0x650000} bytes, sectores de 4096 bytes y 170 registros útiles por sector. Esto produce 274720 registros. Con un intervalo de 10 s, el README del firmware estima una retención aproximada de 31.8 días; con 5 s, cerca de 15.9 días; y con 30 s, alrededor de 95.4 días. El formato evita almacenar JSON o CSV dentro del microcontrolador, reduce desgaste y permite exportar posteriormente el binario con \texttt{dump\_obd\_log.py}.

La telemetría en vivo sale por BLE mediante un servicio GATT propio. El dispositivo anuncia el nombre \texttt{AutoSense OBD2}, recibe comandos por la característica RX y notifica respuestas por la característica TX. El protocolo cubre información del dispositivo, lectura de VIN, perfil activo, transferencia y aplicación de perfiles, lista de PIDs soportados, inicio de streaming y detención de streaming. Cuando la app activa el flujo en vivo, el firmware envía \texttt{TELEMETRY} cada 1000 ms con valores recientes y con el resultado de anomalía vigente.

\begin{table}[H]
\centering
\caption{Intervalos principales de ejecución del firmware. Elaboración propia con base en la configuración del firmware.}
\label{tab:firmware-intervalos}
\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{5pt}
\small
\begin{tabular}{@{}p{0.32\textwidth}p{0.18\textwidth}p{0.40\textwidth}@{}}
\toprule
Proceso & Intervalo & Finalidad \\
\midrule
Consulta \emph{key lane} & 50 ms por solicitud & Priorizar señales críticas de operación y visualización. \\
Consulta \emph{background} & 350 ms por solicitud & Consultar señales secundarias o descubiertas dinámicamente. \\
Dashboard USB & 1000 ms & Refrescar la vista serial por USB-CDC. \\
BLE stream & 1000 ms & Notificar telemetría en vivo hacia la aplicación móvil. \\
Guardado binario & 10 s & Registrar una muestra compacta en \texttt{obdlog}. \\
IA local & 10 s & Ejecutar inferencia de anomalía si existe muestra válida. \\
Guardado de baseline/modelo & 300 s & Persistir estado analítico por lotes. \\
Mode 09 & 3 s & Leer VIN, CALID, CVN, ECU name e IPT si responden. \\
Mode 06 & 10 s & Consultar monitores a bordo. \\
Mode 03 / 07 / 02 / 0A & 15 s / 20 s / 25 s / 30 s & Leer DTC almacenados, pendientes, freeze frame y DTC permanentes. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Clasificación de ruta y análisis local}

El firmware incorpora dos módulos analíticos que no generan nuevas consultas OBD, sino que reutilizan la muestra compacta ya recolectada. El primero es el clasificador de ruta, que emplea velocidad, RPM, carga del motor y posición del acelerador para estimar si el comportamiento corresponde a ciudad, autopista o un estado intermedio. La lógica usa una ventana de muestras, velocidad promedio, porcentaje de alta velocidad, porcentaje de paradas, evidencia de frenado y estabilidad de aceleración. Esta clasificación aporta contexto a la lectura, especialmente en campañas de datos donde se comparan trayectos \texttt{city} y \texttt{highway}.

El detector local trabaja sobre la misma muestra compacta. Primero la convierte a \texttt{ObdSample}; luego identifica el contexto de manejo, actualiza o consulta la línea base, calcula Z-score por señal y, si el modelo ya existe, evalúa el Tiny Isolation Forest. La inferencia corre cada 10 s. El baseline requiere al menos 300 muestras para considerarse estable y el estado se guarda cada 300 s.

La salida del detector no nombra una pieza dañada. Entrega \texttt{score}, \texttt{severity}, \texttt{areaMask}, \texttt{topSignals}, \texttt{baselineReady} y \texttt{modelReady}. La severidad puede ser \texttt{NORMAL}, \texttt{WATCH}, \texttt{WARNING} o \texttt{CRITICAL}. Para evitar alarmas por ruido, el firmware exige repetición antes de escalar el aviso. AutoSense, por tanto, señala patrones anómalos y áreas probables de revisión; no reemplaza un diagnóstico mecánico.

\subsection{Verificación y herramientas de soporte}

El repositorio incluye herramientas para validar el comportamiento del firmware sin depender únicamente de observación manual. \texttt{audit\_read\_only\_policy.py} revisa que la política de lectura mantenga los servicios permitidos y bloqueados esperados antes de flashear el dispositivo. \texttt{capture\_obd\_report.py} lee el dashboard serial, elimina códigos ANSI y genera reportes de métricas OBD, módulos VW/VAG y estado de diagnóstico. \texttt{dump\_obd\_log.py} exporta la partición binaria a CSV y permite conservar también el binario crudo cuando se necesita trazabilidad.

La validación inicial recomendada por el propio firmware inicia con el vehículo estacionado, freno de mano activo, ignición encendida y motor apagado; después se repite con motor encendido en parking o neutral. Solo cuando \texttt{can=UP}, \texttt{rsp/s > 0}, \texttt{decoded/s > 0}, \texttt{log seq} avanza y \texttt{guard=0}, la sesión se considera apta para avanzar hacia pruebas de recolección más extensas. Estos criterios conectan el diseño del firmware con la metodología del capítulo anterior, porque permiten comprobar que adquisición, decodificación, almacenamiento y seguridad funcionan antes de analizar datos de conducción.

En síntesis, el firmware de AutoSense está diseñado como una capa embebida de adquisición segura y procesamiento local. Su arquitectura separa responsabilidades, mantiene una política de solo lectura, aprovecha perfiles vehiculares para adaptar el sondeo, registra información compacta en flash, ofrece telemetría por BLE, conserva una salida técnica por USB-CDC y ejecuta análisis local sin enviar comandos adicionales al vehículo. Con ello, el software del ESP32-S3 completa el puente entre el hardware OBD-II/CAN y la aplicación móvil que se describe en la siguiente sección.

\section{Diseño de la aplicación móvil}

La aplicación móvil de AutoSense constituye la interfaz principal entre el usuario y el sistema de adquisición vehicular. Su diseño no se limita a mostrar valores recibidos desde el ESP32-S3; también organiza la sesión del usuario, conserva datos básicos del vehículo, administra el estado de conexión con el adaptador OBD-II, presenta métricas resumidas de eficiencia y alertas, y sincroniza información con los servicios en la nube del proyecto. Dentro de la arquitectura general, la aplicación cumple el papel de capa de visualización, control operativo y consulta histórica.

La aplicación usa React Native sobre Expo, con Expo Router para organizar la navegación por archivos. Esta elección permite mantener una base de código orientada a iOS y Android, con componentes nativos cuando son necesarios. La compatibilidad práctica queda condicionada por los requisitos vigentes del SDK de Expo y por la disponibilidad de Bluetooth Low Energy en el teléfono; en el caso del SDK empleado, Expo documenta soporte para sistemas Android e iOS modernos, mientras que las funciones BLE nativas requieren una \emph{development build} y no dependen de Expo Go \cite{expo2026_sdk_support,expo2026_development_build}. Por ello, la interfaz puede probarse en simulador para navegación, autenticación y estados demo, pero la conexión real con el ESP32-S3 debe validarse en un dispositivo físico con BLE.

\subsection{Arquitectura de navegación}

La navegación móvil está separada en dos grupos principales. El primer grupo corresponde al flujo de acceso, donde se ubican el inicio de sesión, el registro de usuario y la recuperación de cuenta. El segundo grupo corresponde al flujo autenticado, compuesto por las pestañas \emph{Inicio}, \emph{Tiempo real} y \emph{Perfil}. Esta separación evita que las pantallas internas estén disponibles sin sesión activa y simplifica la transición entre usuario no autenticado y usuario autenticado.

\texttt{SessionProvider} concentra el estado de usuario. Escucha Firebase Authentication, consulta los datos básicos de la cuenta y entrega a las pantallas el perfil activo. La sesión se guarda con SecureStore, por lo que el usuario no inicia sesión en cada apertura y las credenciales no quedan expuestas en almacenamiento plano. A partir de ese estado se relacionan telemetría, vehículo y preferencias de visualización con una cuenta concreta.

\begin{figure}[H]
\centering
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_login.png}
\vspace{2mm}
{\footnotesize Inicio de sesión}
\end{minipage}
\hfill
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_register.png}
\vspace{2mm}
{\footnotesize Registro de usuario}
\end{minipage}
\hfill
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_recovery.png}
\vspace{2mm}
{\footnotesize Recuperación de cuenta}
\end{minipage}
\caption{Flujo de acceso de la aplicación móvil AutoSense en simulador iOS. Elaboración propia.}
\label{fig:mobile-auth-flow}
\end{figure}

\subsection{Gestión de sesión y datos}

La gestión de datos combina almacenamiento local seguro y sincronización remota. En el cliente móvil se utiliza almacenamiento seguro para conservar la sesión de autenticación, mientras que Firebase centraliza datos de usuario, perfil vehicular, información de configuración, registros de aplicación de perfiles y recursos descargables. Esta división mantiene el teléfono como punto de interacción inmediata, pero permite que la información relevante del sistema pueda recuperarse desde la nube cuando el usuario inicia sesión en otro dispositivo o cuando se actualiza un perfil vehicular.

El diseño también contempla que no toda la información procede de la nube. Cuando la aplicación necesita comunicarse con el adaptador, consulta el estado local de la conexión BLE y el perfil activo aplicado al dispositivo. Si existe un perfil asociado al VIN o si el usuario selecciona un perfil compatible, la aplicación puede solicitar, validar y transferir dicho perfil al firmware. En este flujo intervienen Firebase Functions para resolver el perfil vehicular, Firebase Storage para descargar el archivo de perfil cuando corresponde y Firestore para conservar la trazabilidad de aplicación del perfil.

\begin{table}[H]
\centering
\caption{Responsabilidades principales de la aplicación móvil AutoSense. Elaboración propia con base en el repositorio móvil.}
\label{tab:mobile-responsabilidades}
\small
\begin{tabular}{@{}p{0.24\textwidth}p{0.36\textwidth}p{0.30\textwidth}@{}}
\toprule
Bloque & Responsabilidad & Uso en AutoSense \\
\midrule
Autenticación y sesión & Inicio de sesión, registro, recuperación de cuenta y persistencia segura. & Controla el acceso al flujo autenticado y asocia la telemetría a un usuario. \\
Navegación & Organización por rutas de acceso y pestañas internas. & Separa inicio, tiempo real y perfil sin mezclar estados de sesión. \\
Perfil vehicular & Resolución, descarga y aplicación de perfiles OBD-II. & Permite usar \texttt{vw\_passat\_2016}, perfil genérico o modo demo. \\
Comunicación BLE & Escaneo, conexión, comandos GATT y recepción de notificaciones. & Vincula la app con el ESP32-S3 para telemetría y transferencia de perfil. \\
Sincronización remota & Consulta y escritura de datos en Firebase. & Conserva usuario, vehículo, estado operativo y trazabilidad básica. \\
Visualización & Tarjetas, métricas, alertas, eficiencia y telemetría en vivo. & Presenta datos técnicos en una forma comprensible para el usuario final. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Pantallas principales}

La pantalla de inicio resume el estado general del vehículo y de la sesión. En la implementación actual muestra el vehículo asociado, el combustible estimado, el estilo de manejo, el puntaje de eficiencia, la distancia del viaje actual y el estado de avisos. Esta pantalla cumple una función de síntesis: evita que el usuario tenga que revisar todos los parámetros OBD-II y concentra la información en indicadores de lectura rápida.

La pantalla de eficiencia profundiza en el comportamiento de conducción. A partir de valores procesados por la aplicación y por el sistema, presenta un puntaje general, suavidad de aceleración, control de frenado, tiempo en ralentí y consumo estimado. El objetivo no es sustituir un análisis mecánico, sino ofrecer una interpretación operativa que ayude a revisar hábitos de manejo y consistencia de la telemetría recolectada. La pantalla de avisos, por su parte, organiza estados como batería, frenos, temperatura, llantas y consumo. Aunque algunas advertencias dependen de información disponible por OBD-II y otras de reglas de la aplicación, el diseño mantiene una lista clara de elementos revisables y evita presentar mensajes técnicos sin contexto.

\begin{figure}[H]
\centering
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_home.png}
\vspace{2mm}
{\footnotesize Inicio}
\end{minipage}
\hfill
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_efficiency.png}
\vspace{2mm}
{\footnotesize Eficiencia}
\end{minipage}
\hfill
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_alerts.png}
\vspace{2mm}
{\footnotesize Avisos}
\end{minipage}
\caption{Pantallas principales de supervisión y resumen operativo en AutoSense. Elaboración propia.}
\label{fig:mobile-main-screens}
\end{figure}

Además de la lista general de avisos, la aplicación contiene vistas de detalle para los estados principales del vehículo. En batería, frenos, temperatura y llantas se presenta el estado actual, la fuente de datos, la cantidad de avisos activos, la acción recomendada, una lectura actual y una indicación de seguimiento. La vista de consumo se concentra en la pantalla de eficiencia, porque allí la aplicación relaciona consumo, aceleración, frenado, ralentí y trayectoria en un mismo módulo operativo.

\begin{figure}[H]
\centering
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_alert_battery.png}
\vspace{2mm}
{\footnotesize Batería}
\end{minipage}
\hfill
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_alert_brakes.png}
\vspace{2mm}
{\footnotesize Frenos}
\end{minipage}
\hfill
\begin{minipage}{0.30\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_alert_temperature.png}
\vspace{2mm}
{\footnotesize Temperatura}
\end{minipage}
\caption{Pantallas de detalle para avisos de batería, frenos y temperatura. Elaboración propia.}
\label{fig:mobile-alert-details-primary}
\end{figure}

\begin{figure}[H]
\centering
\begin{minipage}{0.38\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_alert_tires.png}
\vspace{2mm}
{\footnotesize Llantas}
\end{minipage}
\hfill
\begin{minipage}{0.38\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_alert_consumption.png}
\vspace{2mm}
{\footnotesize Consumo y eficiencia}
\end{minipage}
\caption{Pantallas de detalle para llantas y consumo dentro del flujo móvil. Elaboración propia.}
\label{fig:mobile-alert-details-secondary}
\end{figure}

El módulo de perfil reúne usuario, vehículo, emparejamiento y configuración. La administración queda separada de las pantallas de lectura para evitar cambios accidentales durante una prueba o una sesión de conducción.

\begin{figure}[H]
\centering
\begin{minipage}{0.40\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_profile.png}
\vspace{2mm}
{\footnotesize Perfil y configuración}
\end{minipage}
\hfill
\begin{minipage}{0.40\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_vehicle.png}
\vspace{2mm}
{\footnotesize Datos del vehículo}
\end{minipage}
\caption[Perfil y vehículo en la aplicación móvil]{Pantallas de administración del perfil y del vehículo asociado al usuario de prueba. Elaboración propia.}
\label{fig:mobile-profile-vehicle}
\end{figure}

\subsection{Conexión OBD-II desde la app}

La pestaña \emph{Tiempo real} concentra la interacción con el adaptador OBD-II. El flujo inicia con la búsqueda del dispositivo BLE del proyecto, mostrado al usuario como adaptador AutoSense. Si el hardware no está disponible, la pantalla informa que no se encontró el dispositivo y ofrece reintentar o trabajar con perfiles manuales. Así se distingue entre tres estados: sin conexión, sin perfil y con telemetría activa.

Con una conexión válida, la aplicación solicita información del adaptador, lee VIN, consulta PIDs soportados, transfiere perfiles y activa la telemetría. La comunicación usa características RX/TX y notificaciones para recibir eventos desde el ESP32-S3. El firmware mantiene la política de solo lectura, por lo que la app actúa como cliente de visualización y coordinación, no como herramienta de modificación de la ECU.

Se capturaron dos estados de esta vista. El primero muestra el emparejamiento cuando el simulador iOS no tiene hardware BLE conectado. El segundo muestra la telemetría en vivo en modo demo, usada para validar la presentación de velocidad, RPM, temperatura, voltaje y otras magnitudes. Con el adaptador físico, la misma pantalla recibe datos por BLE desde el ESP32-S3.

\begin{figure}[H]
\centering
\begin{minipage}{0.40\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_realtime_pairing.png}
\vspace{2mm}
{\footnotesize Emparejamiento OBD-II}
\end{minipage}
\hfill
\begin{minipage}{0.40\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/mobile_realtime_live.png}
\vspace{2mm}
{\footnotesize Telemetría en vivo}
\end{minipage}
\caption{Estados de conexión y visualización en tiempo real desde la aplicación móvil. Elaboración propia.}
\label{fig:mobile-realtime}
\end{figure}

\subsection{Sincronización y persistencia móvil}

La sincronización móvil se diseñó para separar los datos de interacción del usuario, la configuración del vehículo y la telemetría operativa. Firebase Authentication identifica al usuario; Firestore conserva información estructurada como perfil, vehículo, estado de dashboard, trazabilidad e índices de exportación; Firebase Functions resuelve perfiles vehiculares cuando la aplicación consulta por VIN; y Firebase Storage aloja tanto archivos de perfil como respaldos binarios del historial OBD-II sincronizado por la app. Esta arquitectura reduce la dependencia de perfiles embebidos dentro de la aplicación y facilita actualizar configuraciones sin recompilar el cliente móvil.

En paralelo, la aplicación mantiene estado local suficiente para ofrecer una experiencia continua. El usuario puede conservar sesión, ver datos recientes, acceder a la configuración del vehículo y entrar al flujo de tiempo real sin repetir pasos innecesarios. Cuando no hay adaptador conectado, el modo demo permite verificar diseño, navegación y comportamiento de la interfaz, pero no sustituye la validación sobre vehículo real. La diferencia entre demo y lectura real queda indicada en la pantalla para no confundir datos simulados con datos adquiridos por OBD-II.

El diseño móvil de AutoSense integra navegación autenticada, administración de usuario, configuración vehicular, conexión BLE, visualización de telemetría, métricas de eficiencia, alertas y sincronización con Firebase. Este apartado se concentra en la interfaz que utiliza el usuario; el almacenamiento de datos y la integración completa con las demás capas del sistema se desarrollan más adelante.

\section{Diseño de almacenamiento de datos}

El diseño de almacenamiento de AutoSense se organiza como una cadena de persistencia en tres niveles: el ESP32-S3 conserva los datos necesarios durante la recolección, la aplicación móvil mantiene estado local para continuidad de uso y Firebase centraliza la información que debe sincronizarse con el usuario. La persistencia se resuelve mediante memoria flash interna del ESP32-S3, almacenamiento seguro y archivos locales en la aplicación, y servicios administrados de Firebase para la capa remota.

Esta organización responde a la naturaleza del sistema. Durante la conducción, el adaptador debe registrar muestras de telemetría sin depender de conectividad móvil ni de una sesión activa de la aplicación. Por ello, el almacenamiento local del ESP32-S3 es el primer punto de resguardo. La aplicación actúa como interfaz, caché operativo y puente de sincronización, mientras que Firebase conserva perfiles, estado del usuario, viajes y datos estructurados que deben estar disponibles entre sesiones.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    node distance=1.1cm and 1.5cm,
    box/.style={draw, rounded corners=5pt, align=center, minimum width=3.8cm, minimum height=1.1cm, font=\small, inner sep=6pt},
    edge/.style={-{Latex[length=3mm]}, thick},
    note/.style={font=\footnotesize\bfseries, align=center}
]
\node[note] (localTitle) at (0,0) {Persistencia local\\en ESP32-S3};
\node[note] (cloudTitle) at (6.3,0) {Sincronización\\móvil y nube};

\node[box, fill=gray!12, below=0.65cm of localTitle] (obd) {\autosenseicon{obd}\\OBD-II / CAN\\lecturas del vehículo};
\node[box, fill=blue!10, below=of obd] (sample) {\autosenseicon{stats}\\\texttt{ObdCompactSample}\\muestra validada};
\node[box, fill=orange!15, below=of sample] (log) {\autosenseicon{storage}\\\texttt{obdlog}\\historial binario};
\node[box, fill=yellow!16, left=1.2cm of sample] (config) {\autosenseicon{config}\\\texttt{configfs}\\LittleFS\\configuración y perfil};

\node[box, fill=green!12, below=0.65cm of cloudTitle] (ble) {\autosenseicon{ble}\\BLE\\telemetría e historial};
\node[box, fill=purple!10, below=of ble] (app) {\autosenseicon{phone}\\Aplicación móvil\\caché, sesión y exportación};
\node[box, fill=cyan!12, below=of app] (cloud) {\autosenseicon{firebase}\\Firebase\\Firestore / Storage};

\draw[edge] (obd) -- (sample);
\draw[edge] (sample) -- (log);
\draw[edge] (config) -- (sample);
\draw[edge] (sample.east) -- node[above, font=\footnotesize]{stream} (ble.west);
\draw[edge] (ble) -- (app);
\draw[edge] (app) -- (cloud);
\end{tikzpicture}%
}
\caption{Flujo general de persistencia y sincronización de datos en AutoSense. Elaboración propia.}
\label{fig:storage-flow}
\end{figure}

\subsection{Almacenamiento local en el ESP32-S3}

El ESP32-S3 utiliza dos zonas de almacenamiento diferenciadas. La primera corresponde a la partición \texttt{configfs}, montada mediante LittleFS. En esta zona se conservan archivos de configuración y estado operativo, como \texttt{config.ini}, el perfil activo del vehículo en \texttt{/active\_profile.json} y el estado persistente del detector de anomalías. Estos archivos no representan el historial principal de telemetría, sino la información necesaria para que el dispositivo pueda arrancar con la misma política de lectura, perfil vehicular y parámetros de análisis que tenía antes del reinicio.

La segunda zona es la partición raw \texttt{obdlog}, reservada exclusivamente para historial OBD-II. Esta partición inicia en \texttt{0x1A0000} y tiene tamaño \texttt{0x650000}. A diferencia de \texttt{configfs}, no se maneja como un sistema de archivos con nombres de archivo; se escribe directamente como una secuencia de registros binarios de tamaño fijo. Esta separación evita mezclar configuración con telemetría histórica y permite que el registro tenga comportamiento predecible durante sesiones prolongadas de conducción.

El firmware no guarda el historial en JSON, CSV ni texto hexadecimal dentro del ESP32-S3. Cada muestra histórica se codifica en un registro binario de 24 bytes. Esta representación reduce el espacio ocupado por cada lectura, disminuye el volumen de escritura sobre la flash y evita el costo de formatear texto en un microcontrolador que debe seguir atendiendo CAN, BLE, dashboard USB e inferencia local. En un sistema que recolecta datos cada pocos segundos, el ahorro acumulado es significativo: guardar texto sería más legible para humanos, pero menos eficiente para memoria flash y para operación continua.

El registro se escribe según el intervalo de guardado configurado; en la configuración actual, el valor por defecto es de 10 segundos. Antes de escribir, el logger verifica que exista al menos una señal válida mediante \texttt{validMask != 0}. Esto evita llenar la memoria con muestras vacías cuando el vehículo no responde, cuando el bus no está disponible o cuando las señales disponibles están fuera de la ventana de vigencia.

\begin{table}[H]
\centering
\small
\caption{Niveles de almacenamiento utilizados por AutoSense. Elaboración propia con base en la implementación del proyecto.}
\label{tab:storage-layers}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{0.18\textwidth} p{0.20\textwidth} p{0.28\textwidth} p{0.24\textwidth}@{}}
\toprule
\textbf{Capa} & \textbf{Tecnología} & \textbf{Datos guardados} & \textbf{Propósito} \\
\midrule
ESP32-S3 & LittleFS en \texttt{configfs} & Configuración, perfil activo y estado del modelo local & Arranque consistente y operación sin depender de la app \\
ESP32-S3 & Partición raw \texttt{obdlog} & Muestras OBD-II compactas en binario & Historial local durante la recolección vehicular \\
Aplicación móvil & \texttt{SecureStore} & Sesión Firebase y sesión activa de viaje & Continuidad de uso y recuperación de estado local \\
Aplicación móvil & FileSystem de Expo & Caché del perfil vehicular activo y archivos \texttt{.bin} exportados del \texttt{obdlog} & Reutilizar perfiles y conservar respaldo local del historial descargado \\
Firebase & Firestore y Storage & Usuario, vehículo, viajes, alertas, dashboard, perfiles e historial binario OBD-II & Sincronización remota y disponibilidad entre sesiones \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Formato binario del historial OBD-II}

El formato de registro v1 está orientado a guardar solo las variables necesarias para análisis histórico, validación de conducción y reconstrucción básica del estado del vehículo. Cada registro inicia con dos bytes de identificación \texttt{AS}, contiene una versión de formato, conserva un contador de secuencia y termina con un \texttt{crc8}. Esta estructura permite distinguir registros válidos de zonas borradas, detectar escrituras incompletas y continuar el registro después de un reinicio.

\begin{table}[H]
\centering
\small
\caption{Estructura del registro binario v1 en la partición \texttt{obdlog}. Elaboración propia con base en el firmware AutoSense.}
\label{tab:obdlog-record}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{0.24\textwidth} p{0.12\textwidth} p{0.58\textwidth}@{}}
\toprule
\textbf{Campo} & \textbf{Tamaño} & \textbf{Función} \\
\midrule
\texttt{magic} & 2 bytes & Identifica registros AutoSense mediante \texttt{AS}. \\
\texttt{version} & 1 byte & Indica la versión del formato binario. \\
\texttt{validMask} & 2 bytes & Marca qué señales estaban disponibles al momento de compactar la muestra. \\
\texttt{sequence} & 4 bytes & Contador creciente usado para ordenar y continuar el historial. \\
\texttt{uptimeSeconds} & 4 bytes & Tiempo de operación del ESP32-S3 al momento de guardar la muestra. \\
\texttt{rpm} & 2 bytes & Revoluciones por minuto del motor. \\
\texttt{speedKph} & 1 byte & Velocidad vehicular en km/h. \\
\texttt{coolantRaw} & 1 byte & Temperatura del refrigerante codificada como \texttt{temperatura + 40}. \\
\texttt{throttlePct} & 1 byte & Apertura del acelerador en porcentaje. \\
\texttt{fuelLevelPct} & 1 byte & Nivel de combustible en porcentaje. \\
\texttt{engineLoadPct} & 1 byte & Carga calculada del motor. \\
\texttt{mapKpa} & 1 byte & Presión absoluta del múltiple en kPa. \\
\texttt{mafGps} & 1 byte & Flujo de aire másico redondeado a g/s. \\
\texttt{ecuVoltage} & 1 byte & Voltaje de ECU en decivoltios. \\
\texttt{crc8} & 1 byte & Verificación de integridad del registro. \\
\bottomrule
\end{tabular}
\end{table}

El uso de \texttt{sequence} permite que el sistema escanee la partición al arrancar, encuentre el último registro válido y continúe en la siguiente posición disponible. El uso de \texttt{crc8} evita aceptar registros corruptos o escrituras parciales como datos reales. Esto es relevante en un dispositivo alimentado desde el puerto OBD-II, porque el apagado del vehículo o la desconexión física pueden ocurrir sin una secuencia formal de cierre.

La partición \texttt{obdlog} se administra como un log circular. Cada sector de flash mide 4096 bytes y contiene 170 registros de 24 bytes, dejando un pequeño espacio sin utilizar por sector. Cuando el puntero de escritura llega al inicio de un sector, el sector se borra antes de escribir nuevos registros. Cuando se llena la partición completa, el sistema regresa al inicio y reemplaza las muestras más antiguas. Con el tamaño configurado, el firmware dispone de 1616 sectores, equivalentes a 274720 registros. A 10 segundos por registro, esta capacidad representa aproximadamente 31.8 días de historial continuo.

La muestra compacta proviene de \texttt{ObdCompactSample}. Esta estructura contiene las señales recientes que el firmware pudo validar desde OBD-II, pero el registro binario v1 no exporta todas las señales posibles. En la versión actual se guardan RPM, velocidad, temperatura del refrigerante, acelerador, combustible, carga del motor, MAP, MAF y voltaje de ECU. Señales como temperatura de admisión y avance de chispa pueden participar en telemetría BLE o en análisis local si están disponibles, pero no forman parte principal del registro histórico v1. Esta limitación es una decisión de tamaño: el formato prioriza un conjunto estable de variables relevantes antes que guardar todo el dashboard.

\subsection{Persistencia móvil y sincronización remota}

La aplicación móvil complementa el almacenamiento embebido con persistencia local de sesión, perfil e historial sincronizado. En la app, \texttt{SecureStore} conserva la sesión de Firebase y mantiene el acceso del usuario entre aperturas de la aplicación. El mismo mecanismo se usa para guardar la sesión activa de viaje, de modo que el seguimiento pueda recuperarse o cerrarse de forma coherente si la aplicación se reinicia. Para perfiles vehiculares, la aplicación utiliza FileSystem de Expo: guarda el JSON del perfil activo y su metadato asociado en el directorio local de documentos, evitando descargar el mismo perfil cada vez que se entra al flujo OBD-II.

Cuando el teléfono se conecta al adaptador, la aplicación consulta el estado del log mediante BLE, lee el último contador sincronizado y solicita únicamente los registros con \texttt{sequence} mayor al cursor local o remoto. La exportación se realiza por el mismo servicio GATT usado por AutoSense, mediante comandos de información, inicio de exportación, chunks y cierre de sesión. El ESP32-S3 no borra el \texttt{obdlog} después de sincronizar; conserva su historial circular y la app guarda una copia \texttt{.bin} en el almacenamiento local del teléfono.

En la nube, Firestore conserva el documento principal del usuario en la colección \texttt{users}. Allí se guarda información estructurada como datos de perfil, vehículo, configuración, estado de conexión en tiempo real, dashboard, eficiencia y alertas. Los viajes se almacenan como subcolección bajo \texttt{users}, asociada al identificador del usuario autenticado. Firebase Storage se utiliza para perfiles vehiculares descargables y para respaldar los archivos binarios históricos exportados desde el ESP32-S3. En este último caso, la ruta se organiza por usuario, dispositivo y rango de secuencia; Firestore mantiene el índice de exportación y el cursor \texttt{lastSyncedSequence}, lo que evita volver a subir registros ya respaldados.

Las Cloud Functions participan como servicios consumidos por la aplicación para resolver el perfil vehicular a partir del VIN, obtener información de descarga de perfil y registrar la aplicación de un perfil al dispositivo. La sincronización del historial crudo, en cambio, se ejecuta desde la app: descarga los registros no sincronizados por BLE, conserva el archivo local y lo respalda en Firebase Storage cuando el modo de datos permite sincronización en la nube.

El almacenamiento de AutoSense está diseñado para no perder la recolección por falta de conexión móvil y, al mismo tiempo, permitir sincronización remota cuando la app y Firebase están disponibles. El ESP32-S3 resguarda la evidencia básica en binario, la aplicación mantiene continuidad de sesión, perfiles e historial descargado, y Firebase organiza los datos de usuario, vehículo, recorridos e índices de archivos históricos para consulta posterior.

\section{Interacción del sistema}

La interacción del sistema describe la forma en que los componentes de AutoSense cooperan durante una sesión real de uso. A diferencia de la arquitectura, que explica la estructura por capas, aquí se presenta el orden de comunicación entre el usuario, la aplicación móvil, el ESP32-S3, el vehículo y Firebase. El flujo principal inicia cuando el usuario entra al módulo de tiempo real, continúa con la búsqueda del dispositivo \texttt{AutoSense OBD2}, resuelve el perfil vehicular, aplica la configuración al firmware y finalmente presenta telemetría en vivo junto con actualizaciones de estado en la nube.

El diseño de interacción mantiene una separación clara de responsabilidades. El ESP32-S3 no se conecta directamente a Firebase ni ejecuta lógica de usuario; su función es responder comandos BLE, aplicar perfiles validados, consultar el vehículo por OBD-II/CAN y emitir telemetría. La aplicación móvil actúa como coordinador: autentica al usuario, localiza el dispositivo por BLE, solicita VIN y perfiles, envía configuraciones al firmware, presenta datos al usuario y sincroniza estados seleccionados con Firestore. Firebase participa como plano de control, no como transporte de adquisición directa: resuelve perfiles, entrega metadatos, registra aplicaciones de perfil y conserva el estado del usuario.

\subsection{Inicio de sesión y descubrimiento del dispositivo}

La interacción comienza antes de conectarse al vehículo. La aplicación móvil utiliza el estado de sesión de Firebase para determinar si el usuario puede ingresar al flujo autenticado. Una vez dentro de la pestaña de tiempo real, la pantalla de emparejamiento intenta reutilizar una conexión activa si ya existe; de lo contrario, busca dispositivos BLE que correspondan al servicio de AutoSense o que anuncien un nombre asociado al proyecto. En Android, la app solicita permisos Bluetooth y ubicación cuando son necesarios; en iOS, la conexión depende de la disponibilidad del módulo BLE nativo dentro del \emph{development build}.

El dispositivo embebido anuncia el nombre \texttt{AutoSense OBD2} y expone un servicio GATT propio. La aplicación se conecta al dispositivo, solicita un MTU mayor cuando la plataforma lo permite y descubre servicios y características antes de enviar comandos. El protocolo utiliza una característica RX para comandos escritos desde la app y una característica TX para respuestas y notificaciones. Esta organización evita depender de lectura serial por cable para el usuario final, pero conserva USB-CDC como canal técnico de monitoreo.

Si la app no encuentra el dispositivo en el tiempo de búsqueda configurado, cambia a un estado de error visible para el usuario. Si ya existe un dispositivo previamente conectado, intenta reconectarse usando el identificador guardado localmente. Este comportamiento mejora la experiencia de uso porque evita repetir exploraciones completas cuando el mismo teléfono se conecta al mismo adaptador en sesiones sucesivas.

\subsection{Identificación del vehículo y aplicación de perfil}

Después de establecer el enlace BLE, la aplicación solicita información básica al ESP32-S3 mediante \texttt{GET\_DEVICE\_INFO}. Esta respuesta incluye identificador del dispositivo, versión de firmware, versión de hardware y tamaño máximo recomendado para transferencia de fragmentos. Luego la app intenta leer el VIN con \texttt{READ\_VIN}. Si el VIN está disponible, se consulta la función remota de resolución de perfil; si no está disponible, la app permite seleccionar manualmente un perfil compatible.

Cuando la resolución identifica el perfil verificado del proyecto, la app descarga el perfil \texttt{vw\_passat\_2016}; si el vehículo no coincide con ese caso, puede recurrir al perfil genérico o al perfil de simulador según corresponda. Antes de transferir el perfil, la aplicación consulta el perfil activo con \texttt{GET\_ACTIVE\_PROFILE}. Si el ESP32-S3 ya tiene la misma versión aplicada, la app evita repetir la transferencia. Si el perfil es distinto o no existe, inicia una transferencia por fragmentos.

La transferencia de perfil se realiza en cuatro pasos. Primero, la app envía \texttt{START\_PROFILE\_TRANSFER} con tamaño, versión y hash esperado. Luego transmite el JSON del perfil mediante comandos \texttt{PROFILE\_CHUNK}; cada fragmento viaja codificado en base64 porque la librería BLE móvil representa los valores escritos en ese formato. Al finalizar, la app envía \texttt{END\_PROFILE\_TRANSFER}, y el firmware valida tamaño, estructura y SHA-256 antes de aceptar la configuración. Finalmente, \texttt{APPLY\_PROFILE} guarda el perfil activo en LittleFS y reinicia el plan de sondeo OBD-II con las señales permitidas.

\begin{table}[H]
\centering
\footnotesize
\caption{Comandos principales de interacción BLE entre la app y el ESP32-S3. Elaboración propia con base en el protocolo BLE del proyecto.}
\label{tab:system-interaction-ble-commands}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.30\textwidth} >{\raggedright\arraybackslash}p{0.19\textwidth} >{\raggedright\arraybackslash}p{0.41\textwidth}@{}}
\toprule
\textbf{Comando} & \textbf{Momento de uso} & \textbf{Función dentro de AutoSense} \\
\midrule
\texttt{GET\_DEVICE\_INFO} & Conexión inicial & Obtiene identificador, versión y tamaño máximo de fragmento. \\
\texttt{READ\_VIN} & Identificación vehicular & Solicita el VIN si el firmware ya lo obtuvo por Mode 09. \\
\texttt{GET\_ACTIVE\_PROFILE} & Antes de transferir & Verifica si el ESP32-S3 ya tiene el perfil requerido. \\
\texttt{START\_PROFILE\_TRANSFER} & Inicio de carga & Declara tamaño y hash del perfil que será enviado. \\
\texttt{PROFILE\_CHUNK} & Transferencia & Envía fragmentos del JSON de perfil hacia el firmware. \\
\texttt{END\_PROFILE\_TRANSFER} & Validación & Solicita validar tamaño, estructura y hash antes de aplicar. \\
\texttt{APPLY\_PROFILE} & Activación & Guarda el perfil y reconstruye el plan de consulta OBD-II. \\
\texttt{GET\_SUPPORTED\_PIDS} & Registro de soporte & Obtiene una lista resumida de PIDs soportados por el vehículo. \\
\texttt{START\_STREAM} & Telemetría en vivo & Activa notificaciones periódicas de datos OBD-II. \\
\texttt{STOP\_STREAM} & Cierre de sesión & Detiene el envío de telemetría hacia la aplicación. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Telemetría en vivo y actualización de estado}

Una vez aplicado el perfil, la app marca el estado de conexión del usuario en Firestore y abre la pantalla de datos en vivo. En esta etapa el comando principal es \texttt{START\_STREAM}. El ESP32-S3 responde activando el envío de mensajes \texttt{TELEMETRY} cada 1000 ms. Cada mensaje incluye variables como velocidad, RPM, temperatura del motor, combustible estimado, carga del motor, voltaje, acelerador, temperatura de admisión, \texttt{validMask}, clasificación de ruta y resultado del detector de anomalías local.

La app normaliza la telemetría recibida antes de presentarla. Esto significa que convierte valores ausentes a estados seguros, limita rangos numéricos y traduce estados de ruta o severidad a categorías conocidas por la interfaz. La pantalla de tiempo real muestra velocidad, RPM, temperatura, combustible, carga del motor, voltaje, acelerador, admisión y estado de diagnóstico. Si el flujo opera en modo demo, la pantalla genera datos simulados para validar navegación e interfaz; cuando existe conexión real con el ESP32-S3, la telemetría proviene del stream BLE.

Además de mostrar datos, la aplicación actualiza estado remoto de forma controlada. El dashboard y las alertas se sincronizan con Firestore solo cuando hay telemetría real, conexión activa y datos útiles. El intervalo de sincronización se limita para no escribir en la nube en cada notificación BLE. En paralelo, el seguimiento de viaje se activa cuando el vehículo se considera en movimiento y el motor muestra actividad. Ese módulo registra o actualiza documentos de viaje, distancia, duración y consumo estimado, sin bloquear la visualización en vivo.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=0.55cm,
    seqbox/.style={draw, rounded corners=5pt, align=center, text width=0.58\textwidth, minimum width=0.66\textwidth, minimum height=0.85cm, font=\small, inner sep=5pt},
    edge/.style={-{Latex[length=3mm]}, thick}
]
\node[seqbox, fill=gray!12] (user) {\autosenseicon{user}\\Usuario abre\\Tiempo real};
\node[seqbox, fill=green!12, below=of user] (scan) {\autosenseicon{phone}\\App busca\\AutoSense OBD2};
\node[seqbox, fill=blue!10, below=of scan] (info) {\autosenseicon{ble}\\\texttt{GET\_DEVICE\_INFO}\\y \texttt{READ\_VIN}};
\node[seqbox, fill=cyan!12, below=of info] (profile) {\autosenseicon{firebase}\\Firebase resuelve\\perfil vehicular};
\node[seqbox, fill=yellow!16, below=of profile] (transfer) {\autosenseicon{ble}\\App transfiere\\perfil por BLE};
\node[seqbox, fill=orange!15, below=of transfer] (apply) {\autosenseicon{esp32}\\ESP32-S3 aplica\\perfil y sondeo};
\node[seqbox, fill=purple!10, below=of apply] (stream) {\autosenseicon{stats}\\\texttt{START\_STREAM}\\telemetría 1 s};
\node[seqbox, fill=teal!12, below=of stream] (sync) {\autosenseicon{firebase}\\App muestra datos\\y sincroniza estado};

\draw[edge] (user) -- (scan);
\draw[edge] (scan) -- (info);
\draw[edge] (info) -- (profile);
\draw[edge] (profile) -- (transfer);
\draw[edge] (transfer) -- (apply);
\draw[edge] (apply) -- (stream);
\draw[edge] (stream) -- (sync);
\end{tikzpicture}
\caption{Secuencia general de interacción entre usuario, aplicación, ESP32-S3 y Firebase. Elaboración propia.}
\label{fig:system-interaction-sequence}
\end{figure}

\subsection{Manejo de errores y continuidad operativa}

La interacción del sistema contempla fallos esperados en un entorno móvil y automotriz. Si el VIN no puede leerse, la aplicación no detiene el flujo completo; presenta selección manual de perfil. Si la resolución remota de perfil falla, también se habilita el modo manual. Si BLE no responde dentro del tiempo esperado, la solicitud queda rechazada por \emph{timeout} y la pantalla muestra un estado de conexión fallida. Esta lógica evita que el usuario quede en una pantalla indefinida sin saber si el problema está en Bluetooth, en el firmware o en el vehículo.

El protocolo BLE también incorpora mecanismos de continuidad. La app mantiene una tabla de solicitudes pendientes con identificador único, de manera que cada respuesta pueda asociarse al comando que la originó. Si una notificación BLE llega truncada o no se procesa correctamente, la app intenta leer la característica TX como respaldo. Este mecanismo no cambia la lógica del firmware; solo mejora la tolerancia del cliente móvil ante diferencias de comportamiento entre plataformas BLE.

En el ESP32-S3, la desconexión de un cliente BLE desactiva el streaming, limpia notificaciones pendientes, descarta transferencias incompletas y reinicia la publicidad del dispositivo. Esto permite que una sesión interrumpida pueda empezar nuevamente sin reiniciar físicamente el adaptador. Del lado de la aplicación, al salir de la pantalla en vivo se detiene el seguimiento de viaje, se marca el estado de conexión como inactivo y se cancela la conexión BLE activa.

El resultado de esta interacción es un flujo local-first: la adquisición y validación de datos se mantienen en el ESP32-S3, la app coordina la experiencia de usuario y la nube conserva estado sincronizado sin ser un requisito para cada muestra individual. Esta separación permite que AutoSense siga mostrando y registrando telemetría local aunque la red no esté disponible, y que sincronice información de usuario, vehículo y viajes cuando la aplicación tenga conectividad.

\section{Módulo de análisis de datos}

El módulo de análisis de datos de AutoSense se ubica después de la adquisición OBD-II/CAN, el filtrado de señales y la compactación de muestras realizada por el firmware. Su función no consiste en solicitar nuevos datos al vehículo ni en modificar parámetros de la ECU, sino en interpretar la telemetría ya recolectada para estimar contexto de conducción, detectar desviaciones respecto al comportamiento habitual del automóvil y entregar una salida resumida a la aplicación móvil. Este enfoque mantiene el análisis cerca de la fuente de datos, lo que resulta coherente con arquitecturas de procesamiento en el borde para sistemas vehiculares conectados \cite{zhang2020_iov_edge,kalalas2025_ai_driven_monitoring}.

En el prototipo, el análisis se ejecuta en el ESP32-S3 y se apoya en dos bloques principales: el clasificador de ruta y el detector local de anomalías. El primero interpreta el patrón de velocidad para distinguir escenarios de ciudad y carretera; el segundo compara las señales vehiculares contra una línea base aprendida del propio vehículo. De esta forma, AutoSense no presenta la telemetría como datos aislados, sino como información contextual: una misma variación de RPM, temperatura, carga del motor o voltaje puede tener distinta interpretación si ocurre en ralentí, aceleración, tráfico urbano o conducción sostenida en carretera.

\subsection{Entrada y preparación de muestras}

La entrada del módulo de análisis es la estructura \texttt{ObdCompactSample}, generada por el servicio OBD-II del firmware. Esta estructura concentra únicamente las señales útiles para historial, transmisión BLE y análisis local: RPM, velocidad, temperatura del refrigerante, posición del acelerador, nivel de combustible, carga del motor, presión MAP, flujo MAF, voltaje ECU, temperatura de admisión y avance de encendido cuando están disponibles. Cada muestra incluye además una máscara \texttt{validMask}, que indica qué campos contienen datos recientes y evita que el análisis trate como válidos valores ausentes o vencidos.

Antes de procesar una muestra, el firmware verifica que exista al menos una señal válida y que se haya cumplido el intervalo configurado para inferencia. En la configuración actual, el análisis de anomalías intenta ejecutarse cada 10 segundos, lo cual coincide con la lógica de recolección histórica y reduce el costo de cómputo frente a un análisis en cada paquete BLE. El ESP32-S3 también atiende adquisición OBD-II, clasificación de ruta, almacenamiento local, comunicación BLE y dashboard USB-CDC; por tanto, el análisis debe ser periódico, ligero y no bloqueante.

El diseño evita duplicar consultas al vehículo. El mismo dato que alimenta el logger binario y la telemetría en vivo se transforma internamente en una muestra de análisis, conservando la máscara de validez y las magnitudes ya decodificadas. Con esto, el sistema mantiene una separación clara entre adquisición y análisis: la capa OBD-II se encarga de obtener y convertir respuestas, mientras que el módulo analítico evalúa el comportamiento de las señales ya disponibles.

\subsection{Análisis local de anomalías}

El detector de anomalías se implementa en el firmware como un módulo local basado en línea base estadística, Z-score e Isolation Forest ligero. La línea base se construye con el algoritmo de Welford, que permite actualizar promedio y desviación estándar de manera incremental sin guardar todas las muestras anteriores. Esta característica es adecuada para un sistema embebido, porque reduce memoria, evita cálculos costosos sobre historiales completos y permite que el modelo se adapte al vehículo validado durante el uso normal.

La línea base no se maneja como un único promedio global. El firmware conserva estadísticas generales y estadísticas por contexto de conducción: \texttt{IDLE}, \texttt{CRUISING}, \texttt{ACCELERATING}, \texttt{DECELERATING} y \texttt{UNKNOWN}. Cuando el contexto tiene suficientes datos, la comparación se realiza contra ese comportamiento específico; si aún no existe suficiente historial, se usa la línea base global. Este mecanismo evita tratar como anomalía una condición que puede ser normal en un contexto determinado, por ejemplo una variación de RPM durante aceleración o una velocidad estable durante carretera.

Sobre esa línea base se calcula un Z-score por señal. En términos operativos, el firmware mide qué tan lejos se encuentra el valor actual respecto del promedio aprendido para esa señal y contexto. Para reducir falsos positivos, cada señal cuenta con un piso mínimo de desviación estándar; de esa manera, una variable que normalmente varía poco, como el voltaje ECU, no produce alertas exageradas por cambios pequeños. Los Z-score individuales se agregan en un puntaje de 0 a 100, y las señales con mayor desviación quedan registradas como contribuciones principales del evento.

Cuando el sistema ya cuenta con línea base suficiente, también puede usar un Tiny Isolation Forest entrenado localmente con muestras normales y estables. Este modelo está dimensionado para el microcontrolador: utiliza árboles pequeños, profundidad limitada e inferencia sin memoria dinámica. Su función es complementar el Z-score, detectando combinaciones de señales que pueden verse anómalas en conjunto aunque cada variable aislada no alcance un umbral crítico. En la configuración actual, el puntaje final combina 70\% del Z-score y 30\% del Isolation Forest cuando el modelo está listo; antes de ese punto, el sistema utiliza únicamente la salida estadística.

El resultado se traduce en cuatro niveles de severidad: \texttt{NORMAL}, \texttt{WATCH}, \texttt{WARNING} y \texttt{CRITICAL}. Para evitar alertas por eventos aislados, el firmware aplica un mecanismo de \emph{debounce}: una muestra inusual puede elevar el estado a vigilancia, pero una alerta fuerte requiere persistencia dentro de una ventana de inferencias. Con el intervalo actual de 10 segundos, esto significa que una condición debe mantenerse aproximadamente durante varias muestras antes de ser presentada como advertencia mayor. Este criterio mejora la estabilidad de la salida y evita que el usuario reciba alarmas por ruido de sensores o transiciones normales de conducción.

\subsection{Clasificación de ruta y contexto de conducción}

El análisis de datos también incorpora la clasificación de ruta. Este bloque utiliza principalmente velocidad, RPM, carga del motor y posición del acelerador para estimar si el vehículo opera en ciudad, carretera o en un estado intermedio. La clasificación no depende de GPS; se basa en el comportamiento dinámico observado desde la telemetría OBD-II. Por ejemplo, una velocidad promedio alta, pocas detenciones y baja variación de aceleración favorecen la clasificación de carretera, mientras que velocidades bajas, paradas frecuentes y cambios repetidos favorecen la clasificación urbana.

La campaña de recolección contempla conducción real en contextos \texttt{city} y \texttt{highway}. Al separar esos escenarios, AutoSense interpreta mejor las muestras: el consumo, la carga del motor, la velocidad y los cambios de aceleración no siguen el mismo patrón en tráfico urbano que en carretera. La etiqueta de ruta también ordena el análisis posterior, porque permite comparar segmentos por condición de conducción y no solo por tiempo.

El clasificador trabaja con una ventana temporal y no con una única lectura. Esto reduce cambios bruscos de estado cuando el vehículo atraviesa transiciones cortas, como una salida de carretera, una parada temporal o una aceleración puntual. La salida incluye tipo de ruta, estado interno, nivel de confianza, puntaje y una razón textual breve. Estos campos se transmiten junto con la telemetría y sirven para que la aplicación móvil muestre una interpretación más completa del estado de conducción.

\subsection{Persistencia del modelo y continuidad}

Para que el análisis no empiece desde cero en cada reinicio, el firmware guarda estado local. La línea base estadística se conserva en \texttt{Preferences}, dentro del espacio \texttt{obd\_anom}, mientras que el modelo Tiny Isolation Forest se almacena en LittleFS como \texttt{/anomaly\_iforest.bin}. Este esquema separa datos de configuración y modelo, y permite recuperar el aprendizaje acumulado siempre que el firmware, el perfil vehicular y la identidad del modelo sigan siendo compatibles.

La identidad del análisis se calcula a partir de la versión de firmware, el identificador del perfil y el hash del perfil activo. Si alguno de estos elementos cambia, el módulo invalida el estado anterior y reinicia la línea base. Esta decisión es necesaria porque un modelo aprendido con otro perfil, otro conjunto de señales o una versión distinta del firmware podría producir comparaciones incorrectas. En el caso de AutoSense, el perfil \texttt{vw\_passat\_2016} representa la validación principal; si se aplica un perfil diferente, el detector debe aprender nuevamente el comportamiento normal del vehículo correspondiente.

La persistencia también se realiza por lotes. El estado se guarda cada intervalo configurado, por defecto cada 300 segundos cuando hubo cambios, en lugar de escribir flash después de cada inferencia. Con ello se reduce el desgaste de memoria y se mantiene la operación del sistema dentro de un patrón predecible. Esta estrategia es consistente con el almacenamiento local descrito para el historial OBD-II: se privilegia un formato compacto, una frecuencia controlada y una recuperación suficiente para continuar el análisis.

\subsection{Salida hacia la aplicación móvil}

La aplicación móvil no recalcula el modelo de anomalías. Recibe por BLE el resumen que publica el ESP32-S3: puntaje, severidad, máscara de área probable, estado de línea base y estado del modelo. La misma telemetría mantiene velocidad, RPM, temperatura, voltaje, combustible y carga del motor. Así, la interfaz muestra el valor medido junto con la interpretación local.

El campo de área probable no identifica una pieza dañada. Solo agrupa la desviación en zonas funcionales: motor, admisión, combustible, sistema eléctrico, temperatura, conducción o sensor. La salida orienta la revisión y ayuda a priorizar atención, pero no reemplaza un diagnóstico técnico ni garantiza una causa mecánica exacta. Ese uso es coherente con el mantenimiento predictivo asistido por datos: detectar patrones anómalos y conservar evidencia para una revisión posterior \cite{mahale2025_review_predictive_maintenance,tormos2025_explainable_ai}.

\begin{table}[htbp]
\centering
\small
\caption{Componentes del módulo de análisis de datos en AutoSense. Elaboración propia con base en el firmware AutoSense.}
\label{tab:data-analysis-module}
\begin{tabular}{p{0.25\textwidth}p{0.39\textwidth}p{0.27\textwidth}}
\toprule
\textbf{Componente} & \textbf{Función} & \textbf{Salida en AutoSense} \\
\midrule
Muestra compacta & Agrupa las señales OBD-II decodificadas y marca cuáles están vigentes mediante \texttt{validMask}. & Entrada común para análisis, BLE y registro histórico. \\
\addlinespace
Línea base Welford & Calcula promedio y desviación estándar incremental por señal y contexto. & Referencia normal del vehículo para comparar nuevas lecturas. \\
\addlinespace
Z-score & Mide la distancia de cada señal respecto a su línea base, con pisos mínimos de desviación. & Puntaje estadístico y señales con mayor desviación. \\
\addlinespace
Tiny Isolation Forest & Evalúa combinaciones de señales mediante un modelo local ligero. & Puntaje complementario cuando el modelo está entrenado. \\
\addlinespace
Debounce de severidad & Filtra eventos aislados antes de elevar una advertencia fuerte. & Estados \texttt{NORMAL}, \texttt{WATCH}, \texttt{WARNING} y \texttt{CRITICAL}. \\
\addlinespace
Clasificador de ruta & Estima ciudad, carretera o estado intermedio según la dinámica de conducción. & Contexto \texttt{city}/\texttt{highway}, confianza y razón de clasificación. \\
\addlinespace
Telemetría BLE & Publica el resumen calculado por el ESP32-S3 hacia la aplicación móvil. & Puntaje, severidad, área probable, estado de línea base y estado del modelo. \\
\bottomrule
\end{tabular}
\end{table}

El módulo de análisis convierte las lecturas OBD-II en indicadores interpretables sin desplazar la responsabilidad diagnóstica a la nube ni prometer predicciones mecánicas exactas. AutoSense detecta desviaciones, clasifica el contexto de conducción y presenta señales de atención temprana; la confirmación de una falla específica continúa dependiendo de inspección técnica, historial acumulado y contraste con síntomas reales del vehículo.

\section{Seguridad del sistema}

La seguridad de AutoSense se plantea como un conjunto de controles distribuidos entre el firmware, la aplicación móvil y Firebase. La intención principal no es convertir el prototipo en una plataforma de ciberseguridad automotriz completa, sino reducir riesgos concretos durante la lectura vehicular, proteger la información del usuario y evitar que perfiles o comandos no válidos alteren el comportamiento del sistema. Por ello, el diseño prioriza una política de solo lectura sobre OBD-II/CAN, validación estricta de perfiles, autenticación de usuarios en la app y reglas de acceso en la nube.

\subsection{Seguridad por capas}

El sistema aplica seguridad por capas porque cada componente enfrenta riesgos distintos. En el vehículo, el riesgo principal es enviar comandos que modifiquen la ECU, borren información diagnóstica o activen funciones de control. En el ESP32-S3, el riesgo está en aceptar perfiles inválidos o comandos fuera del alcance del prototipo. En la aplicación móvil, el riesgo se relaciona con el acceso no autorizado a datos de usuario, sesión, vehículo y viajes. Finalmente, en Firebase, el riesgo consiste en que un usuario pueda leer o modificar información que no le corresponde.

Esta separación evita depender de un único mecanismo de protección. Aunque la aplicación resuelva y transfiera un perfil, el firmware vuelve a validarlo antes de aplicarlo. Aunque el usuario esté autenticado en Firebase, las reglas de Firestore y Storage vuelven a comprobar que el documento o archivo pertenezca al usuario correcto. Aunque exista un canal BLE operativo, los comandos aceptados por el ESP32-S3 permanecen limitados a información del dispositivo, perfil, telemetría y control del flujo de lectura. Esta redundancia es deliberada y permite que un error en una capa no implique automáticamente una acción insegura sobre el vehículo.

\subsection{Política de solo lectura en el firmware}

El control más importante del firmware es \texttt{ObdReadOnlyGuard}. Todo comando diagnóstico que sale por CAN pasa por esta política antes de ser transmitido. La guardia clasifica los servicios OBD-II y UDS en dos grupos: servicios permitidos de lectura y servicios bloqueados por riesgo de escritura, control o modificación del estado del vehículo. En uso normal, el contador \texttt{guard\_blocked} debe permanecer en cero; si aumenta, el evento se considera una señal de revisión durante las pruebas.

Para OBD-II estándar, el firmware permite los modos \texttt{01}, \texttt{02}, \texttt{03}, \texttt{06}, \texttt{07}, \texttt{09} y \texttt{0A}, asociados a datos actuales, \textit{freeze frame}, DTC almacenados, monitores a bordo, DTC pendientes, información del vehículo y DTC permanentes. En cambio, bloquea \texttt{04}, porque borra o reinicia información de emisiones, y \texttt{08}, porque habilita operaciones de control. Con ello, AutoSense conserva el enfoque de observación y evita que la prueba modifique evidencia diagnóstica del automóvil.

En el caso de UDS, solo se permiten \texttt{19} y \texttt{22}, correspondientes a lectura de información de DTC e identificación por datos. Los servicios asociados a cambio de sesión, reinicio de ECU, borrado de diagnóstico, acceso de seguridad, escritura de datos, control de entradas/salidas, rutinas, descarga, carga, transferencia y escritura de memoria quedan bloqueados. Incluso \texttt{23}, que técnicamente es lectura de memoria, se mantiene fuera del alcance porque implicaría un \textit{dump} de memoria de ECU y no aporta al objetivo del prototipo. La regla práctica es conservadora: si un servicio no está en la lista segura, el firmware lo bloquea.

\subsection{Validación de perfiles vehiculares}

Los perfiles vehiculares son una parte crítica de la seguridad, porque definen qué señales se consultan, con qué frecuencia y mediante qué fórmulas se interpretan. AutoSense no acepta un perfil únicamente porque la aplicación lo envíe por BLE. Antes de transferirlo, la app valida el esquema, el tamaño y los campos permitidos; después, el ESP32-S3 repite la validación antes de guardarlo como \texttt{/active\_profile.json} y aplicarlo al plan de sondeo.

La validación incluye un límite máximo de 16 KB, JSON válido, \texttt{schemaVersion} soportado, identificador y versión de perfil, bus \texttt{CAN}, señales presentes, PIDs en formato hexadecimal y fórmulas incluidas en una lista permitida. Además, el perfil descargado se contrasta con el hash SHA-256 esperado. Si el contenido recibido no coincide con el hash, si excede el tamaño máximo o si incluye una fórmula no permitida, el firmware rechaza la aplicación del perfil.

El perfil también se restringe por seguridad diagnóstica. En la app, los modos OBD permitidos para señales son \texttt{01} y \texttt{09}, mientras que modos como \texttt{04} y \texttt{08} quedan bloqueados. Para UDS extendido, solo se aceptan servicios de lectura permitidos. Esta doble validación evita que un perfil manipulado convierta la capa de configuración en una vía para enviar comandos de escritura o control al vehículo.

\subsection{Seguridad en la aplicación móvil}

La aplicación móvil utiliza Firebase Auth para el acceso de usuario mediante correo y contraseña o cuenta de Google. El estado de autenticación se centraliza en \texttt{SessionProvider}, que determina si el usuario puede ingresar al grupo de rutas autenticadas. Si no existe sesión válida, la navegación redirige al flujo de acceso; si ya existe sesión, se permite entrar a las pantallas de inicio, tiempo real, perfil, vehículo, alertas, eficiencia y viajes.

En dispositivos móviles, la sesión de Firebase se persiste mediante SecureStore de Expo. Esto evita manejar la sesión como texto plano dentro de almacenamiento ordinario de la aplicación y permite que el usuario conserve acceso entre aperturas sin volver a escribir credenciales. El mismo mecanismo se utiliza para conservar la sesión activa de viaje, de modo que el seguimiento pueda recuperarse o cerrarse de forma consistente después de reinicios de la app.

Los cambios sensibles de cuenta se manejan con controles adicionales. Para modificar correo o contraseña en cuentas basadas en contraseña, la app solicita reautenticación con la credencial actual antes de aplicar el cambio. Para cuentas de Google, la app no permite cambiar contraseña desde esa pantalla porque la autenticación depende del proveedor externo. Además, la actualización de correo se realiza mediante verificación previa, evitando cambiar el identificador principal sin confirmación del usuario.

\subsection{Reglas de acceso en Firebase}

Firestore está organizado alrededor de documentos de usuario en la colección \texttt{users} y subcolecciones como \texttt{trips}. Las reglas permiten lectura y escritura solo cuando el usuario autenticado coincide con el identificador del documento. También se validan campos obligatorios, tipos, rangos numéricos, formatos de correo y marcas de tiempo recientes. Campos inmutables como \texttt{uid}, \texttt{email} y \texttt{createdAt} no pueden modificarse durante una actualización ordinaria, y la eliminación de documentos de usuario o viajes queda bloqueada.

Las reglas también restringen colecciones auxiliares. Los dispositivos solo pueden leerse si pertenecen al usuario autenticado, los perfiles vehiculares publicados son de lectura autenticada y las aplicaciones de perfil solo se leen cuando corresponden al usuario. La escritura directa sobre dispositivos, perfiles vehiculares, aplicaciones de perfil y cachés de VIN no se permite desde el cliente. Ese diseño concentra cambios sensibles en servicios controlados, como las funciones que resuelven perfiles o registran la aplicación de un perfil vehicular.

En Firebase Storage se aplica una política similar. Las imágenes de perfil se almacenan bajo la ruta del usuario y solo pueden leerse o escribirse por ese mismo usuario autenticado. Los respaldos binarios del historial OBD-II también quedan bajo una ruta del usuario, con tipo \texttt{application/octet-stream} y límite de tamaño para evitar escrituras arbitrarias. Los perfiles vehiculares se publican bajo la ruta \texttt{vehicle-profiles} por identificador y versión; la app puede leerlos si el usuario está autenticado, pero la escritura queda bloqueada desde el cliente. Cualquier ruta no contemplada queda cerrada por defecto.

\begin{table}[htbp]
\centering
\small
\caption{Controles de seguridad aplicados en AutoSense. Elaboración propia con base en el firmware, la app y las reglas Firebase del proyecto.}
\label{tab:system-security-controls}
\begin{tabular}{p{0.23\textwidth}p{0.39\textwidth}p{0.29\textwidth}}
\toprule
\textbf{Capa} & \textbf{Control aplicado} & \textbf{Riesgo mitigado} \\
\midrule
Firmware OBD-II/CAN & Guardia \texttt{ObdReadOnlyGuard} con lista de servicios seguros. & Envío de comandos de borrado, control, escritura o reprogramación. \\
\addlinespace
Perfil vehicular & Validación de tamaño, SHA-256, JSON, bus, modos, servicios y fórmulas permitidas. & Aplicación de perfiles alterados o fuera del alcance del prototipo. \\
\addlinespace
BLE operativo & Comandos limitados a información, transferencia de perfil y telemetría. & Exposición de funciones no necesarias para lectura y monitoreo. \\
\addlinespace
Aplicación móvil & Firebase Auth, rutas protegidas y sesión en \texttt{SecureStore}. & Acceso no autenticado a pantallas y datos del usuario. \\
\addlinespace
Firestore & Reglas por propietario, validación de esquema, rangos y bloqueo de eliminación. & Lectura o escritura cruzada entre usuarios y datos mal formados. \\
\addlinespace
Storage & Avatares por usuario, logs OBD-II por usuario y perfiles vehiculares de solo lectura autenticada. & Escritura arbitraria de archivos, acceso cruzado entre usuarios o modificación directa de perfiles publicados. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Limitaciones y controles pendientes}

La seguridad actual debe entenderse dentro del alcance de un prototipo académico. El enlace BLE implementado no se documenta como un canal cifrado ni como un canal autenticado por token de usuario dentro del firmware. La protección principal del ESP32-S3 se basa en reducir el conjunto de comandos disponibles, validar estrictamente los perfiles recibidos y mantener la comunicación con el vehículo en modo de solo lectura. Por tanto, si una versión futura requiere operación en ambientes no controlados, convendría agregar emparejamiento/autorización BLE más estricta, control de dispositivo vinculado y verificación de sesión antes de aceptar comandos de configuración.

También quedan como mejora futura políticas más completas de retención y consulta del historial sincronizado. El sistema ya evita escrituras no autorizadas en Firebase y limita la eliminación directa desde el cliente, pero una versión de producción debería definir con precisión cuánto tiempo se conservan viajes, telemetría histórica, perfiles aplicados y evidencia de diagnóstico. Esta decisión no afecta la validación funcional del prototipo, pero sí sería necesaria para una operación prolongada con múltiples usuarios.

En síntesis, la seguridad de AutoSense se apoya en controles concretos y verificables: lectura vehicular no invasiva, perfiles validados, sesión móvil autenticada y reglas remotas restrictivas. El sistema no afirma resolver todos los riesgos de ciberseguridad automotriz, pero sí establece una base prudente para recolectar y analizar datos sin modificar el vehículo ni exponer información de usuario fuera de su cuenta.

\section{Funcionamiento general}

El funcionamiento general de AutoSense puede entenderse como una sesión continua de observación vehicular. El adaptador ESP32-S3 se alimenta desde el puerto OBD-II, inicializa firmware, CAN, BLE y almacenamiento local; la aplicación móvil se conecta al servicio \texttt{AutoSense OBD2}; y Firebase queda como soporte de identidad, perfiles e historial sincronizado. Cada bloque conserva una responsabilidad definida: el firmware interpreta el vehículo, la app presenta y coordina, y la nube resguarda información consultable.

La operación evita que el teléfono interprete bytes CAN crudos o decida qué comandos son seguros. El firmware aplica el perfil activo, ejecuta consultas OBD-II permitidas, normaliza las señales y entrega a la app datos listos para visualización, registro y análisis.

\subsection{Secuencia de operación}

La sesión comienza cuando el usuario conecta el adaptador al puerto OBD-II. En el arranque, el firmware prepara USB-CDC, LittleFS, configuración, perfil activo, logger binario, detector de anomalías y CAN. Una vez termina esa preparación, el dispositivo queda disponible para comandos BLE y para el ciclo de lectura.

Desde la aplicación móvil, el usuario inicia sesión, busca dispositivos compatibles y se conecta a \texttt{AutoSense OBD2}. La app consulta \path{GET_DEVICE_INFO}, intenta leer el VIN con \path{READ_VIN} y resuelve el perfil correspondiente. Para el vehículo validado, el perfil esperado es \path{vw_passat_2016}; si no existe coincidencia, se usa el perfil genérico OBD-II.

Cuando el perfil debe aplicarse o actualizarse, la app descarga el JSON, revisa su estructura y lo envía por BLE. El firmware lo acepta solo si cumple las validaciones de tamaño, hash, fórmulas y servicios permitidos. Después reconstruye el plan de sondeo. A partir de ese momento procesa respuestas CAN, actualiza la muestra compacta y entrega telemetría cuando la app solicita \texttt{START\_STREAM}.

\begin{table}[htbp]
\centering
\small
\caption{Resumen operativo de una sesión AutoSense. Elaboración propia.}
\label{tab:autosense-general-operation}
\begin{tabular}{p{0.24\textwidth}p{0.36\textwidth}p{0.31\textwidth}}
\toprule
\textbf{Etapa} & \textbf{Acción principal} & \textbf{Resultado esperado} \\
\midrule
Arranque del adaptador & Inicialización de USB-CDC, LittleFS, CAN, BLE, logger, perfil y análisis local. & Módulo embebido listo para lectura y conexión móvil. \\
\addlinespace
Conexión BLE & La app descubre \texttt{AutoSense OBD2} y abre el servicio GATT propio. & Canal de comandos y notificaciones disponible. \\
\addlinespace
Identificación vehicular & Consulta de información del dispositivo y lectura de VIN si está disponible. & Base para seleccionar o resolver el perfil del vehículo. \\
\addlinespace
Aplicación de perfil & Transferencia del perfil por fragmentos y validación en app y firmware. & Perfil activo guardado en LittleFS y plan de sondeo actualizado. \\
\addlinespace
Lectura OBD-II/CAN & Sondeo de PIDs y servicios de lectura permitidos por la política segura. & Muestras decodificadas y listas para visualización, registro y análisis. \\
\addlinespace
Análisis local & Clasificación de ruta, línea base, Z-score, Isolation Forest y debounce. & Puntaje, severidad y contexto de conducción interpretables. \\
\addlinespace
Visualización móvil & Recepción de telemetría por BLE y actualización de pantallas. & Datos en vivo, alertas, estado del vehículo y resumen de sesión. \\
\addlinespace
Persistencia y sincronización & Registro binario local en el ESP32-S3, descarga incremental desde la app y respaldo en Firebase Storage con índice en Firestore. & Evidencia técnica disponible para consulta posterior sin borrar el historial del adaptador. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Estados operativos del sistema}

AutoSense opera con estados diferenciados. En arranque, el adaptador prepara recursos internos y publica BLE. En conexión sin transmisión, la app puede consultar estado, revisar perfil o preparar una actualización sin recibir telemetría continua. En transferencia de perfil, se actualiza la configuración solo cuando el perfil local no coincide o cuando existe una versión requerida.

El estado principal es el monitoreo activo. El firmware trabaja con un ciclo no bloqueante: planifica consultas OBD-II, procesa respuestas CAN, registra muestras en \texttt{obdlog}, calcula indicadores de anomalía y atiende BLE o USB-CDC. Si no hay VIN, no existe perfil específico o algunas señales no responden, el sistema opera con alcance reducido y conserva solo valores confirmados dentro de la máscara de validez.

\subsection{Flujo de información durante una sesión}

El flujo de datos inicia con respuestas OBD-II recibidas por CAN. El firmware las convierte en magnitudes de ingeniería y las agrupa en una muestra compacta. De esa muestra salen tres rutas: telemetría BLE para visualización inmediata, registro binario local para historial y análisis de anomalías para contexto operativo.

La capa remota complementa el flujo local. Firestore conserva documentos de usuario, vehículo, estado, alertas, viajes, resúmenes e índices de exportación. Firebase Storage respalda perfiles y archivos binarios sincronizados desde el ESP32-S3. Con esta separación, el adaptador guarda la captura local, la app coordina la sesión y Firebase permite consulta posterior.

\subsection{Condiciones de cierre y continuidad}

El cierre de una sesión ocurre cuando el usuario detiene la transmisión, cierra la pantalla de tiempo real, pierde conexión BLE o retira el adaptador del vehículo. Si la desconexión ocurre desde la app, el cliente móvil intenta enviar \texttt{STOP\_STREAM} y liberar el enlace. Si la pérdida es física o inesperada, el firmware conserva su estado local mientras continúe alimentado, y la app limpia las solicitudes pendientes para evitar presentar una conexión falsa.

La continuidad depende de dos elementos: persistencia local y cursor de sincronización. El perfil activo queda guardado como \texttt{/active\_profile.json}. El historial binario permanece en el almacenamiento circular del ESP32-S3. La app descarga solo los registros posteriores al último respaldo conocido; no borra el historial del adaptador. Crea una copia local y, si corresponde, la sube a Firebase Storage.

AutoSense funciona como una cadena de observación no invasiva. Toma datos del vehículo, los valida, los convierte, los registra, los analiza y los presenta al usuario. El resultado es una plataforma para monitorear condiciones vehiculares, detectar desviaciones y conservar evidencia de operación.

\newpage

% =========================
% CAPÍTULO 5
% =========================
\chapter{Pruebas y resultados}

\section{Escenario de pruebas}

La validación de AutoSense se apoyó en tres ambientes antes de ejecutar las pruebas específicas: una prueba preliminar en banco para verificar alimentación y respuesta básica del adaptador, una campaña de recolección sobre un Volkswagen Passat 2016 con datos reales de conducción, y una prueba con simulador OBD-II para generar condiciones controladas sin depender siempre del vehículo físico.

La combinación de estos ambientes permitió cubrir dos necesidades distintas. Por un lado, el vehículo real permitió comprobar el flujo completo del sistema en condiciones de uso cotidiano, incluyendo conexión al puerto OBD-II, lectura mediante CAN, aplicación del perfil \path{vw_passat_2016}, registro de telemetría y clasificación de trayectos urbanos y de carretera. Por otro lado, el simulador OBD-II permitió modificar manualmente variables como velocidad, revoluciones, temperatura o carga, lo cual fue útil para observar respuestas del firmware y de la aplicación en un entorno repetible.

\subsection{Contexto general del escenario}

El escenario de pruebas inició con la verificación física del prototipo. En esta etapa se energizó el adaptador AutoSense, se comprobó el encendido del módulo ESP32-S3, la alimentación por USB-C y la disponibilidad inicial del firmware. Esta comprobación no sustituyó la prueba vehicular, pero permitió validar que el hardware ensamblado respondiera antes de conectarlo al automóvil o al simulador.

\begin{figure}[H]
\centering
\includegraphics[width=0.64\textwidth,height=0.48\textheight,keepaspectratio]{Figures/test_autosense_adapter_powered.jpg}
\caption{Adaptador AutoSense energizado durante la verificación preliminar en banco. Elaboración propia.}
\label{fig:test-adapter-powered}
\end{figure}

Después de esta comprobación inicial, las pruebas se dividieron en dos grupos principales. El primer grupo correspondió a pruebas reales sobre el Volkswagen Passat 2016, vehículo de referencia del proyecto. El segundo grupo usó un simulador OBD-II para variar parámetros de diagnóstico de forma controlada y validar el comportamiento del sistema con un perfil genérico.

\subsection{Prueba en vehículo real}

El escenario principal de validación fue un Volkswagen Passat 2016. En este vehículo se utilizó el perfil \path{vw_passat_2016}, porque corresponde al perfil verificado dentro del proyecto y permite recorrer el flujo completo de AutoSense: conexión al puerto OBD-II, lectura de datos por CAN, identificación del vehículo, aplicación del perfil, transmisión BLE hacia la aplicación móvil, registro de datos y posterior análisis de telemetría.

La campaña de recolección cubrió el periodo del 5 de mayo al 3 de julio de 2026. Durante ese periodo se consolidaron 86\,998 registros con una cadencia de 10 segundos, equivalentes a 241.7 horas efectivas de observación. Los datos incluyen conducción real en trayectos urbanos y de carretera. Esta distinción fue importante porque el comportamiento del vehículo no es igual en tráfico urbano, donde existen paradas frecuentes, aceleraciones cortas y variación de velocidad, que en carretera, donde predominan velocidades más sostenidas y condiciones de carga más estables.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth,height=0.58\textheight,keepaspectratio]{Figures/test_real_passat_2016.jpg}
\caption{Escenario de recolección real de datos en Volkswagen Passat 2016 con el adaptador AutoSense conectado al puerto OBD-II. Elaboración propia.}
\label{fig:test-real-passat-2016}
\end{figure}

En esta etapa no se trabajó con datos simulados. Las muestras provinieron del vehículo físico, por lo que el escenario permitió observar condiciones reales de comunicación, alimentación, estabilidad del enlace, disponibilidad de PIDs y variabilidad propia del manejo. El archivo de salida \path{vw_passat_2016_raw_route_classified_60d_10s.xlsx} conserva la evidencia clasificada de esta campaña, organizada por tipo de ruta y por segmentos de conducción.

\subsection{Prueba con simulador OBD-II}

El simulador OBD-II se utilizó como ambiente de apoyo para reproducir señales vehiculares sin depender del automóvil. Este equipo permite ajustar manualmente parámetros como velocidad del vehículo, revoluciones del motor, temperatura, posición del acelerador, voltaje y otros valores de diagnóstico. Con esa posibilidad se validó que el adaptador recibiera respuestas OBD-II, que la aplicación mostrara los cambios esperados y que el flujo de comunicación funcionara en condiciones controladas.

En este escenario se empleó el perfil genérico de OBD-II. A diferencia del Volkswagen Passat 2016, el simulador no representa todas las características particulares del vehículo real, pero sí permite comprobar el comportamiento base del sistema frente a PIDs comunes. Esto resultó conveniente para pruebas preliminares, depuración de comunicación y verificación rápida de pantallas antes de llevar el sistema al vehículo.

\begin{figure}[H]
\centering
\includegraphics[width=0.64\textwidth,height=0.58\textheight,keepaspectratio]{Figures/test_obd_simulator_generic.jpg}
\caption{Simulador OBD-II utilizado para variar parámetros vehiculares de forma controlada con perfil genérico. Elaboración propia.}
\label{fig:test-obd-simulator-generic}
\end{figure}

\subsection{Evidencia generada durante la campaña}

La evidencia principal de la campaña de manejo real quedó consolidada en el archivo Excel de salida del proyecto. Dicho archivo contiene la hoja de telemetría clasificada con 86,998 registros, además de hojas separadas para trayectos urbanos y de carretera. La hoja de resumen de rutas contiene 400 segmentos, lo que permite contrastar posteriormente el comportamiento del sistema por tipo de ruta.

La tabla \ref{tab:test-scenarios} resume los escenarios utilizados y su función dentro de la validación. Los resultados cuantitativos se discuten después, en las pruebas funcionales, de comunicación, lectura, almacenamiento, visualización y rendimiento.

\begin{table}[H]
\centering
\scriptsize
\caption{Escenarios utilizados para la validación de AutoSense. Elaboración propia.}
\label{tab:test-scenarios}
\begin{tabular}{@{}p{0.20\textwidth}p{0.18\textwidth}p{0.30\textwidth}p{0.20\textwidth}@{}}
\toprule
\textbf{Escenario} & \textbf{Perfil utilizado} & \textbf{Propósito} & \textbf{Evidencia} \\
\midrule
Banco preliminar & No aplica & Verificar alimentación, encendido del ESP32-S3 y respuesta física inicial del adaptador. & Fotografía del adaptador energizado. \\
\addlinespace
Volkswagen Passat 2016 & \path{vw_passat_2016} & Recolectar datos reales en ciudad y carretera con el vehículo de referencia del proyecto. & Excel clasificado y fotografía del montaje en vehículo. \\
\addlinespace
Simulador OBD-II & Perfil genérico OBD-II & Variar parámetros de diagnóstico en un entorno controlado para depuración y validación preliminar. & Fotografía del simulador y observación de respuestas OBD-II. \\
\bottomrule
\end{tabular}
\end{table}

\section{Pruebas funcionales}

Las pruebas funcionales verificaron que AutoSense cumpliera el flujo operativo previsto después del montaje físico y la preparación del entorno de prueba. A diferencia del escenario descrito en la sección anterior, este apartado no se enfoca en el lugar o en los equipos utilizados, sino en comprobar que cada función principal del sistema respondiera de forma coherente: arranque del adaptador, identificación del vehículo, selección de perfil, lectura de telemetría, registro local, visualización en la aplicación y restricción de comandos de solo lectura.

La validación se sustentó en evidencia generada por el propio proyecto. En el firmware se utilizaron los reportes de salida del dashboard USB-CDC, el archivo de métricas OBD-II y el registro exportado desde la partición de historial. En la aplicación y en el backend se verificó la resolución de perfiles mediante pruebas automáticas de la función de selección de perfil vehicular. El caso del Volkswagen Passat 2016 resolvió correctamente hacia \path{vw_passat_2016}, mientras que los casos no soportados fueron dirigidos al perfil genérico y el modo de simulador resolvió hacia \path{obd2_simulator}. La prueba automática ejecutada sobre el resolutor de perfiles reportó tres pruebas aprobadas, sin fallos.

El flujo funcional validado inicia cuando el ESP32-S3 arranca el firmware, habilita la salida USB-CDC y carga la configuración disponible en memoria local. Luego el sistema establece la comunicación con el bus OBD-II/CAN, consulta la información de identificación del vehículo y aplica el perfil correspondiente. Una vez activo el perfil, el firmware inicia el sondeo de señales, compacta las muestras en la estructura de telemetría, actualiza el dashboard, registra muestras históricas y expone los datos hacia la aplicación móvil por BLE. Esta secuencia permitió comprobar que los módulos desarrollados no funcionan como piezas aisladas, sino como una cadena integrada de adquisición, procesamiento y visualización.

También se verificó la política de seguridad funcional del adaptador. En los reportes de dashboard aparece el indicador \path{guard=0}, lo que indica que no se registraron intentos bloqueados durante la sesión validada. El sistema está diseñado para operar como lector de datos, no como herramienta de escritura, reprogramación o control activo del vehículo. Por tanto, la prueba funcional no buscó alterar el estado de la ECU, borrar códigos ni ejecutar rutinas de servicio; su objetivo fue confirmar que AutoSense registra información sin modificar parámetros del vehículo.

\begin{table}[H]
\centering
\scriptsize
\caption{Pruebas funcionales aplicadas a AutoSense. Elaboración propia.}
\label{tab:functional-tests}
\begin{tabular}{@{}p{0.18\textwidth}p{0.24\textwidth}p{0.25\textwidth}p{0.11\textwidth}@{}}
\toprule
\textbf{Función} & \textbf{Criterio de validación} & \textbf{Evidencia utilizada} & \textbf{Resultado} \\
\midrule
Resolución de perfil & El vehículo validado debe cargar perfil específico y los casos no soportados deben usar perfil genérico. & Prueba automática del resolutor con tres casos aprobados. & Cumple. \\
\addlinespace
Arranque del adaptador & El firmware debe iniciar con USB-CDC, CAN activo y consulta OBD-II habilitada. & Dashboard con enlace USB-CDC, CAN activo y consulta activa. & Cumple. \\
\addlinespace
Telemetría en vivo & Las señales principales deben actualizarse con valores decodificados y edad reciente. & Métricas OBD-II con RPM, velocidad, refrigerante, MAP, acelerador y voltaje ECU. & Cumple. \\
\addlinespace
Registro local & El historial debe incrementar secuencia y contador sin errores de escritura. & Dashboard con logger activo, contador de registros y exportación CSV. & Cumple. \\
\addlinespace
Clasificación de ruta & La campaña debe conservar etiquetas de ciudad, carretera y registros no clasificados. & Excel de salida con telemetría y resumen de rutas. & Cumple. \\
\addlinespace
Solo lectura & El sistema no debe ejecutar escritura, borrado ni control sobre el vehículo. & Indicador de guardia sin bloqueos y modos de consulta. & Cumple. \\
\bottomrule
\end{tabular}
\end{table}

\section{Pruebas de comunicación}

Las pruebas de comunicación verificaron los canales que permiten a AutoSense transportar datos desde el vehículo hasta los puntos de observación del sistema. Se revisaron tres rutas principales: la comunicación CAN con el vehículo, la salida USB-CDC hacia el computador y la comunicación BLE hacia la aplicación móvil. La evaluación se limitó al intercambio de datos y comandos de consulta; la interpretación detallada de PIDs y modos OBD-II se desarrolla en la sección siguiente para evitar repetir contenido.

El primer canal verificado fue USB-CDC, utilizado como consola técnica y dashboard local del adaptador. Esta salida permitió observar en tiempo real el estado del enlace, el número de respuestas por segundo, la cantidad de señales decodificadas, el estado del logger y la identidad del perfil aplicado. En los reportes se observó \path{link=USB-CDC}, lo que confirma que el ESP32-S3 fue reconocido como puerto serial y entregó información estructurada de diagnóstico durante la sesión. Este canal fue especialmente útil para contrastar el comportamiento del firmware sin depender únicamente de la aplicación móvil.

El segundo canal fue la comunicación CAN sobre el conector OBD-II. La evidencia muestra \path{can=UP}, ruta \path{7DF} para consultas funcionales y tasas de respuesta y decodificación mayores que cero. Durante la captura de dashboard se observaron valores como \path{rsp/s=33} y \path{dec/s=33}, con variaciones normales entre muestras sucesivas. Estos indicadores no representan un resultado de rendimiento definitivo, pero sí demuestran que el adaptador logró enviar solicitudes de consulta, recibir respuestas y convertirlas en señales útiles durante la sesión de prueba.

El tercer canal fue BLE, usado por la aplicación móvil para descubrir el adaptador, consultar información del dispositivo, solicitar el VIN y activar el flujo de telemetría. La validación se hizo a nivel funcional mediante el flujo de comandos esperado: \path{GET_DEVICE_INFO}, \path{READ_VIN} y \path{START_STREAM}. En esta etapa no se utilizó un analizador de paquetes BLE externo; por tanto, la prueba se redacta como verificación de operación integrada entre app y firmware. La evidencia principal fue la disponibilidad de telemetría y estados en la aplicación después del emparejamiento con el adaptador.

\begin{table}[H]
\centering
\scriptsize
\caption{Indicadores de comunicación verificados durante las pruebas. Elaboración propia.}
\label{tab:communication-tests}
\begin{tabular}{@{}p{0.16\textwidth}p{0.25\textwidth}p{0.27\textwidth}p{0.10\textwidth}@{}}
\toprule
\textbf{Canal} & \textbf{Propósito} & \textbf{Indicador observado} & \textbf{Estado} \\
\midrule
USB-CDC & Monitorear el firmware desde computador y obtener dashboard local. & Enlace USB-CDC, dashboard de una pantalla y actualización periódica. & Activo. \\
\addlinespace
CAN / OBD-II & Transportar solicitudes y respuestas entre adaptador y ECU. & CAN activo, ruta funcional 7DF y tasas de respuesta y decodificación mayores que cero. & Activo. \\
\addlinespace
BLE & Conectar la app móvil con el adaptador y transmitir telemetría. & Flujo de información del dispositivo, lectura de VIN e inicio de stream. & Activo. \\
\addlinespace
Perfil remoto & Resolver y aplicar el perfil vehicular usado por app y firmware. & Prueba automática con Passat 2016, perfil genérico y simulador. & Activo. \\
\addlinespace
Guardia de lectura & Evitar comandos no permitidos durante la comunicación con el vehículo. & Dashboard con guardia sin bloqueos durante la captura validada. & Activo. \\
\bottomrule
\end{tabular}
\end{table}

La prueba confirmó que los canales necesarios para la validación inicial funcionaron de manera coordinada. USB-CDC permitió supervisión técnica, CAN permitió acceso a la red de diagnóstico, BLE permitió interacción con la aplicación móvil y la resolución de perfil permitió seleccionar la configuración adecuada para el vehículo o para el simulador. Las mediciones de latencia, estabilidad prolongada y rendimiento sostenido se reservan para los apartados de rendimiento y análisis de resultados.

\section{Pruebas de lectura OBD-II}

Las pruebas de lectura OBD-II comprobaron que el adaptador pudiera consultar información estándar del vehículo sin ejecutar acciones de escritura. La validación se concentró en modos de diagnóstico de consulta: Mode 01 para datos actuales, Mode 03 para códigos almacenados, Mode 07 para códigos pendientes, Mode 09 para información de identificación y Mode 0A para códigos permanentes. En el caso del Volkswagen Passat 2016, estas lecturas fueron realizadas con el perfil específico del proyecto y contrastadas con los reportes exportados por el firmware.

En Mode 01 se validó la lectura de variables en tiempo real. El reporte de métricas incluyó carga calculada del motor, temperatura del refrigerante, presión absoluta del múltiple, revoluciones por minuto, velocidad, posición del acelerador y voltaje reportado por la ECU. Estas señales son relevantes porque forman la base de la visualización móvil, el registro histórico y el análisis local de comportamiento. La prueba no consistió solamente en recibir bytes crudos, sino en confirmar que el firmware aplicó las fórmulas correspondientes y presentó magnitudes interpretables con unidades cuando correspondía.

\begin{table}[H]
\centering
\scriptsize
\caption{Lecturas Mode 01 observadas en reportes OBD-II de prueba. Elaboración propia con base en reportes del firmware.}
\label{tab:obd-pid-tests}
\begin{tabular}{@{}p{0.08\textwidth}p{0.27\textwidth}p{0.14\textwidth}p{0.11\textwidth}p{0.10\textwidth}@{}}
\toprule
\textbf{PID} & \textbf{Magnitud} & \textbf{Valor observado} & \textbf{Unidad} & \textbf{Estado} \\
\midrule
04 & Carga calculada del motor & 21.1 & \% & Leído. \\
05 & Temperatura del refrigerante & 91 & $^\circ$C & Leído. \\
0B & Presión MAP & 30 & kPa & Leído. \\
0C & Revoluciones del motor & 690.5 & rpm & Leído. \\
0D & Velocidad del vehículo & 0 & km/h & Leído. \\
11 & Posición del acelerador & 13.3 & \% & Leído. \\
42 & Voltaje ECU & 13.900 & V & Leído. \\
\bottomrule
\end{tabular}
\end{table}

Además de la captura principal, reportes complementarios del proyecto registraron otras señales disponibles en sesiones de prueba, como ajuste de combustible de corto plazo, ajuste de combustible de largo plazo, temperatura de aire de admisión y tiempo de funcionamiento del motor. Estas lecturas confirman que el conjunto de PIDs disponible puede variar según el estado del vehículo, el perfil activo y la respuesta de la ECU. Por esa razón, AutoSense no asume un catálogo fijo de parámetros; primero descubre soporte y después consulta las señales permitidas por el perfil.

En los modos de diagnóstico de códigos de falla, la captura final reportó ausencia de códigos estándar OBD-II almacenados, pendientes y permanentes. Esto no debe interpretarse como una certificación completa del estado mecánico del vehículo, sino como el resultado observado en los modos estándar consultados durante esa sesión. El mismo reporte también mostró lecturas UDS de solo consulta para algunos módulos Volkswagen/Audi, por lo que la tesis distingue entre el diagnóstico OBD-II estándar y la consulta adicional de módulos específicos cuando el perfil lo permite.

Mode 09 permitió validar la identificación técnica de módulos. En el reporte se observaron identificadores de calibración y CVN para el módulo de control del motor y el módulo de transmisión. Para proteger datos sensibles, la tesis no expone el VIN completo; solo se documenta que el sistema fue capaz de leer información de identificación y usarla para resolver el perfil correspondiente. Esta decisión mantiene trazabilidad técnica sin publicar identificadores completos del vehículo de prueba.

\begin{table}[H]
\centering
\scriptsize
\caption{Modos OBD-II verificados durante las pruebas de lectura. Elaboración propia con base en reportes del firmware.}
\label{tab:obd-mode-tests}
\begin{tabular}{@{}p{0.10\textwidth}p{0.27\textwidth}p{0.29\textwidth}p{0.10\textwidth}@{}}
\toprule
\textbf{Modo} & \textbf{Uso en la prueba} & \textbf{Evidencia observada} & \textbf{Resultado} \\
\midrule
01 & Lectura de datos actuales del tren motriz. & RPM, velocidad, refrigerante, MAP, carga, acelerador y voltaje ECU. & Cumple. \\
\addlinespace
03 & Consulta de DTC almacenados. & Reporte estándar sin códigos almacenados. & Cumple. \\
\addlinespace
07 & Consulta de DTC pendientes. & Reporte estándar sin códigos pendientes. & Cumple. \\
\addlinespace
09 & Identificación de vehículo y módulos. & CALID y CVN observados para ECM y TCM; VIN leído sin publicarlo completo. & Cumple. \\
\addlinespace
0A & Consulta de DTC permanentes. & Reporte estándar sin códigos permanentes. & Cumple. \\
\bottomrule
\end{tabular}
\end{table}

Con estas pruebas se comprobó que el adaptador realiza la lectura OBD-II necesaria para alimentar las demás capas del sistema. El resultado esperado no era diagnosticar una falla mecánica exacta, sino demostrar que AutoSense puede consultar modos estándar, decodificar PIDs relevantes, registrar evidencia y mantener una política de lectura segura durante la comunicación con el vehículo.

\section{Pruebas de almacenamiento}

Las pruebas de almacenamiento se enfocaron en verificar que AutoSense conservara evidencia de la sesión sin depender únicamente de la visualización en tiempo real. El punto principal de la prueba fue el registro local del ESP32-S3, debido a que este módulo puede permanecer conectado al vehículo aunque el teléfono no esté activo todo el tiempo. Por esa razón, la validación se centró en confirmar que el firmware mantuviera el logger encendido, incrementara el contador de muestras y permitiera exportar registros para análisis posterior.

Los reportes del dashboard USB-CDC mostraron el estado \path{log=ON}, con secuencia activa, contador de registros y capacidad total de almacenamiento. En la captura del vehículo real se registró \path{rec=1542/274720}, intervalo de escritura de 10 segundos y \path{err=0}. En el escenario con simulador también apareció el logger activo, con \path{rec=1707/274720}. Estos indicadores permiten establecer que la escritura local se encontraba habilitada, que el contador avanzaba y que no se reportaron errores de almacenamiento durante las sesiones observadas.

La exportación \path{obdlog.csv} permitió revisar el formato de salida del historial. El archivo contiene campos como \path{sequence}, \path{uptime_seconds}, \path{valid_mask}, \path{rpm}, \path{speed_kph}, \path{coolant_c}, \path{throttle_pct}, \path{fuel_level_pct}, \path{engine_load_pct}, \path{map_kpa}, \path{maf_gps} y \path{ecu_voltage_v}. La presencia de \path{valid_mask} es relevante porque no todas las señales están disponibles en cada muestra; por tanto, el registro conserva cuáles magnitudes fueron válidas y evita interpretar espacios vacíos como mediciones reales.

En la capa móvil se verificó que la aplicación dispone de persistencia de sesión y mecanismos para conservar estado de viaje, perfil activo y datos asociados al usuario. Además, el repositorio incorpora el flujo de sincronización histórica del \texttt{obdlog}: al conectarse por BLE, la app consulta el último contador respaldado, descarga solo los registros nuevos, guarda un archivo binario local y lo sube a Firebase Storage cuando el modo de datos permite sincronización en la nube. Firestore mantiene el índice de exportación y el cursor de secuencia, de modo que una reconexión posterior no repite registros ya sincronizados. Esta etapa queda documentada como validación de integración de software y reglas de acceso; para considerarla validación operacional completa se requiere adjuntar el archivo \texttt{.bin}, el objeto creado en Storage y el documento de índice generado por Firestore durante una sesión de exportación.

\begin{table}[H]
\centering
\scriptsize
\caption{Validación del almacenamiento de datos en AutoSense. Elaboración propia.}
\label{tab:storage-tests}
\begin{tabular}{@{}p{0.17\textwidth}p{0.26\textwidth}p{0.30\textwidth}p{0.11\textwidth}@{}}
\toprule
\textbf{Nivel} & \textbf{Criterio} & \textbf{Evidencia} & \textbf{Resultado} \\
\midrule
ESP32-S3 & Logger local activo y sin errores de escritura. & Dashboard con logger encendido, contador de registros, intervalo de 10 s y error cero. & Cumple. \\
\addlinespace
Historial exportado & Formato legible para contraste posterior. & Archivo \path{obdlog.csv} con secuencia, uptime, máscara de validez y señales OBD-II. & Cumple. \\
\addlinespace
Capacidad local & Espacio suficiente para campaña prolongada antes de sobrescritura circular. & Capacidad reportada de 274720 registros, equivalente a cerca de 31.8 días a 10 s por muestra. & Cumple. \\
\addlinespace
Aplicación móvil & Persistencia de sesión, viaje activo y perfil de vehículo. & Uso de SecureStore, caché de perfil y flujo de viaje en la app. & Verificado. \\
\addlinespace
Firebase & Respaldo histórico en nube. & Código de exportación BLE incremental, reglas de Storage por usuario y cursor en Firestore. & Integrado; evidencia operacional pendiente. \\
\bottomrule
\end{tabular}
\end{table}

\section{Pruebas de visualización}

Las pruebas de visualización verificaron que la información capturada por AutoSense pudiera presentarse de forma comprensible en la aplicación móvil. Esta validación no consistió en volver a documentar el diseño de interfaz desarrollado en el capítulo 4, sino en comprobar que las pantallas principales estuvieran alineadas con el flujo funcional del sistema: acceso del usuario, vista inicial, telemetría en vivo, alertas, perfil, vehículo y pantallas de detalle.

La evidencia visual se apoyó en las capturas ya generadas de la aplicación móvil. Las pantallas de acceso permitieron verificar que el usuario puede iniciar sesión, registrarse y recuperar cuenta. Las pantallas principales mostraron la navegación hacia inicio, eficiencia, alertas, perfil y vehículo. La pantalla de emparejamiento y la vista de telemetría en vivo permitieron validar el flujo esperado de conexión con el adaptador, incluyendo la activación del stream BLE mediante el comando \path{START_STREAM}.

En las pantallas de alertas se verificó que la aplicación no agrupe toda la información en una sola vista, sino que distribuya la inspección por categorías. Las vistas de batería, frenos, temperatura, llantas y consumo presentan al usuario áreas específicas de revisión. Esa organización responde al alcance de AutoSense: no entregar un diagnóstico mecánico exacto, sino mostrar señales, estados y advertencias de forma ordenada para facilitar el seguimiento del vehículo.

La validación también consideró la consistencia entre los datos técnicos y la experiencia de uso. El dashboard USB-CDC y las métricas OBD-II demuestran que existen valores decodificados, mientras que las pantallas móviles evidencian que esos datos pueden representarse en una interfaz de usuario. En esta etapa no se midió usabilidad con usuarios externos ni tiempos de interacción; la prueba se limitó a confirmar disponibilidad visual, navegación y correspondencia general entre módulos del sistema.

\begin{table}[H]
\centering
\scriptsize
\caption{Validación visual de pantallas móviles. Elaboración propia con base en capturas del proyecto.}
\label{tab:visual-tests}
\begin{tabular}{@{}p{0.18\textwidth}p{0.28\textwidth}p{0.28\textwidth}p{0.10\textwidth}@{}}
\toprule
\textbf{Pantalla} & \textbf{Información validada} & \textbf{Evidencia} & \textbf{Resultado} \\
\midrule
Acceso & Inicio de sesión, registro y recuperación. & Capturas de login, registro y recuperación. & Cumple. \\
\addlinespace
Inicio & Resumen del usuario, vehículo y acceso a módulos. & Captura de dashboard móvil. & Cumple. \\
\addlinespace
Tiempo real & Emparejamiento OBD-II y telemetría en vivo. & Capturas de emparejamiento y vista live; flujo \path{START_STREAM}. & Cumple. \\
\addlinespace
Alertas & Presentación de estados por categoría. & Capturas de alertas, batería, frenos, temperatura, llantas y consumo. & Cumple. \\
\addlinespace
Perfil y vehículo & Datos personales, vehículo asociado y configuración. & Capturas de perfil y vehículo. & Cumple. \\
\bottomrule
\end{tabular}
\end{table}

\section{Pruebas de rendimiento}

Las pruebas de rendimiento se orientaron a observar la respuesta del sistema durante sesiones de adquisición, comunicación y registro. No se plantearon como un benchmark completo del ESP32-S3 ni como una medición de latencia certificada, sino como una validación práctica de que AutoSense puede consultar datos OBD-II, decodificar respuestas, mantener comunicación con la aplicación y registrar información sin detener el flujo principal.

En el escenario del Volkswagen Passat 2016, el dashboard reportó comunicación CAN activa y tasas aproximadas de 33 a 35 respuestas por segundo, con una cantidad similar de señales decodificadas por segundo. También se observaron 20 consultas clave por segundo y entre 2 y 3 consultas de fondo por segundo. Estos valores muestran que el firmware mantuvo un ciclo de lectura activo durante la sesión validada, sin registrar bloqueos de la guardia de solo lectura.

En el simulador OBD-II se observó el enlace \path{USB-CDC+BLE}, CAN activo y tasas cercanas a 28 a 31 respuestas por segundo. En ese escenario, las señales presentaron frecuencias individuales alrededor de 1.8 a 2.8 Hz para PIDs como carga, refrigerante, MAP, RPM, velocidad, avance de ignición, temperatura de admisión, MAF, acelerador y combustible. Esto permitió comprobar el comportamiento del sistema cuando existe transmisión simultánea hacia el dashboard y hacia la aplicación móvil.

El rendimiento del registro local se evaluó mediante el intervalo de escritura y la capacidad reportada por el firmware. Con un intervalo de 10 segundos por muestra y una capacidad de 274720 registros, el almacenamiento circular ofrece aproximadamente 31.8 días de historial antes de sobrescribir datos antiguos. Esta capacidad es suficiente para respaldar sesiones prolongadas de recolección, aunque no elimina la necesidad de sincronizar o exportar información cuando se requiera conservar campañas completas.

\begin{table}[H]
\centering
\scriptsize
\caption{Indicadores de rendimiento observados durante las pruebas. Elaboración propia con base en reportes del firmware.}
\label{tab:performance-tests}
\begin{tabular}{@{}p{0.24\textwidth}p{0.20\textwidth}p{0.28\textwidth}p{0.12\textwidth}@{}}
\toprule
\textbf{Indicador} & \textbf{Valor observado} & \textbf{Evidencia} & \textbf{Lectura} \\
\midrule
Respuestas OBD-II en vehículo real & 33 a 35 rsp/s & Dashboard del Passat 2016 con CAN activo. & Estable. \\
\addlinespace
Decodificación en vehículo real & 33 a 35 dec/s & Dashboard con señales OBD-II decodificadas. & Estable. \\
\addlinespace
Consultas clave y de fondo & 20 key/s y 2 a 3 bg/s & Reporte USB-CDC del firmware. & Adecuado. \\
\addlinespace
Simulador con BLE activo & 28 a 31 rsp/s & Dashboard con \path{USB-CDC+BLE}. & Adecuado. \\
\addlinespace
Frecuencia por PID en simulador & 1.8 a 2.8 Hz & Métricas del simulador después de aplicar perfil. & Adecuado. \\
\addlinespace
Registro local & 10 s por muestra & Dashboard con logger activo y error cero. & Adecuado. \\
\addlinespace
Capacidad de historial & 274720 registros & Contador de capacidad del firmware. & Suficiente. \\
\addlinespace
Latencia extremo a extremo & No medida con instrumento dedicado & Pruebas funcionales sin cronometraje certificado. & Pendiente. \\
\addlinespace
Tiempo de sincronización histórica & No medido en campaña completa & Flujo implementado en app, firmware y reglas Firebase; falta medición con exportación completa documentada. & Pendiente. \\
\bottomrule
\end{tabular}
\end{table}

Las pruebas indican que el prototipo mantuvo tasas de consulta y decodificación suficientes para la visualización y el registro planteados en esta etapa. La prueba no pretende establecer el límite máximo del sistema, sino confirmar que, bajo los escenarios utilizados, el flujo de adquisición, transmisión BLE, dashboard USB-CDC y almacenamiento local se mantuvo operativo. Las métricas de latencia extremo a extremo, pérdida de paquetes BLE y duración de sincronización histórica quedan identificadas como mediciones pendientes para una validación de rendimiento más estricta.

\section{Resultados obtenidos}

Los resultados se basan en la telemetría recolectada con AutoSense y consolidada en el archivo \path{vw_passat_2016_raw_route_classified_60d_10s.xlsx}. Para el análisis gráfico se seleccionó un segmento urbano de veinte minutos, exportado desde la hoja de datos procesada hacia archivos CSV individuales y visualizado mediante Datawrapper. Los gráficos muestran el comportamiento de variables representativas durante una sesión real de conducción; no constituyen un diagnóstico mecánico definitivo.

El segmento utilizado contiene 775 muestras válidas por variable después de separar los registros auxiliares usados para visualización. Las métricas seleccionadas cubren señales de seguimiento operativo: velocidad, revoluciones del motor, temperatura del refrigerante, carga del motor, consumo instantáneo y una vista normalizada mediante Z-score.

\begin{table}[H]
\centering
\scriptsize
\caption{Resumen estadístico del segmento urbano utilizado en los gráficos de resultados. Elaboración propia con base en el Excel de salida.}
\label{tab:resultados-datawrapper-resumen}
\begin{tabular}{@{}p{0.30\textwidth}p{0.18\textwidth}p{0.18\textwidth}p{0.20\textwidth}@{}}
\toprule
\textbf{Variable} & \textbf{Media} & \textbf{Máximo} & \textbf{Rango observado} \\
\midrule
Velocidad del vehículo & 28,47 km/h & 109,33 km/h & 0,00 a 109,33 km/h \\
\addlinespace
Revoluciones del motor & 2165,92 rpm & 3405,00 rpm & 690,00 a 3405,00 rpm \\
\addlinespace
Temperatura del refrigerante & 89,74 $^\circ$C & 95,07 $^\circ$C & 37,17 a 95,07 $^\circ$C \\
\addlinespace
Carga del motor & 34,09\% & 60,55\% & 9,80 a 60,55\% \\
\addlinespace
Consumo instantáneo & 4,02 L/h & 11,69 L/h & 0,53 a 11,69 L/h \\
\bottomrule
\end{tabular}
\end{table}

La campaña completa también fue clasificada por contexto de ruta. AutoSense no se evaluó con una muestra aislada: el archivo contiene registros de ciudad, carretera y segmentos conservadores no determinados.

\begin{table}[H]
\centering
\scriptsize
\caption{Resumen por contexto de ruta en la campaña completa. Elaboración propia.}
\label{tab:route-context-summary}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
\textbf{Contexto} & \textbf{Registros} & \textbf{Vel. media} & \textbf{RPM media} & \textbf{Temp. media} & \textbf{Carga media} & \textbf{Consumo medio} \\
 & & \textbf{(km/h)} & & \textbf{($^\circ$C)} & \textbf{(\%)} & \textbf{(L/h)} \\
\midrule
\texttt{city} & 71\,114 & 28,81 & 2225,31 & 91,58 & 33,50 & 4,08 \\
\texttt{highway} & 8521 & 95,13 & 2086,38 & 94,17 & 34,31 & 3,98 \\
\texttt{unknown} & 7363 & 39,55 & 2262,08 & 93,97 & 31,99 & 3,91 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comportamiento de la velocidad}

La Figura~\ref{fig:resultados-speed} muestra la variación de la velocidad durante el segmento urbano analizado. El comportamiento presenta alternancia entre periodos de baja velocidad, detenciones y aumentos puntuales, lo cual es consistente con una ruta de ciudad donde el vehículo no mantiene una velocidad constante durante todo el intervalo. La media de 28,47 km/h confirma que el tramo corresponde a un contexto de circulación urbana, aunque existen picos de mayor velocidad que reflejan momentos de aceleración o tramos con menor congestión.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth,height=0.54\textheight,keepaspectratio]{Figures/datawrapper_speed_kmh.png}
\caption{Velocidad del vehículo en el segmento urbano analizado. Gráfico generado en Datawrapper con datos del Excel de salida. Elaboración propia.}
\label{fig:resultados-speed}
\end{figure}

Este resultado comprueba que el sistema registra cambios dinámicos de conducción, no solo valores estáticos. La velocidad ayuda a diferenciar el contexto de uso del vehículo, especialmente al comparar tráfico urbano con trayectos de carretera.

\subsection{Comportamiento de las revoluciones del motor}

La Figura~\ref{fig:resultados-rpm} presenta las revoluciones por minuto del motor. La señal muestra variaciones asociadas a los cambios de aceleración, desaceleración y demanda de potencia durante el manejo. El valor medio de 2165,92 rpm y el máximo de 3405 rpm indican que el motor trabajó dentro de un rango operativo normal para una sesión de conducción, con incrementos puntuales relacionados con la respuesta del tren motriz ante cambios de velocidad o carga.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth,height=0.54\textheight,keepaspectratio]{Figures/datawrapper_rpm.png}
\caption{Revoluciones del motor en el segmento urbano analizado. Gráfico generado en Datawrapper con datos del Excel de salida. Elaboración propia.}
\label{fig:resultados-rpm}
\end{figure}

La lectura de RPM confirma que el módulo AutoSense pudo capturar una señal de alta utilidad para el análisis operativo del vehículo. En conjunto con la velocidad, esta variable permite observar si los cambios de régimen del motor guardan relación con la dinámica real de conducción.

\subsection{Temperatura del refrigerante}

La Figura~\ref{fig:resultados-coolant} muestra la temperatura del refrigerante. La señal presenta una etapa inicial de incremento térmico hasta alcanzar un rango estable cercano al promedio de 89,74 $^\circ$C. Este comportamiento es coherente con el calentamiento progresivo del motor y su posterior operación dentro de una zona de temperatura relativamente estable durante la conducción.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth,height=0.54\textheight,keepaspectratio]{Figures/datawrapper_coolant_c.png}
\caption{Temperatura del refrigerante en el segmento urbano analizado. Gráfico generado en Datawrapper con datos del Excel de salida. Elaboración propia.}
\label{fig:resultados-coolant}
\end{figure}

La lectura térmica muestra que AutoSense registra variables de evolución lenta, no solo señales instantáneas como velocidad o RPM. Una temperatura estabilizada facilita comparar el comportamiento observado contra la línea base del vehículo.

\subsection{Carga del motor}

La Figura~\ref{fig:resultados-engine-load} presenta la carga calculada del motor. La señal fluctúa entre 9,80\% y 60,55\%, con una media de 34,09\%. Estas variaciones reflejan cambios en la demanda del motor durante aceleraciones, reducciones de velocidad y condiciones de desplazamiento urbano. A diferencia de la temperatura, la carga del motor responde de forma más inmediata al comportamiento de conducción.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth,height=0.54\textheight,keepaspectratio]{Figures/datawrapper_engine_load_pct.png}
\caption{Carga del motor en el segmento urbano analizado. Gráfico generado en Datawrapper con datos del Excel de salida. Elaboración propia.}
\label{fig:resultados-engine-load}
\end{figure}

El registro de esta variable aporta contexto al análisis de consumo y al análisis de anomalías, debido a que incrementos de carga pueden explicar cambios simultáneos en otras señales, como caudal de aire, acelerador o consumo instantáneo.

\subsection{Consumo instantáneo}

La Figura~\ref{fig:resultados-fuel-rate} muestra el consumo instantáneo estimado en litros por hora. El valor medio observado fue de 4,02 L/h, con un máximo de 11,69 L/h. La variación de esta señal guarda relación con los cambios de carga y aceleración del vehículo, por lo que resulta útil para evaluar el comportamiento energético durante el trayecto.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth,height=0.54\textheight,keepaspectratio]{Figures/datawrapper_fuel_rate_lph.png}
\caption{Consumo instantáneo en el segmento urbano analizado. Gráfico generado en Datawrapper con datos del Excel de salida. Elaboración propia.}
\label{fig:resultados-fuel-rate}
\end{figure}

El consumo no se mantiene constante; cambia con la operación del vehículo. En AutoSense, esta variable funciona como indicador de eficiencia y como señal complementaria para interpretar condiciones de conducción.

\subsection{Comparación normalizada mediante Z-score}

La Figura~\ref{fig:resultados-zscore} presenta una comparación normalizada de las variables seleccionadas mediante puntajes Z-score. Esta representación muestra desviaciones relativas entre señales que originalmente tienen unidades distintas. Por ejemplo, velocidad, RPM, temperatura, carga y consumo no pueden compararse directamente por escala, pero su normalización permite identificar momentos donde una o varias señales se alejan de su comportamiento medio dentro del segmento analizado.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth,height=0.56\textheight,keepaspectratio]{Figures/datawrapper_zscore_overlay.png}
\caption{Comparación normalizada mediante Z-score para variables seleccionadas. Gráfico generado en Datawrapper con datos del Excel de salida. Elaboración propia.}
\label{fig:resultados-zscore}
\end{figure}

Esta visualización apoya la interpretación del módulo de análisis de datos. Cuando una señal se aparta de su comportamiento esperado, el sistema puede usar esa desviación como insumo para alertas o revisión posterior. El resultado debe entenderse como detección de variaciones y condiciones anómalas en los datos, no como predicción exacta de fallas mecánicas.

Los gráficos muestran que AutoSense capturó, almacenó y procesó señales suficientes para describir el comportamiento operativo del Volkswagen Passat 2016 durante una sesión urbana real. Las variables son coherentes entre sí: los cambios de velocidad se relacionan con variaciones de RPM, carga y consumo, mientras que la temperatura del refrigerante evoluciona con mayor lentitud hasta estabilizarse. Estos resultados respaldan el funcionamiento del sistema como herramienta de monitoreo vehicular basada en datos OBD-II/CAN.

\section{Análisis de resultados}

\subsection{Interpretación técnica de las señales}

El segmento urbano de las Figuras~\ref{fig:resultados-speed} a~\ref{fig:resultados-fuel-rate} corresponde a una conducción real en ciudad. La velocidad media de 28,47 km/h no explica por sí sola el comportamiento del vehículo; debe leerse junto con RPM, carga del motor y consumo instantáneo. Cuando la velocidad aumenta o cambia con frecuencia, las revoluciones y la carga reflejan la respuesta del motor ante aceleraciones, reducciones de velocidad y transiciones del tráfico urbano. Las señales capturadas por AutoSense no son lecturas sueltas: forman un conjunto coherente dentro de una sesión de manejo.

La temperatura del refrigerante se comporta de otra manera. Mientras velocidad, RPM, carga y consumo reaccionan rápido ante el estilo de conducción, la temperatura cambia con más lentitud y tiende a estabilizarse cerca de 89,74 $^\circ$C. Esa diferencia afecta el análisis predictivo: no todas las variables deben evaluarse con la misma sensibilidad temporal. Una variación rápida en RPM puede ser normal durante una aceleración; una desviación térmica sostenida, en cambio, requiere seguimiento. Por eso el historial resulta necesario para separar cambios transitorios de comportamientos persistentes.

El consumo instantáneo promedio de 4,02 L/h y la carga media de 34,09\% dependen del contexto. En ciudad, los cambios de acelerador, detenciones y arranques producen oscilaciones que no necesariamente indican anomalía. Al relacionar consumo, carga, velocidad y RPM, el sistema obtiene una lectura más completa del estado operativo. Esta lectura multivariable reduce el riesgo de marcar como anormal una variación que responde al patrón habitual de conducción.

\subsection{Lectura de la campaña de recolección}

La campaña completa aporta evidencia operativa: el archivo clasificado contiene 86 998 registros distribuidos en 71 114 urbanos, 8521 de carretera y 7363 no determinados. La presencia de datos \path{city} y \path{highway} muestra que el sistema se expuso a condiciones distintas. Un sistema de monitoreo vehicular no puede evaluarse solo con vehículo detenido o simulador OBD-II; necesita trayectos reales, donde cambian velocidad, carga, temperatura y consumo.

El grupo \path{unknown} no debe interpretarse como una falla directa del sistema, sino como una decisión conservadora del clasificador de ruta. En una arquitectura de monitoreo, resulta preferible marcar un segmento como no determinado cuando la evidencia no es suficiente, antes que forzar una clasificación incorrecta. Esta estrategia mantiene la confiabilidad del análisis posterior, especialmente si los datos se utilizan para comparar comportamientos urbanos y de carretera o para entrenar una línea base por contexto de conducción.

\begin{table}[H]
\centering
\scriptsize
\caption{Discusión técnica de los hallazgos principales. Elaboración propia.}
\label{tab:analisis-resultados}
\begin{tabular}{@{}p{0.26\textwidth}p{0.34\textwidth}p{0.30\textwidth}@{}}
\toprule
\textbf{Hallazgo} & \textbf{Interpretación técnica} & \textbf{Implicación para AutoSense} \\
\midrule
Patrón urbano en velocidad y RPM & Las variaciones de velocidad se acompañan de cambios en el régimen del motor y en la carga. & El sistema captura relaciones coherentes entre señales, no solo valores aislados. \\
\addlinespace
Temperatura con evolución gradual & El refrigerante se estabiliza cerca de 89,74 $^\circ$C, con dinámica más lenta que las variables de conducción. & La línea base debe considerar el tipo de señal y su velocidad natural de cambio. \\
\addlinespace
Consumo asociado a carga y aceleración & El consumo medio de 4,02 L/h se explica mejor al observarlo junto con carga, RPM y velocidad. & El análisis multivariable aporta mayor contexto que una lectura individual. \\
\addlinespace
Cobertura de ciudad y carretera & La campaña contiene 86 998 registros, con muestras \path{city}, \path{highway} y \path{unknown}. & La validación se apoya en operación real y no solo en banco o simulador. \\
\addlinespace
Z-score como normalización & El gráfico normalizado permite comparar señales con unidades y escalas distintas. & Facilita detectar desviaciones relativas para alimentar alertas y revisión posterior. \\
\addlinespace
IA local en el ESP32-S3 & El firmware combina Welford, Z-score e Isolation Forest ligero cuando el modelo está listo. & El análisis puede ejecutarse en el borde sin depender de procesamiento remoto continuo. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Aporte del análisis estadístico e inteligencia artificial}

El gráfico de Z-score de la Figura~\ref{fig:resultados-zscore} evita comparar unidades incompatibles. Velocidad, RPM, temperatura, carga y consumo tienen escalas distintas; una lectura directa entre ellas puede llevar a conclusiones equivocadas. La normalización convierte cada señal en una medida de distancia respecto de su propio comportamiento medio. Así se identifican desviaciones relativas sin mezclar kilómetros por hora, revoluciones, grados Celsius o litros por hora.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth,height=0.54\textheight,keepaspectratio]{Figures/ai_zscore_deviation_index.png}
\caption{Índice de desviación normalizada calculado a partir del segmento urbano analizado. Visualización propia generada con los datos exportados del Excel de salida; no representa una falla mecánica confirmada.}
\label{fig:ai-zscore-deviation-index}
\end{figure}

La Figura~\ref{fig:ai-zscore-deviation-index} resume el mayor valor absoluto de Z-score entre las variables seleccionadas. La mayor parte del segmento permanece en rangos bajos; algunos picos aislados superan el umbral visual de seguimiento. Esos puntos no se interpretan como fallas. Indican momentos donde una o varias señales se alejaron de su comportamiento medio dentro del tramo analizado. Si una desviación aparece una sola vez, puede corresponder a una aceleración, una frenada, una subida o una transición normal. Si aparece de forma repetida o sostenida, el sistema la trata como condición que amerita revisión.

En el firmware, este principio se implementa mediante una línea base estadística calculada con Welford. La ventaja de este método para un sistema embebido es que permite actualizar promedio y desviación estándar sin almacenar todas las muestras históricas en memoria. Esto se ajusta al enfoque de registro y procesamiento en el borde, donde el ESP32-S3 dispone de recursos limitados y debe mantener una operación estable durante periodos prolongados. La línea base convierte la telemetría en una referencia dinámica del comportamiento esperado del vehículo.

El Z-score aporta una interpretación explicable: estima cuánto se aleja una señal de su patrón aprendido. El sistema no queda como una caja negra completa, porque puede indicar qué señales aportan más a una desviación. Una elevación simultánea de carga, consumo y caudal de aire puede corresponder a una aceleración normal; una desviación térmica sostenida requiere otra lectura, sobre todo si se mantiene fuera de la línea base.

El Tiny Isolation Forest complementa el análisis estadístico al evaluar patrones multivariables. La implementación del firmware utiliza 16 árboles, tamaño de muestra 64 y profundidad máxima 6, con inferencia local y sin depender de memoria dinámica para la etapa de evaluación. Cuando el modelo ya se encuentra listo, el puntaje final combina 70\% del resultado por Z-score y 30\% del Isolation Forest. Esta ponderación mantiene mayor peso en una medida estadística explicable, mientras agrega una capa adicional para reconocer combinaciones de señales que podrían no apreciarse con una sola variable.

\begin{table}[H]
\centering
\scriptsize
\caption{Evidencia funcional y límites del módulo de análisis local. Elaboración propia.}
\label{tab:ai-analysis-evidence}
\begin{tabular}{@{}p{0.24\textwidth}p{0.34\textwidth}p{0.30\textwidth}@{}}
\toprule
\textbf{Elemento} & \textbf{Evidencia disponible} & \textbf{Interpretación} \\
\midrule
Entrada de análisis & Muestras OBD-II compactas con \path{validMask} y señales decodificadas. & El análisis opera sobre datos ya recolectados, sin generar comandos adicionales al vehículo. \\
\addlinespace
Baseline estadístico & Welford, mínimo de 300 muestras y persistencia local del estado. & Permite construir una referencia incremental del comportamiento normal observado. \\
\addlinespace
Z-score & Segmento urbano con 775 muestras válidas; índice medio de desviación 1,27 y máximo 7,12 en la visualización externa. & Muestra desviaciones relativas, no fallas confirmadas. \\
\addlinespace
Tiny Isolation Forest & 16 árboles, muestra 64, profundidad máxima 6 y peso de 30\% cuando el modelo está listo. & Refuerza la detección multivariable con bajo costo de memoria. \\
\addlinespace
Severidades & Estados \path{NORMAL}, \path{WATCH}, \path{WARNING} y \path{CRITICAL} con \textit{debounce}. & Reduce alertas por picos aislados y prioriza persistencia. \\
\addlinespace
Métrica no evaluada & No existe conjunto de fallas mecánicas etiquetadas. & No se reportan precisión, recall ni F1 del detector. \\
\bottomrule
\end{tabular}
\end{table}

La inferencia configurada cada 10 segundos también es coherente con el propósito del sistema. AutoSense no busca reaccionar ante cada variación instantánea como si fuera una falla, sino identificar patrones persistentes que ameriten seguimiento. Por esa razón, el firmware aplica un mecanismo de \textit{debounce}: una muestra aislada puede elevar el estado a \path{WATCH}, pero para reportar estados como \path{WARNING} o \path{CRITICAL} se requiere persistencia dentro de una ventana de inferencias. Esta decisión reduce falsos positivos en condiciones reales, donde frenadas, aceleraciones y cambios de pendiente pueden alterar temporalmente varias señales.

\subsection{Implicaciones y límites del resultado}

Los resultados muestran que AutoSense funciona como una base de monitoreo predictivo porque registra evidencia continua, clasifica contexto de ruta, conserva historial y ejecuta análisis local de desviaciones. Su aporte principal es observar tendencias y condiciones fuera de patrón con suficiente continuidad para apoyar decisiones de mantenimiento: permite ver qué ocurrió, cuándo ocurrió y qué señales estuvieron asociadas al cambio.

El alcance debe interpretarse con prudencia. Los datos disponibles permiten afirmar que el prototipo captura y analiza telemetría real del Volkswagen Passat 2016, y que el módulo de IA produce indicadores de desviación basados en línea base, Z-score e Isolation Forest. Convertir una alerta en diagnóstico de una pieza específica requeriría más vehículos, fallas confirmadas, comparación contra instrumentos profesionales y evaluación controlada de falsos positivos.

La validación principal utilizó un vehículo específico y un perfil asociado a ese modelo. Aunque OBD-II permite acceder a señales comunes, la disponibilidad, precisión y comportamiento de cada PID puede variar entre marcas, modelos y años. Por ello, los resultados son sólidos para el entorno evaluado, pero la generalización exige perfiles adicionales y nuevas campañas de prueba.

En síntesis, el prototipo adquiere, registra, visualiza e interpreta señales vehiculares desde una arquitectura embebida. La combinación de registro local, clasificación de ruta, visualización móvil y análisis estadístico con Tiny Isolation Forest transforma lecturas OBD-II/CAN en evidencia continua e interpretable para seguimiento vehicular.

\newpage

% =========================
% CAPÍTULO 6
% =========================
\chapter{Conclusiones y recomendaciones}

\section{Conclusiones}

El desarrollo de AutoSense permitió comprobar la viabilidad de una arquitectura Edge-IoT para monitoreo vehicular basada en lectura OBD-II/CAN, procesamiento local en ESP32-S3, comunicación BLE, aplicación móvil y servicios de soporte en Firebase. La propuesta logró integrar hardware propio, firmware embebido, almacenamiento local, visualización móvil y análisis de datos dentro de un mismo flujo operativo. Con ello, el sistema dejó de ser únicamente un lector de parámetros OBD-II y pasó a funcionar como una plataforma de observación continua, capaz de adquirir datos del vehículo, validarlos, conservarlos y presentarlos de forma comprensible para el usuario.

Desde el punto de vista técnico, el prototipo demostró que el ESP32-S3 puede actuar como núcleo de adquisición y procesamiento en el borde. El firmware implementado permite descubrir PIDs soportados, aplicar perfiles vehiculares, consultar servicios OBD-II de solo lectura, compactar muestras, registrar historial binario y transmitir telemetría hacia la aplicación móvil. La política de solo lectura fue un elemento esencial del diseño, porque permitió mantener el alcance del sistema dentro de la observación y el monitoreo, sin ejecutar comandos de borrado, control o reprogramación sobre la ECU del vehículo.

El diseño de hardware también cumplió una función relevante dentro del resultado final. La placa AutoSense integró conector OBD-II, transceptor CAN, protección eléctrica, etapa de alimentación, USB-C y módulo ESP32-S3-WROOM-1 en una solución compacta orientada al uso vehicular. Esta integración redujo la dependencia de módulos externos y permitió validar el sistema tanto en banco como en el vehículo real. La adaptación del diseño al perfil del proyecto hizo posible contar con un adaptador propio, alineado con el firmware y con la aplicación móvil desarrollada.

En cuanto a la aplicación móvil, se logró implementar una interfaz multiplataforma con autenticación, navegación protegida, pantallas de resumen, telemetría en vivo, alertas, perfil de usuario y gestión del vehículo. La aplicación funciona como puente entre el usuario, el módulo embebido y Firebase, permitiendo visualizar el estado del vehículo y mantener información estructurada asociada a la cuenta. Las capturas y pruebas realizadas evidencian que la interfaz permite consultar información del sistema de forma organizada, separando el flujo de acceso, las pantallas principales, la conexión OBD-II y las vistas de detalle.

Los resultados de prueba muestran que AutoSense funcionó en escenarios complementarios: banco preliminar, simulador OBD-II y Volkswagen Passat 2016. La campaña de datos consolidó 86\,998 registros clasificados, de los cuales 71\,114 correspondieron a contexto urbano, 8521 a carretera y 7363 a segmentos no determinados. Esta evidencia permitió observar señales como velocidad, RPM, temperatura del refrigerante, carga del motor y consumo instantáneo en condiciones reales de conducción. La clasificación \path{unknown} se interpretó como una decisión conservadora del sistema cuando la evidencia de ruta no resultaba suficiente, no como una falla directa del prototipo.

El análisis de resultados mostró que las señales capturadas mantienen coherencia técnica entre sí. Las variaciones de velocidad se relacionaron con cambios en RPM, carga y consumo, mientras que la temperatura del refrigerante presentó una evolución más gradual hasta alcanzar un rango estable. Esta diferencia entre señales rápidas y señales lentas confirma la importancia de analizar el comportamiento vehicular por contexto y no mediante valores aislados. Por ello, el registro histórico y la clasificación de ruta aportan una base útil para distinguir entre cambios normales de operación y posibles desviaciones que ameriten seguimiento.

El módulo de análisis local aportó una capa adicional al monitoreo. La combinación de línea base estadística mediante Welford, Z-score por señal y Tiny Isolation Forest permite producir indicadores de desviación ejecutados en el borde, asignar severidades y apoyar al usuario con alertas tempranas dentro del alcance validado.

AutoSense cumple su propósito como prototipo funcional de monitoreo vehicular inteligente. El sistema adquiere datos desde el vehículo, los procesa localmente, los registra, los transmite, los visualiza y los organiza para consulta posterior. Sus resultados son válidos principalmente para el entorno evaluado, especialmente el Volkswagen Passat 2016 y los escenarios de simulación utilizados. La extensión a otros vehículos requiere perfiles adicionales, validación de PIDs disponibles y nuevas campañas de prueba. Aun con esas limitaciones, el proyecto establece una base sólida para evolucionar hacia un sistema más amplio de mantenimiento predictivo apoyado en datos reales del vehículo.

\section{Cumplimiento de objetivos}

El cumplimiento de los objetivos se evaluó a partir de la correspondencia entre lo planteado en el capítulo inicial y la evidencia desarrollada en los capítulos de diseño, implementación, pruebas y análisis de resultados. La valoración se presenta con matices, debido a que el prototipo fue validado principalmente sobre un Volkswagen Passat 2016 y sobre escenarios de simulación OBD-II, por lo que no se asume compatibilidad universal con todos los vehículos.

\begin{table}[H]
\centering
\scriptsize
\caption{Cumplimiento de los objetivos específicos del proyecto. Elaboración propia.}
\label{tab:cumplimiento-objetivos}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{0.31\textwidth}p{0.39\textwidth}p{0.20\textwidth}@{}}
\toprule
\textbf{Objetivo específico} & \textbf{Evidencia desarrollada} & \textbf{Cumplimiento} \\
\midrule
Diseñar un sistema de adquisición de datos vehiculares mediante lectura de parámetros disponibles por OBD-II/CAN. & Se diseñó e implementó una placa propia con ESP32-S3, conector OBD-II, transceptor CAN y firmware capaz de descubrir PIDs, aplicar perfil vehicular y recolectar señales como RPM, velocidad, temperatura, carga, MAP, MAF, voltaje, acelerador y consumo estimado cuando el vehículo las expone. & Cumplido dentro del alcance validado. \\
\addlinespace
Implementar un módulo de análisis de datos vehiculares que identifique variaciones anómalas y genere alertas tempranas. & El firmware incorporó línea base estadística con Welford, Z-score por señal, Tiny Isolation Forest, combinación ponderada de puntajes, debounce y severidades \path{NORMAL}, \path{WATCH}, \path{WARNING} y \path{CRITICAL}. & Cumplido como detección de desviaciones, no como diagnóstico mecánico definitivo. \\
\addlinespace
Implementar un entorno en la nube para almacenamiento estructurado, consultas históricas, visualización remota y respaldo. & La solución integró Firebase Auth, Firestore, Functions y Storage para usuario, vehículo, perfiles, estado, alertas y respaldo histórico. El ESP32-S3 conserva historial local en \path{obdlog}; la app incluye descarga incremental por BLE, archivo binario local, respaldo en Firebase Storage e índice con cursor en Firestore. & Cumplido en implementación; validación operacional parcial para exportaciones completas. \\
\addlinespace
Diseñar e integrar una aplicación móvil multiplataforma para visualizar estado, alertas e historial. & La aplicación React Native/Expo integra sesión autenticada, pantallas de inicio, eficiencia, alertas, tiempo real, perfil, vehículo y vistas de detalle. La app recibe telemetría por BLE, presenta datos en vivo y organiza información del usuario y vehículo. & Cumplido dentro de la versión funcional del prototipo. \\
\addlinespace
Validar el funcionamiento del sistema completo mediante pruebas prácticas de adquisición, comunicación, almacenamiento, visualización y análisis local. & Se realizaron pruebas en banco, simulador OBD-II y Volkswagen Passat 2016. Se verificaron comunicación USB-CDC, BLE, CAN activo, lectura OBD-II, almacenamiento, visualización móvil y una campaña con 86\,998 registros clasificados en \texttt{city}, \texttt{highway} y \texttt{unknown}. & Cumplido para los escenarios evaluados. \\
\bottomrule
\end{tabular}
\end{table}

Con base en la tabla anterior, el objetivo general se considera alcanzado en el alcance experimental definido para el proyecto. Se diseñó e implementó una arquitectura IoT vehicular con procesamiento en el borde e integración de inteligencia artificial ligera, capaz de leer parámetros críticos desde OBD-II, procesarlos localmente y visualizarlos mediante una aplicación móvil. La capacidad de expansión hacia funciones de control queda planteada desde la arquitectura y los trabajos futuros, pero no forma parte de las funciones activas validadas sobre el vehículo real. El cumplimiento no implica cobertura universal de todos los vehículos ni diagnóstico mecánico exacto, sino validación funcional de una arquitectura completa y operativa sobre el entorno probado.

\section{Recomendaciones}

Con base en el desarrollo y en las pruebas realizadas, AutoSense debe conservar su enfoque de monitoreo de solo lectura. Esa decisión reduce el riesgo de alterar el estado del vehículo y permite usar el prototipo como herramienta de observación, análisis y apoyo al mantenimiento. Cualquier ampliación debe revisar primero la política de comandos permitidos, la validación de perfiles y el contador de bloqueos del firmware, de manera que el acceso OBD-II/CAN siga limitado a servicios seguros.

La validación debe ampliarse a otros vehículos compatibles con OBD-II. Aunque el sistema fue probado principalmente sobre Volkswagen Passat 2016 y simulador OBD-II, la compatibilidad real depende de los PIDs disponibles, la respuesta de la ECU, el protocolo de comunicación y el perfil aplicado. Cada nuevo modelo requiere un perfil vehicular específico, una fase de descubrimiento de PIDs y una campaña mínima de pruebas antes de considerarlo soportado.

En relación con la recolección de datos, se recomienda continuar separando los escenarios de prueba por contexto de conducción. La distinción entre \texttt{city}, \texttt{highway} y \texttt{unknown} permite analizar las señales con mayor precisión y evita comparar condiciones operativas diferentes como si fueran equivalentes. Para futuras campañas, conviene documentar la duración de cada sesión, el tipo de ruta, el estado del vehículo, la versión del firmware, el perfil aplicado y cualquier evento observado durante la conducción.

Respecto al almacenamiento, se recomienda definir políticas de retención, consulta y depuración para los archivos históricos ya sincronizados. El flujo implementado descarga registros incrementales del ESP32-S3, conserva una copia local en el teléfono y respalda el binario en Firebase Storage, mientras Firestore mantiene el cursor y el índice de exportación. A partir de esa base, futuras versiones deberían documentar en cada exportación el rango de secuencias, el tamaño del archivo, la ruta local, la ruta en Storage y el documento Firestore asociado. También conviene establecer cuánto tiempo se conservan los archivos, cómo se agrupan por viaje o día y qué resúmenes se generan para evitar depender de la descarga completa del binario en cada consulta.

El módulo de análisis de datos necesita más calibración con sesiones reales. Welford, Z-score e Isolation Forest ligero son adecuados para análisis local, pero su precisión práctica depende de la cantidad y variedad de datos observados. Antes de asociar una alerta con una falla concreta, se debe contrastar la señal con inspecciones técnicas, códigos DTC confirmados o evidencia de mantenimiento.

En la aplicación móvil conviene mantener una diferenciación visible entre datos reales, datos demo y señales no disponibles. Esta separación evita interpretaciones incorrectas por parte del usuario. Las pantallas de alerta también deben indicar cuándo una categoría depende de información leída directamente desde OBD-II y cuándo corresponde a una regla visual, estimación o estado pendiente de validación. Esta precisión es necesaria porque la disponibilidad de señales varía entre vehículos y fabricantes.

La metodología de prueba debe seguir siendo incremental. Antes de cada campaña real, el sistema debería verificarse en banco, después con simulador y luego en vehículo detenido antes de pasar a conducción. Indicadores como \path{can=UP}, \path{rsp/s > 0}, \path{decoded/s > 0}, \path{guard=0}, perfil aplicado correctamente y secuencia de log incrementando deben revisarse como condiciones mínimas de operación. Esta práctica reduce errores de configuración, protege la integridad de los datos y permite identificar fallos antes de una sesión prolongada de recolección.

\section{Trabajos futuros}

Como trabajo futuro se propone evolucionar AutoSense desde un adaptador dependiente de la conexión móvil hacia una arquitectura con un módulo central dedicado dentro del vehículo. En la versión actual, el ESP32-S3 realiza adquisición, registro local, análisis ligero y transmisión hacia la aplicación móvil cuando esta se conecta por BLE. Una versión más avanzada podría incorporar un dispositivo central con pantalla, mayor almacenamiento, conectividad permanente y más capacidad de procesamiento. Ese módulo permitiría mostrar información y ejecutar análisis aun cuando el teléfono del usuario no esté conectado.

El dispositivo central propuesto funcionaría como una unidad de borde ampliada. A diferencia del adaptador actual, que utiliza indicadores simples y delega la visualización principal a la app móvil, esta unidad podría integrar una pantalla para presentar telemetría, alertas, estado de conexión, severidad de anomalías, historial resumido y recomendaciones básicas de revisión. Esto permitiría consultar datos esenciales desde el propio vehículo sin depender exclusivamente del celular. La aplicación móvil seguiría siendo útil para configuración, sincronización, reportes y consulta histórica, pero el monitoreo inmediato quedaría disponible en el tablero o en una pantalla auxiliar.

Otra mejora consiste en ampliar almacenamiento y procesamiento local. El log binario del ESP32-S3 conserva muestras compactas durante la conducción, pero un módulo central con memoria externa permitiría guardar ventanas más largas, agrupar sesiones por viaje, conservar eventos de alta frecuencia y mantener resúmenes históricos sin depender de una descarga inmediata. Con más recursos podrían evaluarse modelos de series temporales, redes neuronales ligeras, aprendizaje incremental, modelos híbridos estadístico-supervisados o análisis multivariable entrenado con campañas más extensas.

El avance hacia algoritmos más precisos debe mantenerse ligado a la disponibilidad de datos reales. Para que el sistema mejore su capacidad de análisis predictivo, sería necesario recolectar información de más vehículos, más rutas, más condiciones ambientales y más estados de mantenimiento. Con esa base, el módulo de análisis podría pasar de detectar desviaciones generales a reconocer patrones asociados con condiciones específicas, siempre manteniendo una separación clara entre alerta temprana, recomendación de inspección y diagnóstico mecánico definitivo. Esta evolución requeriría validar cada modelo contra evidencia técnica, códigos DTC, inspecciones de mantenimiento o registros confirmados de fallas.

También se plantea como trabajo futuro estudiar funciones de interacción avanzada con el vehículo. En la presente investigación, AutoSense se mantiene deliberadamente como sistema de solo lectura, porque su objetivo es monitorear y no modificar el comportamiento del automóvil. Una versión futura podría investigar, primero en banco y simulador, la posibilidad de enviar comandos autorizados y no críticos, por ejemplo para interactuar con funciones de confort como climatización, configuración de pantalla o estados auxiliares cuando el vehículo y su arquitectura lo permitan. Esta línea no debe implementarse directamente sobre un vehículo real sin una política estricta de seguridad, autenticación, permisos, validación de comandos, pruebas en entorno controlado y cumplimiento normativo.

En una etapa más avanzada, un módulo central con almacenamiento ampliado, sensores adicionales, análisis local más potente y comunicación bidireccional podría servir como base experimental para investigaciones de asistencia a la conducción. El manejo autónomo, sin embargo, pertenece a un campo mucho más complejo que el monitoreo predictivo desarrollado en esta tesis. Para avanzar hacia ese objetivo se necesitarían sistemas redundantes de percepción, control, actuación, localización, toma de decisiones, seguridad funcional, ciberseguridad, pruebas cerradas y validación regulatoria. En AutoSense, la autonomía debe entenderse como una línea de investigación de largo plazo, no como una extensión directa del prototipo actual.

Las próximas versiones deberían avanzar por etapas: consolidar el módulo central con pantalla y almacenamiento ampliado; optimizar consulta, retención y resúmenes históricos; entrenar modelos con más campañas reales; validar cualquier interacción activa solo en simuladores y bancos de prueba; y evaluar capacidades de asistencia o control únicamente bajo condiciones seguras. Esta secuencia permite que AutoSense crezca sin perder trazabilidad técnica, seguridad operacional ni coherencia con su propósito inicial de monitoreo vehicular inteligente.

\newpage

% =========================
% REFERENCIAS
% =========================
\begingroup
\sloppy
\printbibliography[heading=bibintoc,title={Referencias bibliográficas}]
\endgroup

\newpage

% =========================
% ANEXOS
% =========================
\appendix

\chapter{Código fuente}

El código fuente del proyecto se organiza en tres bloques principales: firmware embebido, aplicación móvil y configuración de servicios Firebase. Por razones de extensión, este anexo no reproduce archivos completos, sino que identifica los módulos relevantes para replicar, revisar o auditar el sistema.

El repositorio público del proyecto está disponible en \url{https://github.com/Fonio22/AutoSense}. Desde este enlace se puede consultar el firmware del ESP32-S3, la aplicación móvil, los perfiles vehiculares, las reglas de Firebase, los archivos de hardware y las fuentes utilizadas para generar la documentación técnica del proyecto.

\begin{table}[H]
\centering
\scriptsize
\caption{Estructura principal del código fuente de AutoSense. Elaboración propia.}
\label{tab:anexo-codigo-fuente}
\begin{tabular}{@{}p{0.28\textwidth}p{0.30\textwidth}p{0.32\textwidth}@{}}
\toprule
\textbf{Ubicación} & \textbf{Componente} & \textbf{Función principal} \\
\midrule
\path{OBD2-Firmware/src/app.cpp} & Arranque y ciclo principal & Inicializa servicios, configura CAN/BLE/USB y ejecuta el ciclo operativo. \\
\path{OBD2-Firmware/src/obd_service.cpp} & Servicio OBD-II & Descubre PIDs, planifica consultas y decodifica respuestas vehiculares. \\
\path{OBD2-Firmware/src/obd_binary_logger.*} & Registro histórico & Escribe, exporta y administra el historial binario \texttt{obdlog}. \\
\path{OBD2-Firmware/src/obd_ble_protocol.*} & Protocolo BLE & Atiende comandos de la aplicación, streaming y exportación histórica. \\
\path{OBD2-Firmware/src/anomaly_detector.*} & Análisis local & Ejecuta baseline, Z-score, Tiny Isolation Forest y severidades. \\
\path{app/src/app/(App)/realtime/} & Tiempo real móvil & Implementa emparejamiento, perfil, telemetría y sincronización OBD. \\
\path{app/src/lib/obd/} & Cliente OBD móvil & Encapsula conexión BLE, comandos y sincronización del historial. \\
\path{app/firestore.rules} y \path{app/storage.rules} & Seguridad Firebase & Define acceso por usuario a documentos, perfiles y archivos históricos. \\
\bottomrule
\end{tabular}
\end{table}

La versión compilada del firmware fue verificada mediante PlatformIO y la aplicación móvil mediante verificación estática de TypeScript. Las pruebas funcionales del resolutor de perfiles se ejecutaron sobre los casos de Volkswagen Passat 2016, perfil genérico y simulador OBD-II.

\begin{table}[H]
\centering
\scriptsize
\caption{Comandos principales para replicar la verificación del proyecto. Elaboración propia.}
\label{tab:anexo-comandos}
\begin{tabular}{@{}p{0.22\textwidth}p{0.28\textwidth}p{0.18\textwidth}p{0.22\textwidth}@{}}
\toprule
\textbf{Componente} & \textbf{Directorio} & \textbf{Comando} & \textbf{Propósito} \\
\midrule
Firmware & \path{OBD2-Firmware} & \texttt{pio run} & Compilar el firmware del ESP32-S3. \\
Aplicación móvil & \path{app} & \texttt{npx tsc --noEmit} & Verificar tipos de TypeScript. \\
Pruebas de perfiles & \path{app/functions/test} & \texttt{node --test *.mjs} & Validar resolución de perfiles vehiculares. \\
Documento LaTeX & \path{Writting} & \texttt{latexmk -pdf main.tex} & Regenerar el PDF de la tesis. \\
\bottomrule
\end{tabular}
\end{table}

\chapter{Tablas de pruebas}

Las tablas siguientes consolidan la evidencia técnica usada para validar el sistema. Los valores provienen de reportes generados por el firmware, del dashboard USB-CDC, de la aplicación móvil y del archivo Excel de salida.

\begin{table}[H]
\centering
\scriptsize
\caption{Resumen de evidencia de pruebas funcionales y de rendimiento. Elaboración propia.}
\label{tab:anexo-pruebas}
\begin{tabular}{@{}p{0.26\textwidth}p{0.25\textwidth}p{0.39\textwidth}@{}}
\toprule
\textbf{Evidencia} & \textbf{Valor o resultado} & \textbf{Interpretación} \\
\midrule
Prueba de perfiles & 3 pruebas aprobadas & El resolutor seleccionó Passat 2016, perfil genérico y simulador según el caso. \\
\texttt{can=UP} & Activo & El enlace CAN respondió durante la sesión validada. \\
\texttt{guard=0} & Sin bloqueos reportados & No se intentaron comandos fuera de la política de solo lectura. \\
Tasas OBD-II Passat & 33 a 35 rsp/s y dec/s & El firmware mantuvo lectura y decodificación activas. \\
Tasas simulador & 28 a 31 rsp/s & El sistema operó con USB-CDC y BLE simultáneamente. \\
Capacidad \texttt{obdlog} & 274720 registros & Equivale a cerca de 31.8 días con intervalo de 10 s. \\
Campaña real & 86\,998 registros & Base de resultados clasificada por contexto de ruta. \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\scriptsize
\caption{Distribución de registros de la campaña de recolección. Elaboración propia.}
\label{tab:anexo-campania}
\begin{tabular}{@{}p{0.28\textwidth}p{0.24\textwidth}p{0.38\textwidth}@{}}
\toprule
\textbf{Clasificación} & \textbf{Registros} & \textbf{Observación} \\
\midrule
\texttt{city} & 71\,114 & Registros asociados con conducción urbana. \\
\texttt{highway} & 8521 & Registros asociados con carretera. \\
\texttt{unknown} & 7363 & Segmentos conservadores sin clasificación suficiente. \\
Total & 86\,998 & Campaña aproximada de 59 días y 241.7 horas efectivas. \\
\bottomrule
\end{tabular}
\end{table}

\chapter{Manual de usuario}

Este manual resume el uso básico del sistema AutoSense desde la perspectiva del usuario final.

\begin{enumerate}
    \item Verificar que el vehículo esté estacionado en un lugar seguro antes de conectar el adaptador.
    \item Conectar el adaptador AutoSense al puerto OBD-II del vehículo.
    \item Abrir la aplicación móvil e iniciar sesión con la cuenta registrada.
    \item Ingresar a la pestaña \emph{Tiempo real} y buscar el dispositivo \texttt{AutoSense OBD2}.
    \item Conectar la aplicación al adaptador y esperar la lectura de información del vehículo.
    \item Aplicar el perfil vehicular sugerido o continuar con el perfil genérico si el modelo no está verificado.
    \item Iniciar la visualización de telemetría en vivo y revisar velocidad, RPM, temperatura, carga, voltaje y demás señales disponibles.
    \item Consultar las pantallas de alertas, eficiencia, perfil y vehículo según la información requerida.
    \item Al finalizar la sesión, desconectar desde la aplicación y retirar el adaptador solo cuando el vehículo esté detenido.
\end{enumerate}

Las alertas mostradas por AutoSense tienen carácter orientativo. El sistema ayuda a identificar desviaciones y condiciones que merecen revisión, pero no reemplaza una inspección mecánica profesional.

\chapter{Manual técnico}

\section*{Firmware}

\begin{enumerate}
    \item Abrir el directorio \path{OBD2-Firmware}.
    \item Verificar la configuración de PlatformIO y el entorno ESP32-S3.
    \item Compilar con \path{pio run}.
    \item Cargar firmware y sistema de archivos según la placa usada.
    \item Abrir el monitor serial USB-CDC para confirmar arranque, perfil activo, estado CAN y dashboard.
    \item Confirmar que el dashboard reporte \texttt{can=UP}, respuestas por segundo, decodificaciones por segundo, logger activo y \texttt{guard=0}.
\end{enumerate}

\section*{Aplicación móvil}

\begin{enumerate}
    \item Abrir el directorio \path{app}.
    \item Instalar dependencias con el gestor definido por el proyecto.
    \item Ejecutar la verificación TypeScript con \path{npx tsc --noEmit}.
    \item Usar un development build de Expo, ya que BLE nativo no funciona dentro de Expo Go.
    \item Validar inicio de sesión, conexión BLE, perfil vehicular y telemetría en vivo.
    \item Para una prueba en iOS, ejecutar la aplicación en simulador o dispositivo físico mediante el flujo de desarrollo de Expo y confirmar que el módulo BLE esté disponible en el build nativo.
\end{enumerate}

\section*{Firebase}

\begin{enumerate}
    \item Revisar reglas de \path{firestore.rules} y \path{storage.rules}.
    \item Confirmar que cada usuario solo acceda a sus documentos, perfiles permitidos y archivos históricos.
    \item Verificar que los perfiles vehiculares estén disponibles en Storage y que Functions resuelva el perfil esperado.
    \item Para sincronización histórica, confirmar el documento de cursor, el archivo binario exportado y el índice de exportación.
    \item Registrar como evidencia la ruta del objeto en Storage, el rango de secuencia exportado y el documento Firestore asociado.
\end{enumerate}

\section*{Verificación mínima}

Antes de una prueba en vehículo real se deben verificar los indicadores \texttt{can=UP}, \texttt{rsp/s > 0}, \texttt{decoded/s > 0}, \texttt{guard=0}, perfil aplicado correctamente y secuencia del log incrementando. Si alguno falla, la prueba debe detenerse y volver a la fase de banco o simulador.

\end{document}
